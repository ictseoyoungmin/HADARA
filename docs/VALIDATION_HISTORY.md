# Validation History

This document records historical external validation observations that are not current release
contracts. Current RC2 acceptance is based on task-local evidence and reproducible local source,
build, package, clean-checkout, and release-gate checks.

The current task evidence plane uses the reduced `hadara.packageSmoke.v1` report and
`PACKAGE_SMOKE_EVIDENCE` release-gate category; this marker does not claim that the historical
remote observation below is current.

## Remote CI Observation

- Historical baseline: GitHub Actions CI run #109 on `main` succeeded for commit
  `8b4f33d1bf926d051cf63e13ca2de222bfc22d8c`.
- Historical run URL: `https://github.com/ictseoyoungmin/HADARA-dev/actions/runs/26497664485`.
- This observation is retained for release-gate compatibility and must not be interpreted as current
  `0.5.0-rc.2` remote acceptance.
