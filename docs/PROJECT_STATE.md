# PROJECT_STATE

## Product

HADARA - Portable Agentic Development Workbench

## Metadata

| Field | Value |
|---|---|
| HADARA Profile | governed |
| Latest Completed Task | T-0478 0.4.0-rc.0 publish env safe-directory fix |
| Active Task | Operator-controlled 0.4.0-rc.0 npm publish |

## Next Planned Line

Phase 9 Evidence v2 writer/docs work is complete through T-0341, and stable `hadara@0.3.2` is published and recycled from installed consumer paths. T-0342 then registered the 0.3.3 context-routing specs in `docs/IMPLEMENTATION_SOP.md`, `docs/DOC_REGISTRY.md`, and `.hadara/docs-registry.json`, corrected the worker-plan path to `docs/specs/0.3.3/context-routing/...`, and unignored `docs/specs/0.3.3/**` so the staged specs are visible as docs artifacts. T-0343 through T-0352 completed C1 context graph/state projection through the read-only `hadara context graph` CLI. T-0353 through T-0359 completed C2 code link/index work through schema, ignore rules, imports/exports, symbols, command/test edges, graph integration, and explicit budget/degraded hardening. T-0360 added the detailed C6 fast context cache implementation spec. T-0361 and T-0362 completed C3 context pack schema/ranking and the public read-only `hadara context pack --task T-XXXX --json` CLI. T-0363 through T-0368 implemented the speed-first C6 line through metadata-first source manifests, cache record/status, cache warm, high-impact graph extractor shards, and git worktree fingerprint reuse so fresh cached graph/cache reads can avoid rebuilding the full source manifest. T-0369 added the C4 core raw slice read surface with `hadara.contextSlice.v1`, `hadara context slice`, safe single-file reads, explicit range, tail window, keyword-window overlap merge, and managed-section strategies. T-0371 then added the registered `08_C6_Speed_First_Graph_Build_and_Warm_Path_Spec.md` to make C6's next work explicitly speed-first around cold graph ordering, warm shard reads, code-index persistence, context-pack warm paths, and performance fixtures. T-0370 completed the remaining C4 symbol-neighborhood and context-pack candidate slicing surface, so C3 and C4 public read/slice commands became complete enough to feed C5 session-start work. T-0372 then hardened C4 before C5 consumption by enforcing the context-slice byte budget as a hard `CONTEXT_SLICE_TOO_LARGE` failure with no raw `slices[]` text, blocking `.hadara/local/**` raw slice reads by default, repairing T-0370 AC-6 drift, and adding Done-level acceptance validation for `In Progress` rows. T-0373 measured C6 graph/cache/pack behavior on both the `/mnt/f` mounted workspace and an ext4 `/tmp` copy, added `scripts/context-routing-performance-baseline.mjs`, registered `docs/CONTEXT_ROUTING_PERFORMANCE_BASELINE.md`, and updated the C6 speed-first spec with observed timings. T-0374 then made graph-only `context graph` and `context pack` reads consume explicitly warmed graph-core cache read-only. T-0375 added the code-aware companion path: `context cache warm --execute` writes `.hadara/local/cache/context/code-index.json`, and fresh `context graph --include-code` reads it without cache writes while falling back live on missing/stale/corrupt/schema-mismatched records. T-0376 then handled the bounded review-hardening batch: raw context-slice reads now deny ignored/generated `.hadara/**` surfaces by default with a small public-context allowlist, graph-core stale/corrupt/include-code coverage is tighter, the mounted/ext4 benchmark escalates stuck children from SIGTERM to SIGKILL with diagnostics, and context-pack slice candidates expose structured `suggestedCommandArgs`. T-0377 added explicit-warm-only incremental per-file code-index recompute so unchanged code files can be reused when rebuilding `.hadara/local/cache/context/code-index.json`. T-0378 added the bounded C5 `hadara session start --json` MVP over `hadara.sessionStart.v1`; default session start now returns a fast degraded no-live packet with lifecycle/current-state/context-pack guidance, while `--live` explicitly opts into slower full context-pack graph discovery. T-0379 then refined the C5/C6 bridge so default Session Start first tries a read-only warm path using a cached source-manifest fast-freshness hit plus fresh graph-core/code-index shards, and falls back to the T-0378 bounded no-live packet when freshness cannot be proven without scanning. T-0380 added advisory performance regression fixtures: the context-routing performance baseline script now measures default/include-code Session Start, supports `--thresholds`, `--workloads`, and explicit `--fail-on-regression`, and registers `docs/CONTEXT_ROUTING_PERFORMANCE_THRESHOLDS.json` as a conditional reference fixture. T-0381 added the registered `09_Context_Routing_Implementation_Completion_Audit.md`, mapped C1-C6 to implemented/partial/deferred state, and removed stale C6 future/remaining language from specs 06/07/08. T-0382 then hardened Session Start JSON/UX by adding structured `guidance` metadata, making no-task bounded `hadara session start --json` return actionable degraded task-selection guidance with `ok:true`, and keeping default reads bounded/no-live. T-0383 added `scripts/context-routing-e2e-smoke.mjs` with fast/full/custom workload profiles, package script wiring, fake CLI coverage, built fast smoke validation, and cache fingerprint checks so normal smoke runs do not write `.hadara/local/cache/context`. T-0384 then added additive cache diagnostics to status/warm reports so missing/stale/corrupt/partial states include operator summary, mounted/full-manifest slow-path metadata, manifest change counts, shard summary/planned keys, and structured warm command args. T-0385 aligned the 0.3.3 context-routing readiness docs so the implementation audit, C6 spec, and worker plan reflect T-0382 through T-0384 as complete, classify mounted broad cache/graph/pack latency as an explicit-command residual, and route the remaining hardening to T-0386/T-0387. T-0386 added shared acceptance parser v2 lifecycle hardening. T-0387 completed the final security-boundary audit by sharing raw slice path checks between `context slice` and `context pack` and filtering pack slice candidates through the same denylist/allowlist. T-0388 then clarified context pack read recommendations by adding `sourceAccess.rawSlice` metadata to builder-produced items, preserving graph relevance while marking raw-sliceability. T-0389 dogfooded 0.3.3 context routing and hardened context pack `sourceHash` fidelity so raw-sliceable items prefer the current item file text hash while retaining graph-source fallback. T-0390 through T-0392 then closed final dogfood hardening and started the lifecycle convenience line, T-0393 added read-only `hadara task lifecycle --task T --json` over `hadara.task.lifecycle.v1`, T-0394 added read-only `hadara task close-repair-plan --task T --json` over `hadara.task.closeRepairPlan.v1`, T-0395 removed redundant successful-close dry-run validation rerun next actions so agents get one close execute command after current validation is already embedded, T-0396 added read-only `hadara task finalize --task T --json` over `hadara.task.finalize.v1` with ordered lifecycle step planning, expected write paths, stable `planHash`, and explicit execute refusal, T-0397 added guarded `task finalize --execute --plan-hash` orchestration with current-plan hash matching, serial execution, stop-on-blocker behavior, and final audit proof, T-0398 aligned README, SOP, command-surface docs, and generated init docs with those lifecycle convenience surfaces, and T-0399 hardened dogfooded finalize UX by adding weak-evidence repair guidance plus lazy evaluated/skipped report diagnostics that avoid unnecessary ready/close/audit composition on unfinished tasks. The 0.3.3 context-routing specs are staged under `docs/specs/0.3.3/context-routing/`, and the lifecycle convenience spec is staged under `docs/specs/0.3.3/lifecycle/`.

Evidence v2 current boundary: `evidence add-command` writes explicit v2 `category`/`outcome` metadata and exact `resolves:`/`supersedes:` tags without shell execution or broad migration. Incompatible explicit `--result`/`--outcome` pairs are rejected with `EVIDENCE_RESULT_OUTCOME_MISMATCH`, exact resolution markers only resolve earlier failed evidence when the later evidence outcome is passed or recorded, evidence writer task lookup ignores `TASK.md`-less leftovers while rejecting ambiguous same-id valid capsules, and `evidence list` is the supported id discovery surface. T-0334 through T-0341 completed the 0.3.2 rebuild-boundary/docs/readiness/publish/recycle line.

T-0477 follow-up note: `hadara@0.4.0-rc.0` source/readiness is prepared without npm publish mutation. Package metadata and lockfile now target `0.4.0-rc.0`; README, `docs/RELEASE_READINESS.md`, `docs/RELEASE_NOTES.md`, and the T-0477 GitHub release-note draft describe the concrete 0.4 productization, dogfood, agent UX, dashboard/preflight, and release boundaries. Docker clean-copy validation passed `npm ci`, `npm run check` with 148 files / 987 tests, `npm run build`, and refreshed workspace `dist`; built CLI version smoke reports `packageVersion:"0.4.0-rc.0"` with `distLooksStale:false`; release helper shell syntax and `git diff --check` passed. Release publish dry-run returned the expected no-mutation blocked state because `NPM_TOKEN` and `GITHUB_TOKEN` are absent in this workspace. The next step is operator-controlled publish after commit: `npm login`, then `bash scripts/release/manual-publish-rc.sh T-0477 --execute`; installed-package recycle remains a separate post-publish capsule. Evidence: `ev:T-0477:d68e7155025d4ac4a2748c4e`, `ev:T-0477:c1d07b3af38d42e2a95e2c98`, `ev:T-0477:94932d7e2ded42d1bc00a777`, `ev:T-0477:e5c133488d56407286455af9`, `ev:T-0477:a65852a1ad8143f4a150758d`.

T-0478 follow-up note: the T-0477 publish environment helper residual is fixed. `scripts/release/prepare-publish-env.sh` now idempotently registers both `/workspace` and `/workspace/.git` as Git safe directories before cloning from the mounted repo, matching the failure Git reported during the operator prep run. Shell syntax passed, focused release-helper regression tests passed in a container ext4 clone after `npm ci`, and `bash scripts/release/prepare-publish-env.sh T-0477 --skip-dry-run` passed through clone, `npm ci`, build, strict release gate, and clean final worktree. Host focused Vitest failed because local `vitest` is absent and is resolved by the container/ext4 pass, so Docker/ext4 remains the validation baseline. Actual npm publish is still operator-controlled: after committing T-0478, rerun `bash scripts/release/prepare-publish-env.sh T-0477`, then `npm login` and `bash scripts/release/manual-publish-rc.sh T-0477 --execute` from `/root/hadara-publish`. Evidence: `ev:T-0478:b6bfce23a3ef4dcca669aa46`, `ev:T-0478:ddc3565699d04e1b8e9faa1c`, `ev:T-0478:0890480f746f4a788b3dcb25`, resolver `ev:T-0478:1d8c31ecd2134975b56d4ebf` for host residual `ev:T-0478:3d5d9e72e1ba45bc93115b2c`.

T-0461 follow-up note: selected-task `task status` now includes additive `authoringSuggestions` with `readOnly:true` and `writesProse:false`. The read model suggests title cleanup for handoff-like titles, Source Documents guidance, hash rows for existing concrete source paths, and conservative generic acceptance guidance without writing task prose or inventing domain-specific criteria. Dashboard fast selected-task detail also includes the additive field. Docker focused workbench/schema/dashboard tests and TypeScript build passed, workspace `dist` was refreshed, and built CLI status smoke confirmed the suggestion payload and duplicate title-signal suppression. Evidence: `ev:T-0461:d6486726fd0b4eea8775d3b0`, `ev:T-0461:dd3a7a42646e4e8cae779c1c`. The next UX capsule should inspect fresh `hadara init` output and reduce verbosity or add a compact quickstart path without weakening generated workflow safety.

T-0462 follow-up note: generated `docs/HADARA_WORKFLOW.md` now starts with a compact Quickstart before Minimal Loop, giving first actions for new project entry, work selection, task creation, routed file reads, and finalize close without adding new scaffold files. Fresh governed init smoke created 15 files, reported Quickstart at line 9 before Minimal Loop at line 21, and `init doctor` returned `ok:true` with no issues. Scoped Docker init/schema tests and TypeScript build passed. Evidence: `ev:T-0462:84b3176bc6cd4cde96c34534`, `ev:T-0462:ca54a04cfc91403f91eb81b5`. A broad focused attempt that included `docs-doctor.test.ts` failed on older docs-doctor fixture assumptions and is recorded as residual evidence `ev:T-0462:e0c87ae662b84f8fa20128cc`; the next UX capsule should target mounted-workspace progress/latency diagnostics for long silent status/finalize runs.

T-0463 follow-up note: `task status` and `task finalize` now attach additive CLI diagnostics to JSON/text output, including command path, elapsed duration, slow threshold, slow flag, and slow-run note. Focused Docker status/finalize/schema tests and TypeScript build passed, workspace `dist` was refreshed, and direct built CLI smokes proved mounted-workspace diagnostics output. Evidence: `ev:T-0463:d7ed90ac429d428eb84ce44a`, `ev:T-0463:b8afb3afd6544e5d8ce7319f`, `ev:T-0463:e51a09de51e649ab9d9f1f45`. This is after-command latency visibility, not live progress; T-0464 should choose between live progress/narrower status composition and cleanup of repeated validation-fixture residuals.

T-0464 follow-up note: `task finalize --execute` now emits stage progress lines to stderr for finish, refresh, ready, close, and audit while keeping the final report on stdout for JSON consumers. Focused Docker `task-finalize` tests and TypeScript build passed, workspace `dist` was refreshed, and a disposable basic-project built CLI smoke proved progress lines appear before the final blocked JSON report. Evidence: `ev:T-0464:51c58e7d001d42b8b4b009c2`, `ev:T-0464:c3125378748d4fb79980cfe1`. The requested five priority capsules are complete; remaining UX decisions should be explicitly selected.

T-0465 follow-up note: finalize dry-run now reports staged required-write plans as `planStatus: executable-with-deferred-checks` with `deferredChecks` and `partialExecutionRisk`, and required write next actions warn that execute may stop after re-evaluating deferred checks. Focused Docker finalize/schema tests and TypeScript build passed, workspace `dist` was refreshed, and built CLI staged-plan smoke passed. Evidence: `ev:T-0465:5bab047c37bf4178a8c94cb9`, `ev:T-0465:f4e0591df698476c8d583886`, `ev:T-0465:a3639a0719a5477cb621b1d9`. Remaining UX cleanup candidates are next-action `message`/`summary` duplication, Change Summary line-range authoring/parser behavior, and mounted status/finalize performance.

T-0466 follow-up note: lifecycle next actions now emit canonical `summary` without redundant duplicate `message`, close execute next actions use the same helper, and workbench conversion preserves its intentional UX `message` by falling back to lifecycle `summary`. Focused lifecycle tests passed, TypeScript build passed, workspace `dist` was refreshed, and built CLI finalize smoke showed `summary`-only lifecycle next actions. Evidence: `ev:T-0466:a4416987992f42febb201e3c`, `ev:T-0466:4f9f3002cd4444c4a454d6d3`, `ev:T-0466:014bbcd28b074852b9d85fdf`. RF-1 records adjacent `tests/unit/task-close-repair-plan.test.ts` close-source hash classification failures as a follow-up outside the dedupe scope; evidence `ev:T-0466:a5cdbfddcf1f470887af9188` is resolved by `ev:T-0466:35bccc2385c34ee7be1e8d1d`.

T-0467 follow-up note: `task close-repair-plan` is now positioned in registry/docs as a conditional read-only repair diagnostic for stale, invalid, duplicate, or missing close proof, not an ordinary lifecycle loop command. The T-0466 close-repair-plan fixture drift is resolved against current close-source semantics. Change Summary line validation now accepts single lines, ergonomic ranges, comma-separated ranges, and special file markers with clearer examples, and selected-task `task status` now emits read-only `.git`-derived Change Summary candidate rows without writing TASK.md. Focused Docker tests and TypeScript build passed with refreshed `dist`; built CLI status smoke confirmed git-backed candidate rows and recorded a slow mounted status diagnostic. Evidence: `ev:T-0467:e6450a6e21b6450dbaae39ed`, `ev:T-0467:a469046512334522b0bc0418`. Next high-signal follow-up is mounted `task status` performance if agent UX remains slow.

T-0468 follow-up note: default selected-task `task status --task T-XXXX --json` is now a fast loop cockpit and skips close/protocol-grade diagnostics unless `--detail full` is explicitly requested. Git-derived Change Summary candidates were removed from default status, and new Task Capsule scaffolds/templates now use `Path / Area / Change / Reason / Evidence`; legacy `Lines` tables remain validator-compatible. Focused Docker tests and TypeScript build passed with refreshed `dist`; built CLI smoke showed T-0468 default status completed fast, `--detail full` remained available for heavy diagnostics, and a fresh init plus task create produced the new Area header. A final fast closed-valid metadata correction passed focused task-workbench tests and build. Evidence: `ev:T-0468:91055b787fda40469bca06b5`, `ev:T-0468:73df1894fd45461c9d043e28`, `ev:T-0468:ea4c3539ac3144bd8d299aab`. Remaining high-signal follow-up is explicit full diagnostics/finalize mounted-workspace performance, not default status latency.

T-0469 follow-up note: `task finalize` now owns stale close-proof repair by turning drift into a guarded finalize close append plan; `task status` and dashboard/workbench next actions return no lifecycle action for `closed-valid`; direct `task close-repair-plan` CLI/registry guidance was removed from the public agent loop while the internal classifier remains for now. Focused Docker tests passed 8 files / 68 tests, Docker build passed with refreshed `dist`, and built CLI status/help smokes passed. Evidence: `ev:T-0469:557a38f24fca4928a1893911`, `ev:T-0469:95dfca9fc3c24f79b75bfec5`, `ev:T-0469:efd716488ee4420cb7d94697`. Remaining high-signal follow-up is still explicit full diagnostics/finalize mounted-workspace performance.

T-0470 follow-up note: task-scoped close/finalize diagnostics now avoid broad task-capsule scans by using exact task lookup in close, harness, and task protocol paths, and close-plan data is reused inside finalize, lifecycle, complete-flow, and the internal repair-plan composition where available. Focused Docker tests passed 8 files / 89 tests plus a targeted task-scoped protocol smoke, Docker build passed with refreshed `dist`, and mounted built CLI smokes showed T-0469 audit-close improving from about 15.6s to about 8.5s and full status from 20749 ms to 12009 ms. Evidence: `ev:T-0470:00190a9390e54a3db393d461`, `ev:T-0470:f5fc721056374e47b4a4ea43`, `ev:T-0470:b0beb3b22fea47f1b51b7c78`. Remaining choices are stale broad protocol fixture cleanup and/or global docs/profile diagnostics performance.

T-0471 follow-up note: protocol consistency now treats current 0.4 `TASK.md` Acceptance as the default Done-level acceptance source when legacy `ACCEPTANCE.md` is absent, while preserving sidecar compatibility for older capsules. Broad protocol fixtures were updated away from removed default `FILES.md`/`ACCEPTANCE.md` assumptions, and harness done-level guidance now shows current TASK.md Acceptance table examples. Focused Docker protocol consistency tests passed 20 tests, related harness/task-capsule tests passed 36 tests, Docker build passed with refreshed `dist`, and built CLI task-scoped protocol doctor smoke passed. Evidence: `ev:T-0471:f1ba74206a9c4900a0dc68aa`, `ev:T-0471:f7a1d36929d7422fab03d9b9`, `ev:T-0471:0062866455bb449ebee07c0e`, `ev:T-0471:c9c0dfa110ec49a98e152ba8`. Next cleanup candidate is the remaining legacy sidecar reference audit across task-close, evidence-lint, release/debt, template/upgrade, and TUI read-model surfaces.

T-0472 follow-up note: current 0.4 `TASK.md` section fallback is now wired into release closeout capsule surfaces, state projection plan drift, harness Done-level plan drift, operational debt premature-acceptance detection, evidence semantic lint residual documentation, and TUI task summaries/read order. Legacy sidecars remain readable where old capsules or compatibility fixtures provide them, but removed default sidecars are no longer treated as required current surfaces in these paths. Focused service suites passed 47 tests, harness/TUI regression suites passed 61 tests, Docker build passed with refreshed `dist`, and built release-closeout smoke returned `ok:true` with 9 shared/current capsule surfaces. Evidence: `ev:T-0472:464654f09f824d09ad4e6a4e`, `ev:T-0472:ddf2c1f180054178b95a26a1`, `ev:T-0472:758500c96600471bb12c7bb8`, `ev:T-0472:abb18b6756a440a89fc68fd6`. Remaining choices are global docs/profile diagnostics performance or a narrow compatibility-only audit of migration/template/write-preflight/historical fixture references.

T-0474 follow-up note: dashboard aggregate API route latency profiling is complete. Task-scoped timeline evidence lookup now uses exact task lookup instead of broad task capsule listing, status/tasks/bootstrap routes can reuse process-cache status/task-list reports, and the built route timing smoke showed task-scoped timeline bypass at 91ms plus bootstrap second hit at 1ms after the first cached read. Focused dashboard/workbench tests passed, Docker build refreshed `dist`, and built route timing smoke passed. Evidence: `ev:T-0474:e0d2c6eb9ca448e9858bacb4`, `ev:T-0474:814e9786faaa41aabd4b0087`, `ev:T-0474:7878feaa7ef14577b16e08ff`. The next pre-release cleanup is the user-requested narrow compatibility-only legacy sidecar cleanup across historical specs, old fixtures, migration/template, and write-preflight references.

T-0475 follow-up note: compatibility-only legacy sidecar cleanup is complete for current user-facing surfaces. Task templates no longer carry ignored sidecar factories, task upgrade-scaffold targets current TASK/EVIDENCE/HANDOFF frames, `write preflight task create` predicts only the current 0.4 capsule files plus Task Board, and TUI default detail tabs expose `TASK.md`, `EVIDENCE.md`, and `HANDOFF.md` instead of removed sidecar docs. Legacy sidecar read compatibility remains for old capsules and explicit fixtures. Expanded create/write-preflight/TUI/upgrade-scaffold tests passed, Docker build refreshed `dist`, built CLI smoke verified preflight/template file lists, and close audit returned `closed-valid`. Evidence: `ev:T-0475:0fec147f12bb41c0bd76a38d`, `ev:T-0475:2f903acccdb647639c859021`, `ev:T-0475:e93357bbff7d4ea18e287b79`. The next pre-release cleanup is final release-line code preflight hardening before explicit 0.4.0 release readiness work.

T-0473 follow-up note: default selected-task workbench projections now stay on the fast detail path unless full diagnostics are explicitly requested. `createTaskWorkbenchReport` defaults to fast, dashboard task-workbench accepts `detail=full` only as an opt-in, and dashboard bootstrap/timeline selected-task summaries pass fast detail explicitly. Focused workbench/dashboard tests passed 30 tests, Docker build passed with refreshed `dist`, and built CLI smokes showed default fast status completing in 73ms while explicit full diagnostics remained available and took 9119ms on the mounted workspace. Evidence: `ev:T-0473:6c0e0c8b66ff402fa51ad313`, `ev:T-0473:4fdec13ad3af42928d1d71d8`, `ev:T-0473:2dd3fa4b13d04089bf651e34`. T-0474 handled dashboard aggregate route latency; the remaining selected cleanup is compatibility-only sidecar references.

T-0460 follow-up note: agent UX global option parsing now supports known global options before the command token. The CLI entry normalizes leading `--project <path>` and `--json` before dispatch while preserving command-first invocations and strict option-value validation. Built CLI smokes passed for `node dist/cli/main.js --json --project . version` and `node dist/cli/main.js --project /tmp/hadara-t0460-global-init init --profile basic --json`; Docker focused args tests and TypeScript build passed, and workspace `dist` was refreshed. Evidence: `ev:T-0460:708e920cff184178b4a650d5`, `ev:T-0460:4951e6416ffc4c0bad478427`. Broader command-specific option reordering remains out of scope unless another dogfood friction appears.

T-0459 follow-up note: agent UX hardening continued after T-0458. Source Documents path validation now normalizes Markdown inline-code, angle-bracket, and Markdown-link cells before hash checks, so human-readable `TASK.md` source paths do not become false missing-file blockers. Session Start now recommends `task status` for no-task and selected-task primary guidance instead of teaching `task next` or `task lifecycle`. `hadara init --help` is read-only and does not create scaffold files. Fresh governed init dogfood in `/tmp/hadara-init-t0459-check` created 15 files, produced 435 lines across AGENTS/context/workflow/state/handoff, returned `init doctor ok:true`, and showed no stale `task next` / `task lifecycle` command recommendation in fresh workflow guidance. Focused Docker validation passed 3 files / 50 tests and TypeScript build passed; broad `dev:docker-sync-build` still failed on unrelated historical fixture drift and is recorded as a scoped residual. Evidence: `ev:T-0459:469f12fd91d04455b1587491`, `ev:T-0459:e70d6bc192f446d7ba8b0a95`, `ev:T-0459:d64e7340878e4e57ac628a3d`. T-0460 completed the immediate command-independent global option parsing follow-up.

T-0458 follow-up note: `hadara task status` is now the default lifecycle cockpit. Without `--task`, it returns `hadara.task.status.v1` selection guidance and embeds the compatibility next-work source; with `--task`, the existing workbench report now includes additive `loop.phase`, `loop.primaryNextAction`, and deprecated command replacements. `task next` and `task lifecycle` remain callable compatibility commands but are no longer registry/help/docs primary lifecycle commands. The current workflow docs, generated init workflow, README, AGENTS guidance, schema docs, and `docs/specs/0.4.0/lifecycle/` specs are aligned to status-first finalize flow. Focused Docker validation passed 7 files / 49 tests, TypeScript build passed, workspace `dist` was refreshed, built CLI status smokes passed, and `git diff --check` passed. Evidence: `ev:T-0458:23de043e969f4cfe821911da`, `ev:T-0458:43643f1971ac43a2a6a74e6c`, `ev:T-0458:904b129902fa47839432ede7`. Session-start alignment was completed by T-0459.

T-0400 follow-up note: 0.3.3 agents now use the finalize-first lifecycle as the default path. Root docs, generated init docs, registry-backed `help lifecycle`, and the lifecycle guide JSON projection present `task lifecycle` followed by reviewed `task finalize --json` / guarded `task finalize --execute --plan-hash <hash>` as the ordinary close loop. Low-level `task finish`, `task ready`, `task close`, and `task audit-close` remain available for debugging, recovery, and command implementation work, but they are no longer the primary agent-facing lifecycle.

T-0401 follow-up note: `hadara@0.3.3-rc.0` source/readiness was prepared without publish mutation. Package metadata and lockfile target `0.3.3-rc.0`; README, release notes, release readiness, and README assertions are aligned; Docker sync-build passed 141 files / 929 tests with refreshed `dist`; release artifact, package smoke, clean-checkout smoke, strict release gate, release dry-run, publish dry-run, built version smoke, and `git diff --check` passed. Package smoke and clean-checkout smoke first failed under sandbox npm cache constraints, then passed on approved external reruns. npm publish, GitHub Release creation, Docker/PyPI publish, installer execution, and MCP release/package execution did not run in T-0401.

T-0402 follow-up note: `hadara@0.3.3-rc.0` is published on npm with dist-tag `next`. The approval-gated helper completed npm publish, npm registry verification returned version `0.3.3-rc.0`, dist-tags verified `next=0.3.3-rc.0` and `latest=0.3.2`, tarball shasum matched `3088fca4b4a91b257680ffddf53ab8a0543d6264`, and a temporary-prefix consumer install executed the published `hadara` bin successfully. GitHub Release draft was not requested; Docker/PyPI publish, installer execution, and MCP release/package execution did not run. A broader post-publish recycle capsule remains an optional next release-line decision.

T-0403 follow-up note: the 0.3.3 dogfood SaaS line is specified at `docs/specs/0.3.3/dogfood/00_Procedural_Asset_SaaS_Dogfood_Spec.md` and registered in SOP/docs registry surfaces. The spec defines PatternForge, a Docker Compose runnable procedural material asset SaaS focused on user-assisted image-region-to-editable-material extraction. The first product family is `grass-field-v1`; the default path uses deterministic local CPU image processing and user-assisted masks, with optional ML segmentation deferred behind a future profile. Fifteen capsules are enough for a demo-grade MVP, so the spec allocates 22 capsules to cover product MVP, production hardening, HADARA init/lifecycle/context dogfood audits, consolidated findings, and stable 0.3.3 decision input.

T-0404 follow-up note: PatternForge dogfood findings and stable decision input were imported into `tasks/T-0404-0-3-3-dogfood-findings-release-hardening/artifacts/patternforge/`. PF-F-012 was fixed by classifying cached Task Board nodes from source metadata when graph cache payloads omit `kind`, and PF-F-010 was fixed by suppressing warning-only handoff refresh next actions once a task is closed-valid while preserving error-level handoff actions. Focused Docker validation with guarded `dist` sync passed, built PatternForge `task status` and `context pack` smokes passed, and host focused validation failure was recorded as an environment dependency issue. Stable `0.3.3` still needs a full release readiness refresh before publish or dist-tag promotion.

T-0405 follow-up note: stable `hadara@0.3.3` source/readiness is prepared without publish mutation. Package metadata and lockfile target `0.3.3`; README, release notes, release readiness, and README assertion tests are aligned with stable `0.3.3`; Docker focused validation with guarded `dist` sync passed after raw full Docker validation passed on rerun; release artifact evidence, package smoke evidence, clean-checkout smoke evidence, strict release gate, release dry-run, and `git diff --check` passed. `npm view hadara@0.3.3 version` returned E404, confirming the exact stable version is not yet published. The manual publish helper stopped at npm `whoami` E401 because this environment is not logged into npm; the next capsule should handle operator npm login and approval-gated stable publish.

T-0406 follow-up note: stable `hadara@0.3.3` is published on npm with the `latest` dist-tag. The operator ran the approval-gated helper, npm publish completed, registry verification returned `0.3.3`, dist-tags verified `latest=0.3.3` and `next=0.3.3-rc.0`, and a temporary-prefix consumer install executed the published `hadara` bin with `packageVersion: 0.3.3` plus `help lifecycle` returning `ok:true`. GitHub Release draft was not requested; Docker/PyPI publish, installer execution, and MCP release/package execution did not run.

T-0407 follow-up note: stable `hadara@0.3.3` is recycled from installed npm consumer paths. A temporary prefix installed `hadara@latest`, the installed bin reported `packageVersion: 0.3.3`, `help lifecycle` returned `ok:true`, governed `init` worked in a disposable project, task create/status/lifecycle/finalize read-model surfaces worked, context graph/pack/slice/cache status/cache warm/cache warm execute/session start smokes passed, and temporary prefix/project cleanup completed. Fresh-project context commands produced expected degraded/minimal-project warnings while returning `ok:true`.

T-0408 follow-up note: 0.3.4 is defined as Agent UX Hardening in `docs/specs/0.3.4/agent-ux/00_Agent_UX_Hardening_Spec.md` and registered in the docs registry/SOP. The line is based on HADARA-dev usage friction after 0.3.3 rather than another PatternForge repeat. The budget starts with T-0409 Handoff Stale Known-Problem Detector, then release closeout planning, evidence compact id UX, finalize post-close drift guidance, installed-package recycle scripting, session-start primary-action hardening, context-pack actionability, init generated-doc cleanup, and 0.3.4 release readiness/publish/recycle. Dashboard productization, full agent runtime, broader MCP writes, vector retrieval, and default provider execution remain deferred.

T-0409 follow-up note: `hadara handoff stale-problems --json` now emits `hadara.handoff.staleProblems.v1` as a read-only advisory report over `docs/AGENT_HANDOFF.md` Current Known Problems rows. It reports target before-hash, row counts, candidate stale rows, confidence, matched sources, reasons, and suggested human actions without deleting or rewriting handoff rows. Candidate detection is conservative: completed-task matches require stale review wording such as `needs closeout`, while release/version matches require publish/recycle pending-style wording. Focused Docker build/tests passed for handoff/schema fixtures, workspace `dist` was refreshed, built CLI smoke returned `ok:true` and `candidates:0` on the current repo, and `git diff --check` passed. A full `npm run dev:docker-sync-build` attempt failed on pre-existing dashboard/evidence timeout behavior and is recorded as failed evidence, not as a T-0409 feature failure.

T-0410 follow-up note: `hadara release closeout --version <version> --task <task-id> --json` now emits `hadara.releaseCloseout.v1` as a read-only release closeout planning report. It inspects release readiness, release notes, shared state docs, and selected release capsule docs; classifies surfaces as `current`, `stale`, or `missing`; reports expected/matched/missing signals and roles; and returns suggested Markdown fragments without writing files, appending evidence, building artifacts, publishing packages, or creating GitHub releases. Focused Docker build/tests passed for release-closeout/schema fixtures, workspace `dist` was refreshed, built CLI smoke returned `ok:true` for `0.3.4`/`T-0410`, and `git diff --check` passed.

T-0411 follow-up note: `hadara evidence summary --task <task-id> --json` now emits `hadara.evidence.summary.v1` as a read-only compact evidence id report over the existing evidence list source. It returns compact records, latest evidence, latest close evidence, and copy hints for durable ids without appending evidence, rewriting `EVIDENCE.md`, migrating evidence, or changing `evidence list` semantics. Text mode abbreviates long summaries and idempotency tags for terminal use while JSON keeps full records. Focused Docker build/tests passed for evidence-summary/evidence-json/schema fixtures, focused workflow/schema docs tests passed, workspace `dist` was refreshed, built CLI JSON/text smokes returned `ok:true` for T-0410 evidence, and `git diff --check` passed.

T-0412 follow-up note: `task finalize` and `task lifecycle` now classify close-source drift from audit verdicts instead of treating warning-only audit reports as closed-valid. When the latest close proof no longer matches current close-source files, finalize marks the audit step required, adds `TASK_FINALIZE_CLOSE_SOURCE_DRIFT_GUIDANCE`, and routes agents to `hadara task close-repair-plan --task <task-id> --json`; lifecycle reports `repair-required` with `closed-stale` repair metadata. The lifecycle read model also omits undefined optional command fields. Docker build passed, focused finalize/lifecycle tests passed 14 tests, focused workflow docs tests passed 4 tests, workspace `dist` was refreshed, built CLI finalize/lifecycle smokes returned expected guidance, and `git diff --check` passed.

T-0413 follow-up note: `hadara package recycle --json` now emits `hadara.packageRecycle.v1` as a dry-run-first post-publish installed-package recycle plan. `--execute` explicitly runs npm registry metadata checks, isolated-prefix install, installed CLI version/lifecycle/init/task/context/session smokes, and cleanup while keeping publish and release mutation flags false. The command is registered in the command registry/tools projection, reduced `package-recycle` evidence summaries are supported, release readiness documents the standard post-publish command, focused Docker build/tests passed 5 files / 19 tests, workspace `dist` was refreshed, and built CLI dry-run returned `ok:true` without registry/install execution.

T-0414 follow-up note: `hadara session start --json` now includes additive concrete first-action guidance: `guidance.primaryAction`, `guidance.whyThisNow`, `guidance.avoidForNow`, and `guidance.nextCommandArgs`. Task-scoped Session Start prioritizes the read-only `task lifecycle --task <id> --json` command before status/context/finalize decisions, while no-task Session Start points to `task next --json`. Session Start remains read-only and does not warm cache, append evidence, run validation, read raw slices, or mutate project state. Focused Docker build/tests passed 4 files / 23 tests, workspace `dist` was refreshed, and built CLI Session Start for T-0414 returned `primaryAction.id=task-lifecycle`.

T-0415 follow-up note: `hadara context pack --json` now includes additive read-only `agentActions` with priorities, reasons, commands, structured `commandArgs` where available, source/slice ids, paths, and `writeBoundary:"read-only"`. Ranking reasons now explicitly call out task-local, implementation, test, symbol, known-problem, and command-surface relevance, and graph-connected task-local paths receive a bounded score bonus before broad project docs. Bounded no-live Session Start envelopes include empty `agentActions` to preserve the shared context-pack type. Focused Docker build/tests passed 4 files / 29 tests and refreshed workspace `dist`; built CLI context pack smoke passed in a disposable `/tmp` project. A mounted workspace full-project live context pack smoke produced no output for about 90 seconds and was interrupted; this is recorded as an accepted residual broad-read performance behavior, not a T-0415 feature blocker.

T-0416 follow-up note: generated `hadara init` docs now prioritize the 0.3.4 context-aware agent loop. Fresh generated `AGENTS.md` includes a compact `Default Agent Loop` showing `task next`, `session start`, `task lifecycle`, reviewed `task finalize`, and guarded finalize execute before broader rules. Generated `docs/IMPLEMENTATION_SOP.md` and `docs/TASK_WORKFLOW_COMMANDS.md` now include `hadara session start --task T-XXXX --json` in the standard task loop and describe the path as context-aware while preserving low-level `task finish`, `task ready`, `task close`, and `task audit-close` as debugging/recovery proof-boundary commands. Focused Docker build/tests passed 2 files / 22 tests and refreshed workspace `dist`; built CLI init smoke passed in a disposable `/tmp` project.

T-0417 follow-up note: `hadara@0.3.4-rc.0` source/readiness is prepared without publish mutation. Package metadata and lockfile target `0.3.4-rc.0`; README distinguishes stable npm `hadara@0.3.3` from source candidate `0.3.4-rc.0`; release notes and release readiness describe the 0.3.4 Agent UX Hardening line and defer publish to T-0418. Docker focused build/tests passed and refreshed `dist`; built CLI version smoke reported `packageVersion:"0.3.4-rc.0"` and `distLooksStale:false`; release artifact execute generated 0.3.4-rc.0 tarball/checksum/manifest evidence; ext4 release dry-run returned ready with blockers 0; publish dry-run returned ok true with npm/GitHub token/approval warnings and no mutation; package smoke dry-run and `git diff --check` passed. Mounted release dry-run exceeded 30s and was interrupted, so release readiness proof used the ext4 `/tmp/hadara` copy.

T-0419 follow-up note: the dashboard API route timeout observed during the 0.3.4 RC publish helper full test run was fixed before retrying T-0418. `/api/status` now omits the operational debt scan because debt has explicit routes, and `/api/dashboard/bootstrap` defaults to the fast `core` tier while preserving explicit `tier=full`. Focused Docker validation for `tests/unit/dashboard-static.test.ts` passed 15 tests, with the shared read-model API route test completing in 945 ms, and workspace `dist` was refreshed from the Docker build output. T-0418 remains the active approval-gated publish capsule and must be rerun from a publish clone refreshed to include this hotfix.

T-0420 follow-up note: the full-suite timeout failures reported from the 0.3.4 RC publish clone were hardened before retrying T-0418. The dashboard bootstrap service default now matches the fast `core` first-paint path unless `tier=full` is requested explicitly, and Vitest has an explicit 30s per-test/hook timeout with environment overrides for release-validation environments. Docker ext4 validation passed the six-file timeout regression set (34 tests) and the full Vitest suite (144 files / 947 tests in 19.61s), and workspace `dist` was refreshed. T-0418 remains the approval-gated npm publish capsule; rerun it only from a clone refreshed to include T-0420.

T-0421 follow-up note: the next T-0418 publish helper attempt reached clean-checkout smoke and exposed a remaining dashboard API route bottleneck. The legacy dashboard `/api/debt` route now uses the same fast dashboard debt projection as `/api/dashboard/debt` instead of the full operational-debt scan; full operational-debt diagnostics remain on explicit diagnostic surfaces. Docker ext4 focused validation passed 2 files / 19 tests with the dashboard API route test completing in 2535 ms, a preserved clean-checkout full check passed 144 files / 947 tests in 29.59 s, workspace `dist` was refreshed, and `git diff --check` passed. T-0418 remains the approval-gated npm publish capsule; rerun it only from a `/root/hadara-publish` clone refreshed to include T-0421.

T-0418 publish note: `hadara@0.3.4-rc.0` is published on npm with the `next` dist-tag. The approval-gated helper completed npm publish, npm registry verification returned version `0.3.4-rc.0`, and GitHub Release draft creation was intentionally skipped. Post-publish installed-package recycle remains the next release-line proof before deciding stable `0.3.4` readiness.

T-0422 follow-up note: `hadara@0.3.4-rc.0` has post-publish installed-package consumer proof. npm registry checks returned `0.3.4-rc.0`, dist-tags verified `next=0.3.4-rc.0` and `latest=0.3.3`, an isolated temp-prefix `hadara@next` install reported `packageVersion:"0.3.4-rc.0"`, installed `help lifecycle`, fresh init, task-scoped Session Start, task lifecycle, `task finalize --json` dry-run report generation, task-scoped context pack, context slice, and temp cleanup passed. The published `hadara package recycle --execute` helper itself failed its extra installed `context graph --json` smoke and created stray source-workspace smoke capsules during failed runs; those stray T-0423/T-0424 artifacts were removed. T-0423 later fixed this helper residual. Stable `0.3.4` has not been decided.

T-0423 follow-up note: the T-0422 package-recycle helper residual is fixed before stable `0.3.4` readiness. `hadara package recycle` now defaults to the fast installed-agent UX path: registry metadata, isolated install, installed version/help/init, task lifecycle, session start, task finalize dry-run report, task-scoped context pack, context slice, and cleanup. Broad installed `context graph --json` smoke is opt-in through `--include-graph`. Installed subprocesses strip inherited `HADARA_PROJECT_ROOT`, preventing source-workspace smoke capsules during disposable consumer runs. Docker focused validation passed for package recycle and registry/tool projections with guarded `dist` sync; built dry-runs proved default graph omission plus graph opt-in; networked installed `hadara@next` recycle passed for `0.3.4-rc.0` with `latest=0.3.3` and `next=0.3.4-rc.0`. Stable `0.3.4` readiness is still not started.

T-0424 follow-up note: the 0.4 breaking productization redesign specs were finalized and canonicalized as T-04A0 without Required Reading/docs-registry registration. The duplicate imported package layout was replaced by the registration-ready canonical tree `docs/specs/0.4.0/productization-redesign/`, `.gitignore` now unignores `docs/specs/0.4.0/**`, and `README.md`, `TREE.txt`, and `manifest.json` describe the canonical set. The design now states that HADARA product defaults must stay generic and exclude HADARA-dev-specific validation, Docker, npm, release, repository, or project-history details; authoring ownership guidance is centralized in `docs/HADARA_WORKFLOW.md` and registries rather than repeated in every Task Capsule; `EVIDENCE.md` is CLI projection with proposed `hadara evidence project`; task-local `HANDOFF.md` is continuation guidance and not a default raw close-source file; same-protocol `init upgrade` is distinct from blocked 0.3-to-0.4 migration; and `14_Worker_Agent_Capsule_Plan.md` begins with T-0424/T-04A0 followed by T-04A1 registration. Focused docs validation passed via manifest JSON parse, stale nested-path search, and `git diff --check`. Stable `0.3.4` readiness remains a separate pending release-line task.

T-0425 follow-up note: the 0.4 workflow templates were clarified as T-04A0b before registration. The 0.4 `AGENTS.md` template now keeps compact Required Reading, reading tiers, and top-level safety rules, while `HADARA_WORKFLOW.md` owns project start, task lifecycle command order, Task Capsule document timing, evidence/context/repair guidance, and useful CLI by situation. The standalone workflow Release Boundary section was removed, while external mutation approval remains a top-level AGENTS safety rule. `02_Agent_Entry_and_Workflow_Document.md`, `13_Test_Dogfood_and_Release_Plan.md`, and `14_Worker_Agent_Capsule_Plan.md` were aligned so T-04A1 registration uses the clarified canonical spec set. Focused docs validation passed via manifest JSON parse, release-boundary wording absence in workflow-facing docs, required section presence checks, trailing-whitespace scan, and `git diff --check`. Stable `0.3.4` readiness remains a separate pending release-line task.

T-0426 follow-up note: the 0.4 template final review was accepted by the operator on 2026-06-30. The review clarifies that `AGENTS.md` owns Required Reading while `.hadara/context/HADARA_CONTEXT.md` is only a compact routing anchor, adds a minimal 0.4 `HADARA_CONTEXT.md` template, removes `Scope`, `Out of Scope`, and task-local `Decisions` from the default 0.4 `TASK.md`, clarifies that task-local `HANDOFF.md` `Next Recommended Step` points to next capsule/global-state work rather than same-capsule lifecycle chores, and adds read authority, lifecycle entry gate, evidence truthfulness, finalize dry-run review, common failure mode, and minimal-loop guardrails to `HADARA_WORKFLOW.md`. The spec now states that 0.4 does not generate `docs/IMPLEMENTATION_SOP.md` or `docs/TASK_WORKFLOW_COMMANDS.md` by default; their generic workflow responsibilities consolidate into `docs/HADARA_WORKFLOW.md`. It also resolves document registration ownership: 0.4 uses `hadara docs register` over canonical `.hadara/docs-registry.json`, keeps `init register-doc` as 0.3.x/current-baseline compatibility, and treats `docs/DOC_REGISTRY.md` as optional generated projection. The worker plan now uses a 24-capsule post-acceptance implementation budget with self-review hardening, polish, cleanup, and final review/docs cleanup room, while deferring release readiness, publish, package recycle, and stable release work outside that budget. Focused docs validation passed in `ev:T-0426:496e55c598814f8d8a09cff6`. T-04A1 registration is the next 0.4 capsule.

T-0439 follow-up note: reviewer feedback hardening was applied before the full legacy mutation boundary implementation. `docs register` now accepts and persists v2 metadata for new entries (`readTier`, `authority`, `editPolicy`, `activeForTasks`, and drift metadata); `docs read-map` treats active 0.4 task docs as `TASK.md`, `HANDOFF.md`, and `EVIDENCE.md`; nested `docs/specs/**` discovery is recursive but bounded and excludes temp/eval/task fixture trees; `task finish` shared-state advisories honor docs-registry/profile absence; stale Done-level placeholder guidance no longer points agents at removed Scope/Out of Scope sections; and evidence projection wording now matches the generated `EVIDENCE.md` projection-file implementation. Docker ext4 build/focused tests passed, workspace `dist` was refreshed, built CLI smokes passed, and `git diff --check` passed. Evidence: `ev:T-0439:b7d2205ef1744eb5b87ec87c`, `ev:T-0439:86ff7918e98f424eac9686c7`, `ev:T-0439:62d3893695344992b22ca881`. The next 0.4 task remains full T-04A13 legacy project mutation blocking.

T-0440 follow-up note: full T-04A13 legacy project mutation blocking is implemented. 0.4 mutation CLI surfaces now fail closed when `.hadara/scaffold.json` is missing or declares a non-0.4 protocol, returning `hadara.legacyProjectBoundary.v1` diagnostics before writing; guarded surfaces include task create/execute-mode task mutations, init upgrade/register-doc/enable-integration mutation paths, docs register/patch execute, evidence append/migration execute, and release artifact/publish execute. HADARA-dev now carries generic `.hadara/scaffold.json` so it dogfoods the same supported-project rule. Docker focused validation passed 5 files / 33 tests, workspace `dist` was refreshed, built CLI smokes passed, and `git diff --check` passed. Evidence: `ev:T-0440:8ee5f6fde6e74b3e97487556`, `ev:T-0440:ced75760191e43c8aa18b42a`, `ev:T-0440:04b3e65ed59544da89d3ee77`. The next 0.4 task is T-04A14 Session Start Read-Map Integration.

T-0441 follow-up note: T-04A14 Session Start Read-Map Integration is implemented. Task-scoped `hadara session start --task T-XXXX --json` now includes compact docs read-map metadata derived from `docs read-map`, adds a `docs-read-map` guidance command, and surfaces active task Source Document drift through existing TASK source-hash validation; no-task Session Start remains bounded without task read-map metadata. Docker focused validation passed 3 files / 44 tests, workspace `dist` was refreshed, built CLI smoke passed for T-0441 Session Start read-map metadata, and `git diff --check` passed. Evidence: `ev:T-0441:619636a6d5a34be2a42bf1d9`, `ev:T-0441:22402ccb85324b76a2d2bc79`. The next 0.4 task is T-04A15 Context Pack Read-Map Integration.

T-0442 follow-up note: T-04A15 Context Pack Read-Map Integration is implemented. `hadara context pack --task T-XXXX --json` now consumes docs read-map policy when the canonical docs registry is present, injects read-map active task/spec entries into default read buckets, prioritizes `active-task` and `active-spec` ahead of current-state/workflow docs, and filters read-map `doNotReadByDefault` paths out of default `readFirst`/`readIfNeeded`. Docker focused validation passed 3 files / 24 tests, workspace `dist` was refreshed, built CLI smoke passed for T-0442 context-pack read-map metadata/exclusion behavior, and the initial active-spec budget miss was resolved by `ev:T-0442:fb4472fd84544f7ea682ade0`. Evidence: `ev:T-0442:0442975b9ace4166a801f5a6`, `ev:T-0442:fb4472fd84544f7ea682ade0`. The next 0.4 task is T-04A16 Authoring Guidance Read Models.

T-0443 follow-up note: T-04A16 Authoring Guidance Read Models is implemented. `hadara task status --task T-XXXX --json`, `hadara task lifecycle --task T-XXXX --json`, and `hadara task finalize --task T-XXXX --json` now include additive read-only `authoringGuidance` with `writesProse:false`, task-owned section items, and placeholder/pending/current status so agents can see what prose/table slots to update next without CLI prose mutation. Docker ext4 focused build/tests passed 3 files / 22 tests, workspace `dist` was refreshed, and built CLI smoke passed for T-0443 across all three reports. Evidence: `ev:T-0443:5b1829f6efe44f15913e30b4`, `ev:T-0443:966731c29ab64153af2f79e8`. The next 0.4 task is T-04A17 Init Doctor and Profile Diagnostics.

T-0444 follow-up note: residual reviewer feedback after T-0439 is implemented. Fresh 0.4 docs registries now use `hadara.docsRegistry.v2` while legacy `hadara.docs.registry.v1` reads and 0.3 protocol migration remain compatible; docs read-map keeps active 0.4 task docs as `TASK.md`, `HANDOFF.md`, and `EVIDENCE.md` while treating legacy `CONTEXT.md` as conditional/historical when present; generated evidence wording now describes `EVIDENCE.md` as a CLI-generated projection file; and legacy SOP registration errors route 0.4 users to `hadara docs register`. Docker ext4 build/focused tests passed 5 files / 53 tests, workspace `dist` was refreshed, built CLI smokes passed, done-level harness validation passed, and `git diff --check` passed. Evidence: `ev:T-0444:68cba6d6c6e84a9f84e879ca`, `ev:T-0444:5cba035f87e74b3692ac3df6`. The next 0.4 task remains T-04A17 Init Doctor and Profile Diagnostics.

T-0445 follow-up note: T-04A17 Init Doctor and Profile Diagnostics is implemented. `hadara init doctor --json` now reports warning diagnostics for `INIT_AGENTS_COMMAND_COOKBOOK`, `INIT_CONTEXT_DUPLICATES_WORKFLOW`, `INIT_REQUIRED_READING_TOO_BROAD`, and `INIT_PRODUCT_DEFAULT_LEAK` while keeping fresh governed scaffolds doctor-clean. Docker ext4 build/focused tests passed 3 files / 26 tests, workspace `dist` was refreshed, built CLI smokes passed for fresh scaffold cleanliness and the new diagnostics, and `git diff --check` passed. Evidence: `ev:T-0445:c894a34281b648be844445e2`. The next 0.4 task is T-04A18 Command Registry, Help, and Schema Alignment.

T-0446 follow-up note: T-04A18 Command Registry, Help, and Schema Alignment is implemented. The command registry now covers current 0.4/current surfaces including `task.close-source`, `evidence.summary`, `evidence.project`, and `state.verify`, and exposes proposed-but-unimplemented `docs.complete-spec` and `docs.mark-drift` as `status: planned` / `requiredness: disabled` without schema fixtures or CLI handlers. Registry-backed help now uses 0.4 lifecycle wording and prints command status. Docker ext4 build/focused command registry/help/schema tests passed 4 files / 38 tests, workspace `dist` was refreshed, built CLI smokes passed, and `git diff --check` passed. Evidence: `ev:T-0446:1fc3397609c84c049282d0e2`. The next 0.4 task is T-04A19 Product Default Cleanup.

T-0447 follow-up note: T-04A19 Product Default Cleanup is implemented. Product-default leakage detection now flags concrete release/package command guidance in generated scaffold docs while preserving generic safety wording. Generated Markdown across basic, standard, and governed init profiles is statically checked for HADARA-dev, Node/npm, Docker, machine-local path, package-version, and concrete release/package command leakage; fresh governed init remains doctor-clean; and intentional `hadara release publish ...` leakage reports `INIT_PRODUCT_DEFAULT_LEAK`. Docker ext4 build/focused init/docs-registry/command-registry tests passed 3 files / 29 tests, workspace `dist` was refreshed, built CLI smokes passed, and `git diff --check` passed. Evidence: `ev:T-0447:b19bfcb789b64223bb4f4f45`. The next 0.4 task is T-04A20 Basic Profile Dogfood.

T-0448 follow-up note: T-04A20 Basic Profile Dogfood is complete. A disposable `/tmp` basic profile project passed `init --profile basic`, `init doctor`, required/forbidden scaffold file assertions, generated-doc product-default checks, task create, evidence append/projection/summary, context pack, lifecycle, finalize dry-run review, guarded finalize execute, repair close path after authoring drift, final finalize execute, and audit-close `closed-valid`. Done-level validation correctly blocked invalid source-document authority/hash and placeholder HANDOFF rows during dogfood; the practical close-source repair-path note was recorded in `.hadara/context/MEMORY.md`. Evidence: `ev:T-0448:9a048c17494b4a9fa625d603`. The next 0.4 task is T-04A21 Governed Profile Dogfood.

T-0449 follow-up note: T-04A21 Governed Profile Dogfood is complete. A disposable `/tmp` governed profile project passed `init --profile governed`, `init doctor`, governed scaffold file assertions, docs register/read-map/context-pack/session-start routing, task create, evidence append/projection/summary, lifecycle, finalize dry-run review, guarded finalize execute, close-source repair after HANDOFF wording drift, final audit-close `closed-valid`, and generated-doc/prose leakage assertions. A stale pre-finish done-level harness report was expected before finish/finalize and was not a product defect. Evidence: `ev:T-0449:a81f3af0c4ab408eba907092`. T-0450 followed with lifecycle UX hardening.

T-0450 follow-up note: T-04A22 Lifecycle UX Hardening is complete. `hadara validation run` now executes real validation commands, records durable validation evidence from exit status, refreshes `EVIDENCE.md`, updates the matching `TASK.md` Validation row, and accepts `--resolves` / `--supersedes` markers for failed-evidence repair loops. `task finalize --json` now exposes additive `state`, `planStatus`, `blockingIssues`, and `pendingWrites` fields so close-evidence-only pending work is reported as `ready-to-close` / executable. Evidence projection now displays exact-marker resolved failed evidence as `Resolved` with the resolver id. Focused tests, TypeScript build, built CLI smoke, done-level harness, evidence lint, and diff check passed. Evidence: `ev:T-0450:91632ae5de42456aa4e2c608`, `ev:T-0450:ea07a22c3f7e4630a2987e12`, `ev:T-0450:52e434e359144e9387c5c591`. The next 0.4 task should continue bounded hardening/polish from the worker-agent capsule plan; release-line work remains separate.

T-0451 follow-up note: T-04A23 Validation Run Workflow Polish is complete. Generated 0.4 `docs/HADARA_WORKFLOW.md`, the 0.4 spec template, registry-backed default help, and lifecycle guide now present `hadara validation run` as the ordinary post-validation evidence path. `evidence add-command` remains canonical but is conditional/non-default for already-run operator-supplied results. Focused init/help/lifecycle/registry tests, TypeScript build, done-level harness, evidence lint, and diff check passed. Evidence: `ev:T-0451:c977116a0d344e49b78c69a7`, `ev:T-0451:4c3c982e9f2d405489410abe`, `ev:T-0451:9a87b48cef7e481e8bb3faf1`. The next 0.4 task is T-04A24 final review and documentation cleanup; release-line work remains separate.

T-0452 follow-up note: T-04A24 Final Review and Documentation Cleanup is complete. Final handoff/readiness guidance was reconciled after T-0451 so the implementation-budget line no longer points to another generic 0.4 hardening/polish capsule. `task next` pointed at T-0452 while active, stale T-0450 next-work wording was removed, and release readiness/publish/package recycle/stable release work remains explicitly outside the completed 24-capsule implementation budget. Evidence: `ev:T-0452:25accc6961dc44e293b7041f`. The next step is an operator decision to start a separate 0.4 release-line capsule if desired.

T-0453 follow-up note: T-0453 starts the requested 5-15 capsule agent UX refactor loop based on direct HADARA dogfood. The scoped first fix is complete: `validation run` records evidence without automatic `TASK.md` Validation row sync by default, with `--update-task` reserved for deliberate close-source prose updates. Focused validation-run/init/lifecycle tests, TypeScript build, done-level harness, evidence lint, and diff check passed. Evidence: `ev:T-0453:facedecf71cf4747adfdd522`, `ev:T-0453:bff69d665fcb466fb9bc910b`, `ev:T-0453:6a76b8b335fb4151b6d9f92a`. The next agent UX capsule should address validation attempt/latest-result semantics. This is not release-line readiness, publish, package recycle, or stable release work.

T-0454 follow-up note: T-0454 is the second capsule in the requested 5-15 capsule agent UX refactor loop. `validation run` now tags attempts with a stable `validation-check:<key>`, returns attempt metadata in `hadara.validation.run.v1`, and automatically adds `resolves:<id>` tags for earlier unresolved failed or blocked attempts when a later same-check attempt passes. The dogfood probe intentionally failed and then passed the same check; `ev:T-0454:385fa69b38dc4641839a69bb` resolved `ev:T-0454:f3a4b2dfcbdd44b39c90d9f6`. Focused validation-run tests and TypeScript build passed in `ev:T-0454:10fe55713fac48b2907d76b6` and `ev:T-0454:0287e6d080ec411880afc44b`. Final direct done-level harness validation passed in `ev:T-0454:fe8b5a505bd94cbaa6805dc4`, resolving two nested-spawn wrapper blocked records. The next agent UX capsule should project current validation state per check so agents do not have to inspect raw evidence lists for latest-attempt status, and should triage `validation run` nested spawn EPERM handling plus `evidence add-command --help` recording default evidence.

T-0455 follow-up note: T-0455 is the third capsule in the requested 5-15 capsule agent UX refactor loop. `task status --json` now includes `sources.evidenceList.validationAttempts`, grouping validation attempts by stable check key or summary fallback and exposing latest status, unresolved failed/blocked counts, latest evidence ids, and resolution evidence ids. The text status output also shows compact validation check and unresolved counts. Focused workbench tests passed in `ev:T-0455:ec70182bf0f9491292013cf1`, TypeScript build passed in `ev:T-0455:d17cc01c4fd4444da2e9ace8`, and built CLI dogfood against T-0454 reported four validation checks with zero unresolved failed/blocked attempts in `ev:T-0455:d4b3ea9ddcc549fda9eaeeb5`. The next agent UX capsule should repair command hazards from dogfood: `validation run` nested spawn EPERM handling and `evidence add-command --help` recording default evidence.

T-0456 follow-up note: T-0456 is the fourth capsule in the requested 5-15 capsule agent UX refactor loop. `evidence add-command --help` now resolves before mutation guards and required `--task` parsing, so help prints without appending evidence. Focused evidence CLI tests passed in `ev:T-0456:3575c0472d5b464585261a79`, TypeScript build passed in `ev:T-0456:dbf8b1a1dc8d4707b5d9469c`, and built CLI smoke confirmed `evidence add-command --task T-0456 --help` left the capsule `evidence.jsonl` line count and `EVIDENCE.md` hash unchanged in `ev:T-0456:2c697ca98ea7410a9a9f23d9`. The next agent UX capsule should repair `validation run` wrapper error semantics for nested spawn EPERM behavior.

T-0457 follow-up note: T-0457 is the fifth capsule in the requested 5-15 capsule agent UX refactor loop. `validation run` blocked wrapper outcomes now expose `execution.commandStarted`, stable `execution.failureKind`, structured `execution.error`, and fallback `nextActions`, separating command launch failure from test failure. Focused validation-run tests passed in `ev:T-0457:63bc490c0f524cc0b5b748e3`, TypeScript build passed in `ev:T-0457:28fb374a36e641bab90bd53d`, and built CLI blocked-wrapper smoke passed in `ev:T-0457:ded129c4252440a593372c75` while resolving expected blocked smoke evidence `ev:T-0457:7b8c74f89a86445ab2e25e53`. The next UX candidate is lifecycle/status/finalize progress or fast-path semantics for long silent mounted-workspace runs.

Phase 8 / 0.3.1 is complete through post-rc1 documentation cleanup. It carried Work Item A and Work Item F into implementation as status token governance, document ownership/write-boundary governance, task handoff close-state clarity, installed-package recycle findings cleanup, state consistency projection, and advisory verification gates. The Phase 8 specs are staged under `docs/specs/0.3.1/`; the first rc1 implementation sequence is staged under `docs/specs/0.3.1/rc1/`. Phase 8.1 through Phase 8.6 are complete through T-0324, T-0325 completed the follow-up CloseState derived-state cleanup after review, T-0326 prepared `hadara@0.3.1-rc.1` release readiness without publish mutation, T-0327 published `hadara@0.3.1-rc.1`, verified npm visibility/tarball contents, hardened future rc publish tags, and corrected npm dist-tags so stable `0.3.0` stays on `latest` while rc1 is available as `next`, T-0328 verified the published rc1 package from installed consumer paths in the `hadara-dev` container, and T-0329 refreshed post-rc1 handoff/release-note wording after reviewer feedback.

Phase 7 Surface Refactor is the completed 0.3.0 line. It covers command registry/help, lifecycle guide, document registry, managed sections, docs cleanup, release hardening, and 0.3 adoption migration. The Phase 7 specs are staged under `docs/specs/0.3.0/`; Phase 7.0 through Phase 7.6 are implemented, T-0297 prepared and verified the operator npm publish for `hadara@0.3.0-rc.0`, T-0298 prepared `hadara@0.3.0-rc.1` metadata hardening to correct the immutable rc.0 npm metadata gap, T-0299 added dry-run-first `hadara protocol migrate --target 0.3.0` adoption support for existing projects, T-0300 fixed the task evidence preservation blocker, T-0301 prepared and verified the operator npm publish for `hadara@0.3.0-rc.1`, T-0302 verified the published npm package through registry metadata, npx/global install, help/docs/migration surfaces, and 10 temporary lifecycle capsules, T-0303 started the `0.3.0-rc.2` workflow UX hardening mini-line by making `.hadara/context/HADARA_CONTEXT.md` a project-context scaffold and migration surface, T-0304 added incremental documentation timing plus read-parallel/write-serialized guidance to root and generated workflow docs, T-0305 preserved Task Board `Notes` plus extra cells during `task finish` while still updating command-owned ID/title/status/capsule cells, T-0306 added additive remediation hints to harness/ready/close blocker reports, T-0307 documented semantic Required Reading tiers in root and generated workflow docs, T-0308 added matching additive `tier` metadata to `docs required-reading --json`, T-0309 hardened migration/docs cleanup execute writes before rc.2 readiness, T-0310 prepared and published `hadara@0.3.0-rc.2`, T-0311 added a project-root containment guard to the shared atomic text write helper before publish/recycle, T-0312 verified the published rc.2 package through registry metadata, npx/temp-prefix install, fresh init/docs surfaces, migration execute, and task finish preservation smokes, T-0313 committed HADARA-dev's own docs registry artifacts so context routing no longer points to absent files, T-0314 hardened `docs patch --execute` to use the shared atomic helper with failure-preservation coverage, T-0315 prepared stable `hadara@0.3.0` source/readiness without publish mutation, T-0316 published stable `hadara@0.3.0` to npm and verified `npm view` returned `0.3.0`, and T-0317 verified stable `0.3.0` consumer install/recycle through registry metadata, temp-prefix installed execution, fresh init/docs, migration, finish preservation, and mini lifecycle smokes while carrying forward exact-npx and governed-docs warnings.

T-0297 applied prepublish reviewer feedback after T-0296: root README became package-facing for `hadara@0.3.0-rc.0` install/npx examples, `task complete` is documented as optional read-only workflow compression outside the primary lifecycle block, npm discovery metadata was added to `package.json` and release artifact staging, the stale T-0296 handoff was corrected/reclosed, and the duplicate Phase 7 bundle under `docs/specs/phase7_surface_refactor/` was removed in favor of canonical `docs/specs/0.3.0/`. The operator then published `hadara@0.3.0-rc.0` to npm; `npm view hadara@0.3.0-rc.0 version` returned `0.3.0-rc.0`, registry publish time is `2026-06-11T10:43:37.012Z`, and the `latest` dist-tag points to `0.3.0-rc.0`. Post-publish inspection found the immutable rc.0 tarball has current dist/README content but package metadata lacks intended discovery fields. T-0298 bumped the source candidate to `hadara@0.3.0-rc.1`, made the manual publish helper prefer `node dist/cli/main.js` over global `hadara`, and blocked publish if generated tarball package metadata lacks description, keywords, repository, homepage, or bugs fields. T-0299 then kept rc.1 publish deferred, fixed README install guidance to the current published rc.0, pointed package metadata at the public `HADARA` repo, and added `protocol migrate --target 0.3.0` with project/task scope, before-hash execute guards, docs registry/managed section adoption writes, command surface docs refresh, and protocol-version reporting. T-0300 fixed the release-blocking task-scope migration bug from T-0299: existing task `evidence.jsonl` is now skipped and preserved instead of being planned as an empty update, missing indexes are still created, and T-0299 task-local handoff drift was corrected and reclosed. T-0301 added final rc.1 release notes, README/release-readiness alignment, a release capsule/version guard that rejects stale task ids such as T-0297, and dry-run cleanup for generated release outputs so a fresh `/tmp` clone can run the helper dry-run and then `--execute` without manual cleanup. T-0301 validation passed `bash -n` for the manual helper, a T-0297 mismatch guard smoke, a dry-run cleanup smoke, and sandbox-external `/tmp` `npm run check` with 117 files / 749 tests passing. The operator then published `hadara@0.3.0-rc.1` to npm through the T-0301 helper; release artifact, package smoke, and clean-checkout smoke evidence were refreshed; `npm publish` completed; `npm view` observed `hadara@0.3.0-rc.1` after two retries; GitHub Release draft was not requested.

T-0361 follow-up note: the internal C3 context pack contract now exists as `hadara.contextPack.v1`, with deterministic bounded ranking over injected C1/C2 graph reports, validation suggestions, write-boundary hints, slice-candidate metadata, known problems, state/source/cache summaries, and warning-level budget/degraded issues.

T-0362 follow-up note: public `hadara context pack --task T-XXXX --json` now exposes the C3 read plan over the current graph, supports `--include-code`, `--budget`, `--max-items`, and `--max-read-first`, and is registered in command/CLI docs. It remains read-only and does not implement C4 `context slice` or C6 persistent cache writes. Built graph-only and include-code smokes passed from refreshed `dist`, but both used live graph/index reads with `cache.used:false`; graph-only read 990 sources and include-code read 1307 sources on this mounted workspace. The next 0.3.3 capsule should prioritize C6.1 Source Manifest and Shared Discovery before relying on context pack for routine fast session start.

T-0363 follow-up note: C6.1 now has an internal `hadara.context.sourceManifest.v1` schema and metadata-first manifest builder for project-relative source discovery, source kind/extractor-key mapping, optional previous content-hash carry-forward, manifest comparison, subset hashes, ignore boundaries, and budget/degraded warnings.

T-0364 follow-up note: C6.2 now has `hadara.context.cacheRecord.v1` and `hadara.context.cacheStatus.v1`, schema-guarded cache record/source-manifest helpers under `.hadara/local/cache/context`, stable source manifest hashes that ignore generation timestamp/caller command, read-only `hadara context cache status --json`, and command registry metadata. Focused Docker build/tests passed, full Docker sync-build passed on rerun with 133 files / 855 tests and refreshed `dist`, and built cache-status smoke passed. It still does not populate cache through a public warm command or rewire `context graph`, `context pack`, or code index to consume cached records.

T-0365 follow-up note: the registered C6 detailed spec was hardened so speed is the primary design constraint. `docs/specs/0.3.3/context-routing/07_C6_Fast_Context_Cache_and_Performance_Implementation_Spec.md` records Graphify-inspired lessons and HADARA-specific boundaries, first-build and warm-cache optimization requirements, cache warm phases, required code changes, and a cache-warm-first C6.3 sequence. `docs/specs/0.3.3/context-routing/06_Worker_Agent_Implementation_Plan.md` routes C6.3 to source-manifest cache warm phase 1 before shard/code-index integration.

T-0366 follow-up note: C6.3 phase 1 now exposes `hadara context cache warm [--execute] --json` with `hadara.context.cacheWarm.v1`, command registry/schema/docs registration, dry-run write plans, execute writes for `.hadara/local/cache/context/source-manifest.json`, stale/corrupt repair tests, and built CLI smokes proving dry-run, execute, fresh status hit, and fresh no-op behavior. `source-manifest.ts` now uses git candidate enumeration before filesystem fallback to avoid broad generated/local walks; mounted-worktree dry-run improved from about 23.8s before optimization to about 10.5s after. Fresh status/warm checks still perform safe metadata comparison and remain around 10.6s on the 4k+ source mounted workspace, so C6.4/C6.5 shard fingerprints, watcher/index acceleration, or bounded degraded hot paths remain required before C4 broad slicing relies on cached context speed.

T-0367 follow-up note: C6.4 now writes and reads high-impact graph extractor shards for `extractTaskBoard`, `extractDocsRegistry`, and `extractCommandRegistry` under `.hadara/local/cache/context/extractors/`. `context cache warm --execute` can populate these schema-valid cache records, warm dry-run reports shard plans, and `context graph` consumes fresh shards read-only while falling back to live extraction on missing/stale/corrupt/schema-mismatched records. Subset-hash validation keeps task-board shards fresh across unrelated source-code edits and invalidates them on relevant Task Board changes. Docker validation passed 133 files / 863 tests, `dist` was refreshed, and built cache warm dry-run proved shard plan output.

T-0368 follow-up note: C6.5 now persists git worktree fingerprints in source manifests and uses them as a fast freshness proof for cache status/warm and `context graph` shard orchestration. Fresh cached reads can reuse the cached source manifest instead of rebuilding the full current manifest; dirty/untracked context sources are still statted as a bounded subset, context-irrelevant cache files are excluded from the status hash, mtime-only changes in clean git worktrees remain fresh, and mismatches/non-git/unsupported rename-copy status fall back to full manifest comparison. `context graph` reports additive source-manifest fast-path metadata in its cache block. Docker validation passed 133 files / 865 tests and refreshed `dist`. Code-index shard persistence and graph node/edge cap semantics remain future C6 scope; C4 slicing can proceed against the faster graph/cache read path, then pull C6.6 forward if code-heavy slices remain too slow.

T-0369 follow-up note: C4 core slicing now exposes `hadara context slice --path <path> --from <line> --to <line> --json`, `--tail`, `--keyword --window`, and `--managed-section` over `hadara.contextSlice.v1`. The implementation reads one explicit project file, rejects unsafe/private/generated boundaries and binary-looking files, returns original marker-bounded or line-bounded text with source hash metadata, merges overlapping keyword windows, and keeps payload/line budgets warning-backed. Docker full validation passed 134 files / 871 tests, Docker sync-build refreshed `dist`, and built CLI context-slice smoke passed. Symbol-neighborhood and context-pack candidate slicing remain follow-up work.

T-0371 follow-up note: C6 now has a focused speed-first execution spec at `docs/specs/0.3.3/context-routing/08_C6_Speed_First_Graph_Build_and_Warm_Path_Spec.md`. The document explicitly compares Graphify-style changed-file graph workflows with HADARA's cache-is-not-truth boundary, then routes future implementation toward code-index shard persistence, graph-core shard reuse, context-pack warm paths, explicit warm execute shard phases, and performance regression fixtures. Docs-only validation passed with evidence `ev:T-0371:32ca91e0fcb248688b17900b`.

T-0370 follow-up note: C4 now supports `hadara context slice --path <path> --symbol <name> --json` and `hadara context slice --task <task-id> --candidate <candidate-id> --json` over `hadara.contextSlice.v1`. Symbol slices use deterministic C2 export extraction to return bounded original-text neighborhoods with derived confidence. Candidate slices resolve the current C3 context pack, require an exact `sliceCandidates[]` id, and delegate to explicit-range, managed-section, or symbol-neighborhood strategies without implicit cache writes. Docker focused C4/schema/registry validation passed 5 files / 53 tests, Docker sync-build passed 134 files / 876 tests and refreshed `dist`, and built CLI symbol/candidate smokes passed. Initial `dev:docker-check` had a dashboard-static timeout, later resolved by standalone dashboard-static and full sync-build evidence.

T-0372 follow-up note: C4 context slice now enforces the 512 KiB byte budget as a hard failure: over-budget requests return `ok:false`, `CONTEXT_SLICE_TOO_LARGE`, `summary.truncated:true`, and `slices:[]` instead of warning while carrying oversized raw text. Raw context-slice reads from `.hadara/local/**` are denied by default with `CONTEXT_SLICE_OUTSIDE_PROJECT` because local cache/private state is not canonical source text. T-0370 AC-6 drift was repaired, and done-level harness/protocol validation now treats `ACCEPTANCE.md` rows marked `In Progress` or `Partial` as incomplete. Docker focused validation passed 3 files / 57 tests, Docker sync-build passed 134 files / 880 tests and refreshed `dist`, built CLI boundary smokes passed, and `git diff --check` passed. Evidence: `ev:T-0372:153fbfd1cc37407a99fd7ec1`.

T-0373 follow-up note: C6 now has a repeatable context-routing performance benchmark at `scripts/context-routing-performance-baseline.mjs` and a registered result document at `docs/CONTEXT_ROUTING_PERFORMANCE_BASELINE.md`. Built CLI measurements with one sample showed mounted `/mnt/f` behavior is the hard constraint: cache status 14.1s mounted vs 1.7s ext4, cache warm dry-run 14.0s vs 1.6s, graph 44.7s vs 2.2s, graph include-code 65.0s vs 2.9s, and context pack 53.6s vs 2.2s. The C6 speed-first spec now routes the next implementation toward mounted-safe freshness proof and graph-core/context-pack warm shard consumption before C5 consumes graph/pack live. Validation included script syntax, docs registry JSON parse, mounted/ext4 measurement, `git diff --check`, a focused rerun for an initial dashboard-static timeout, and full Docker sync-build retry passing 134 files / 880 tests with refreshed `dist`. Evidence: `ev:T-0373:24fae15138814164be27956f`.

T-0374 follow-up note: C6 graph-core/context-pack warm path is now implemented. `context cache warm --execute` writes a merged non-code graph-core shard at `.hadara/local/cache/context/graph-core.json`; `context graph --json` consumes a fresh graph-core shard read-only before live fallback; and `context pack --task <id> --json` inherits that warm graph path without writing cache state. Focused Docker validation passed 4 files / 33 tests, Docker sync-build passed 134 files / 882 tests and refreshed `dist`, built CLI cache warm/graph/pack smoke passed from `/workspace/dist`, and `git diff --check` passed. Code-aware `context graph --include-code` still performs live code-index extraction after the graph-core hit, so C6 code-index shard persistence remains the next speed-sensitive follow-up before code-heavy C5 defaults. Evidence: `ev:T-0374:860bb8bc1a8845eb8fd03eb8`, `ev:T-0374:86aabccd90cc46b3875731f7`, `ev:T-0374:1500f663db95403ea409838c`.

T-0375 follow-up note: C6 code-index shard persistence is now implemented. `context cache warm --execute` writes a whole-report `hadara.codeIndex.v1` shard at `.hadara/local/cache/context/code-index.json`; `context graph --include-code --json` consumes a fresh code-index shard read-only and reports mixed cache modes such as `graph-core+code-index` or `graph-core+live-code`; and stale/missing/corrupt/schema-mismatched code-index records fall back to live extraction without writes. Focused Docker temp-workspace validation passed 4 files / 41 tests, full Docker temp-workspace `npm run check` passed 134 files / 885 tests, workspace `dist` was refreshed from Docker build output, and built CLI version/cache-warm smokes passed. First warm still performs full code-index extraction, and full-repo include-code graph smoke still surfaces existing unresolved JSON import warnings; incremental code-index recompute and JSON import resolver quality remain follow-up scope. Evidence: `ev:T-0375:b292024f4a504e08b624f834`, `ev:T-0375:cf8bf56ec33d4847be643074`, `ev:T-0375:a4f37048f61b4709b68d8550`.

T-0376 follow-up note: Review hardening items 1-4 are complete before returning to the original C6/C5 plan. Raw context-slice reads now deny `.hadara/**` by default except canonical public-context allowlist entries such as `.hadara/docs-registry.json` and `.hadara/context/HADARA_CONTEXT.md`; `.hadara/tmp`, `.hadara/run`, `.hadara/local`, and `.dashboard-visual` are covered by regression tests. Graph-core cache tests now cover stale invalidation for task, project-state, handoff, and evidence source changes, corrupt graph-core payload diagnostics, and graph-core plus code-index include-code cache hits. The context-routing performance benchmark now records `killGraceMs`, `killedSignal`, and `processError`, handles child process errors, and escalates timeout cleanup from SIGTERM to SIGKILL. Context-pack slice candidates now expose structured `suggestedCommandArgs` while keeping shell-quoted `suggestedCommand` for compatibility. Docker focused validation passed 5 files / 38 tests, Docker ext4 temp-workspace `npm run check` passed 135 files / 889 tests, workspace `dist` was refreshed, built CLI version and context-slice allow/deny smokes passed, and `git diff --check` passed. Evidence: `ev:T-0376:fc7d0da873a64f9b879d6f84`.

T-0377 follow-up note: C6 incremental code-index recompute is implemented for explicit warm execute. `context cache warm --execute` now feeds source-manifest entries into code-index extraction, reads/writes local `.hadara/local/cache/context/code-index-files/*.json` per-file summary records, reuses unchanged summaries, recomputes changed/missing/corrupt/schema-mismatched files live, and still writes the merged `.hadara/local/cache/context/code-index.json` shard. Per-file summaries store extracted metadata such as hashes, lines, imports, exports, command mentions, and registry command-id lines, not raw source text. Read commands remain non-mutating: `context graph --include-code` consumes a fresh merged code-index shard read-only or falls back to live extraction without writing per-file cache. Docker focused validation passed 2 files / 26 tests, Docker sync-build passed 135 files / 891 tests and refreshed `dist`, and built CLI cache-warm/include-code graph smokes passed. Evidence: `ev:T-0377:0dc1b0f01e0f4902aebe2b82`, `ev:T-0377:d6bf4440e99f41938bef26e7`, `ev:T-0377:b6d0843a157743f7b681170c`, `ev:T-0377:811d2af2ef5142b4be235cc2`.

T-0378 follow-up note: Bounded C5 Session Start MVP is implemented. `hadara session start --json` emits `hadara.sessionStart.v1` with current-state metadata, lifecycle primary/diagnostic commands, known-problem routing, cache/degraded metadata, and a bounded context-pack envelope. The default path intentionally avoids live graph/context-pack discovery so mounted workspaces do not hang; `--live` is explicit for callers that accept full context-pack graph reads. Docker check passed 136 files / 894 tests, Docker sync-build passed and refreshed `dist`, built CLI session-start smoke returned `ok:true` in about 1.6s with degraded no-live metadata, and evidence lint passed. The first live-default design timed out after 45s and is recorded as resolved by the bounded no-live smoke. Evidence: `ev:T-0378:b3e1cc3b1b6d44b4a68c9bf0`, `ev:T-0378:2c321128b97c4efda50ee1ba`, `ev:T-0378:dd42b8f8ded34d988a2090a1`, `ev:T-0378:59772865b91049d6b79fa3ce`.

T-0379 follow-up note: C6 warm cache is now consumed by default Session Start when freshness can be proven cheaply. `hadara session start --task T-XXXX --include-code --json` first checks the cached source manifest fast-freshness path, reads fresh graph-core and code-index shards read-only, and reports cache metadata such as `mode:"graph-core+code-index"` and `sourceManifestFastPath:"hit"`. If freshness cannot be proven without broad scanning, it keeps the T-0378 bounded no-live fallback. Docker check passed 136 files / 897 tests, Docker sync-build passed and refreshed `dist`, built CLI warm/session-start smoke proved warm graph-core/code-index consumption, and evidence lint passed. Evidence: `ev:T-0379:752358a1a77147e6a2d52a04`, `ev:T-0379:e80bf2ffaa394eb899ef88b3`, `ev:T-0379:fb174f9ca4254d2b9aa4bec9`, `ev:T-0379:ef7d014317e24701bd2b61a8`.

T-0380 follow-up note: Context-routing performance regression checks now have an advisory fixture. `scripts/context-routing-performance-baseline.mjs` measures `session_start` and `session_start_include_code`, supports `--thresholds docs/CONTEXT_ROUTING_PERFORMANCE_THRESHOLDS.json`, supports `--workloads` for quick partial checks, and only turns threshold regressions into process failure when explicitly run with `--fail-on-regression`. Docker check passed 136 files / 898 tests, Docker sync-build passed and refreshed `dist`, and built Session Start workload smoke returned `ok:true` with `regression.ok:true`. Evidence: `ev:T-0380:ff1d277e8bbb467e9f9f20af`, `ev:T-0380:e9559e47ff9940999f1171cf`, `ev:T-0380:4bf9cfb9548c411b9a94cc20`.

T-0381 follow-up note: Context-routing implementation completion is now audited in `docs/specs/0.3.3/context-routing/09_Context_Routing_Implementation_Completion_Audit.md`. The audit maps C1-C6 specs to implemented, partial, deferred, and cleanup state as of the T-0381 snapshot; specs 06/07/08 no longer describe T-0374 through T-0380 work as future/remaining work; and the audit is registered in `.hadara/docs-registry.json`, `docs/DOC_REGISTRY.md`, and SOP required-reading. Docs registry parse, docs explain registration, stale phrase check, and `git diff --check` passed. Evidence: `ev:T-0381:45c7ad2200ce4ec1bbb2fb33`.

T-0382 follow-up note: Session Start JSON/UX hardening is implemented. `hadara.sessionStart.v1` now includes structured `guidance` with mode, primary next action, task-required flag, and command args. The default no-task bounded `hadara session start --json` path returns `ok:true` with degraded task-selection guidance instead of failing solely because no task id was supplied, while still avoiding hidden live graph/context-pack scans. Focused Docker tests passed 3 files / 20 tests, Docker sync-build passed 136 files / 898 tests and refreshed `dist`, and built CLI no-task/task-scoped smokes returned the expected guidance. Evidence: `ev:T-0382:93c876280718445e833270ba`.

T-0383 follow-up note: Context-routing E2E smoke coverage now has `scripts/context-routing-e2e-smoke.mjs` and `npm run smoke:context-routing`. The default `fast` profile runs bounded Session Start and raw slice workloads through the built CLI and verifies `.hadara/local/cache/context` does not change; `--profile full` and `--workloads` explicitly include cache status, cache warm dry-run, task graph, code-aware graph, and context pack workloads. Docker sync-build passed 137 files / 901 tests with refreshed `dist`, the built fast smoke passed 4 workloads with cache unchanged, and mounted extended probes showed cache/graph/pack exceed a 20s workload budget and should feed T-0384 diagnostics cleanup. Evidence: `ev:T-0383:d013f3d6e2be494bb6372a41`.

T-0384 follow-up note: Cache status/warm diagnostics now include an additive `diagnostics` object with state (`fresh`, `missing`, `stale`, `corrupt`, `partial`), operator summary, structured warm command args, mounted/full-manifest slow-path metadata, manifest change counts, and shard summary/planned keys. Unit coverage exercises missing, dry-run warm, fresh, partial-shard, stale, and corrupt diagnostics. Docker check and sync-build each passed 137 files / 902 tests with refreshed `dist`; built `context cache status --json` emitted stale diagnostics on the mounted workspace in about 19.6s. Evidence: `ev:T-0384:5cf534a97e2c4ff7a8355fd6`.

T-0385 follow-up note: 0.3.3 context-routing readiness docs now reflect implementation through T-0384. `09_Context_Routing_Implementation_Completion_Audit.md` marks T-0382 through T-0385 cleanup complete, routes T-0386/T-0387 as remaining hardening, and classifies mounted broad cache/graph/pack latency as an accepted residual for explicit diagnostic/warm/full-profile commands rather than default Session Start. The C6 fast-cache spec and worker plan no longer describe T-0382/T-0383/T-0384 cleanup as future work. Evidence: `ev:T-0385:502833bf598b4d31b22d27db`.

T-0386 follow-up note: Acceptance lifecycle validation now uses a shared parser in `src/task/acceptance.ts` for legacy and v2 `ACCEPTANCE.md` tables. Legacy rows remain strict by default (`original`, required, non-deferrable), v2 rows can express `Origin`, `Required`, `Deferrable`, `Follow-up Created`, and `Accepted Risk`, and done-level harness/protocol checks consume the shared analysis while preserving existing issue-code compatibility. Focused Docker temp-copy tests passed 3 files / 50 tests; full Docker sync-build passed 138 files / 906 tests and refreshed `dist`. Evidence: `ev:T-0386:4413cd420e354248bb671461`.

T-0387 follow-up note: Context slice/pack security boundaries are now shared. `src/context/context-slice-boundary.ts` centralizes raw slice path normalization plus the `.hadara` allowlist and generated/local denylist; `context slice` and `context pack` both consume it, and context pack filters slice candidates before publishing suggested commands. Focused Docker temp-copy tests passed 3 files / 31 tests; the first full Docker sync-build timed out in `tests/unit/protocol-consistency.test.ts`, and the retry passed 138 files / 907 tests with refreshed `dist` and `distLooksStale:false`. Evidence: `ev:T-0387:2691a5ec97e045d2814f10f7`, `ev:T-0387:561d66c217184e529964d5ee`.

T-0388 follow-up note: Context pack read recommendations now distinguish graph relevance from raw sliceability. Builder-produced `ContextPackItem` values include additive `sourceAccess.rawSlice` metadata with `sliceable`, `not-sliceable`, or `not-applicable`; `sliceCandidates` remains the executable raw-slice command list. Focused Docker temp-copy tests passed 4 files / 27 tests; the first full Docker sync-build timed out one unrelated `evidence-parallel-append` concurrency test, and the retry passed 138 files / 908 tests with refreshed `dist` and `distLooksStale:false`. Evidence: `ev:T-0388:7faddfa5522e43c9adbb2988`, `ev:T-0388:d63eccfe33c34ca3a3990647`.

T-0389 follow-up note: Context pack item `sourceHash` now prefers the current text hash of raw-sliceable item paths when the file is available, while retaining graph node source-hash fallback for missing or non-sliceable paths. This fixes dogfood output where required-reading document items could inherit the docs-registry graph source hash instead of the actual document file hash. Focused Docker temp-copy validation passed 1 file / 8 tests; full Docker sync-build passed 138 files / 909 tests, refreshed `dist`, and reported `distLooksStale:false`. Evidence: `ev:T-0389:61eafa48eb174f6ea4051e36`, `ev:T-0389:7e68c43ca20f44409d090d95`.

T-0390 follow-up note: Context pack explicit-range slice candidates now treat a single graph source line as an anchor and emit a bounded source window instead of a one-line range, while preserving real multi-line metadata ranges. This fixes the dogfooded `context pack` -> `context slice --task --candidate` scenario where an `AGENT_HANDOFF.md` candidate returned only the heading line. Focused Docker temp-copy validation passed 2 files / 19 tests; full Docker sync-build passed 138 files / 909 tests, refreshed `dist`, and reported `distLooksStale:false`. Evidence: `ev:T-0390:d6ab0cb842d3479faf06b351`, `ev:T-0390:3696103d7d274411b7cc706f`.

T-0391 follow-up note: The dogfooded `session start --json` -> `task next --json` flow now avoids self-referential next-work recommendations. `task next` ignores handoff meta-guidance that merely tells the operator to run/select with `task next`, falls back to concrete work sources, and prefers primary open Task Board rows before legacy `Partial` rows while keeping Partial visible in backlog. `tasks/T-0391-task-next-self-referential-handoff-guidance-hardening/FINDINGS.md` records the agent-usage findings and future improvement ideas. Focused Docker temp-copy validation passed 2 files / 12 tests; full Docker sync-build passed 138 files / 911 tests, refreshed `dist`, and reported `distLooksStale:false`; built dogfood smoke confirmed T-0391 primary and T-0006 Partial backlog. Evidence: `ev:T-0391:cc5750565e7149598bd68683`, `ev:T-0391:ce4b816c3ef64b6eb1154083`, `ev:T-0391:d22ebea228d34e9b966efe53`.

T-0392 follow-up note: The 0.3.3 lifecycle convenience line is specified in `docs/specs/0.3.3/lifecycle/00_Lifecycle_Workflow_Agent_Convenience_Spec.md`. The plan keeps `task finish`, `task ready`, `task close`, and `task audit-close` as canonical proof boundaries, then adds additive agent-friendly surfaces in follow-up capsules: read-only `task lifecycle`, read-only `task close-repair-plan`, lifecycle guidance dedup hardening, dry-run `task finalize`, optional guarded finalize execute, and generated-doc/session guidance alignment. Focused docs registry validation passed 2 files / 6 tests and `git diff --check` passed. Evidence: `ev:T-0392:46f350146736461ea9712b18`.

T-0393 follow-up note: The first lifecycle convenience implementation is now available as `hadara task lifecycle --task T-XXXX --json` with schema `hadara.task.lifecycle.v1`. It composes existing finish, ready, close, and audit-close read models into a read-only normalized phase report with checks, satisfied items, blockers, optional repair metadata, and one primary next action while preserving the canonical lifecycle commands as the only writers. Focused lifecycle validation passed 4 files / 20 tests; full Docker sync-build passed 139 files / 915 tests and refreshed `dist`; built CLI smoke returned `ok:true`, `readOnly:true`, `phase:"finish-required"`, and one primary next action for the active capsule. Evidence: `ev:T-0393:bc944ecc2c894e869dd7e557`, `ev:T-0393:5ec89716142c4e19b7e3abe0`, `ev:T-0393:03d977cfde444c83862cfd3c`.

T-0394 follow-up note: The lifecycle convenience line now has a dedicated close repair plan surface: `hadara task close-repair-plan --task T-XXXX --json` with schema `hadara.task.closeRepairPlan.v1`. The report composes existing close/audit read models, remains read-only, classifies `not-closed`, `closed-stale`, `closed-invalid`, `duplicate-close-proof`, `closed-valid`, and `unknown`, and returns exact next actions without appending evidence. Focused Docker validation passed 5 files / 28 tests; full Docker sync-build passed 140 files / 921 tests and refreshed `dist`; built CLI smoke returned `ok:true`, `readOnly:true`, and `classification:"not-closed"` for the active Draft capsule; `git diff --check` passed. Evidence: `ev:T-0394:f0875b6093844de1ac01053e`, `ev:T-0394:8c47406cc61a4314bde168b0`, `ev:T-0394:a32fcb73ccde4179a56cc267`, `ev:T-0394:e22abfd41b9048a492203406`.

T-0395 follow-up note: Successful `hadara task close --task T-XXXX --json` dry-runs now return a single primary `append-close-evidence` action when validation, evidence lint, and protocol doctor already pass, instead of repeating `run-done-validation` and `run-evidence-lint` as required next actions. Failed close dry-runs still retain validation/lint/blocker guidance. Focused Docker validation passed 4 files / 24 tests; full Docker sync-build passed 140 files / 921 tests and refreshed `dist`; built CLI smoke confirmed a one-action close dry-run; `git diff --check` passed. Evidence: `ev:T-0395:0bfa119bfc5e43a489d31794`, `ev:T-0395:6c210dc953974c32acf008b7`, `ev:T-0395:a2c33196f7704223ae5e0044`, `ev:T-0395:7626ac62b2db4570ac2a87c8`.

T-0396 follow-up note: `hadara task finalize --task T-XXXX --json` is now available as a read-only high-level lifecycle plan with schema `hadara.task.finalize.v1`. The report composes finish, ready, close, and audit-close read models; emits ordered step metadata, expected write paths, a stable `planHash`, and one primary next action; and refuses `--execute` in this capsule without writing. Focused Docker validation passed 7 files / 37 tests; full Docker sync-build passed 141 files / 925 tests and refreshed `dist`; built dry-run and execute-refusal smokes passed; `git diff --check` passed. Evidence: `ev:T-0396:874095dd00434f5195eb144a`, `ev:T-0396:1057e733697c467aa0fbc9cd`, `ev:T-0396:d7b3975a5e4849f9ab74da22`, `ev:T-0396:7d3e8d90a33149be8a8e2e94`, `ev:T-0396:c1bb5501c5d8471f81406164`.

T-0397 follow-up note: `hadara task finalize --task T-XXXX --execute --plan-hash <hash> --json` now rechecks the current dry-run plan hash before any writes, refuses missing or stale hashes, executes finish/ready/close/audit-close serially, stops on the first blocker, records execution step hashes, preserves underlying finish and close write boundaries, and returns `ok:true` only after final audit is `closed-valid`. Focused Docker validation passed 4 files / 21 tests; full Docker sync-build passed 141 files / 928 tests and refreshed `dist`; built dry-run, missing-hash, stale-hash, and `git diff --check` smokes passed. Evidence: `ev:T-0397:59085932aced47be89c4532d`, `ev:T-0397:fd38f35a791e4b179285cc9d`, `ev:T-0397:924236021b714ecaa783c7ec`, `ev:T-0397:454daf3e664843cba5db3b1a`, `ev:T-0397:3436c55fab2344789c6183b9`.

T-0398 follow-up note: README, `docs/IMPLEMENTATION_SOP.md`, `docs/COMMAND_SURFACE.md`, and `src/cli/init.ts` generated docs now teach the lifecycle convenience flow added by T-0393 through T-0397. The docs keep explicit `task finish`, `task ready`, `task close`, and `task audit-close` as canonical proof boundaries while presenting read-only `task lifecycle`, read-only `task close-repair-plan`, read-only `task finalize`, and optional guarded `task finalize --execute --plan-hash` as reviewed convenience surfaces. Focused Docker validation passed 4 files / 34 tests; full Docker sync-build passed 141 files / 928 tests and refreshed `dist`; built command registry smoke and final `git diff --check` passed; substantive passed validation evidence is recorded. Evidence: `ev:T-0398:7226b21db0564b008a4a8dc3`, `ev:T-0398:1d161be5bec2445292c76cb5`, `ev:T-0398:9f630cc9e133415495f689c2`, `ev:T-0398:e0be2ec555684f0a9e12b8df`, `ev:T-0398:d69e62e8e4254bebbfd5d89b`.

T-0399 follow-up note: `hadara task finalize --task T-XXXX --json` now avoids unnecessary lifecycle report work by evaluating only the next needed reports, with additive `summary.evaluatedReports` and `summary.skippedReports` diagnostics. Finalize also derives readiness from a single close dry-run when it reaches ready/close evaluation, and weak Done evidence blockers now point agents at `evidence add-command --result passed --category validation` with fix-hint/example metadata instead of encouraging another readiness rerun. Focused Docker validation passed 3 files / 13 tests; full Docker sync-build retry passed 141 files / 929 tests and refreshed `dist` after a recorded dashboard-static timeout; built finalize smoke and `git diff --check` passed. Evidence: `ev:T-0399:c213cedb4cfe4d20a8858fd9`, `ev:T-0399:8aa7e7dc564e429393a1ea67`, `ev:T-0399:ac178da8a71f482a9d8e702a`, `ev:T-0399:cda485bcea7242448c0da511`.

## Current Phase

Phase 5 Dashboard / Operator Console, Phase 5.5 Dashboard production-readiness, Phase 5.6 Dashboard UI/UX reset, Phase 5.7 dashboard projection redesign, dashboard refresh/read-model hardening, task workflow status-history hardening, TUI projection-first operator read-model alignment, TUI selected task detail/proof shared-read alignment, TUI projection-first task index/cache replacement, TUI CLI lazy startup, TUI Overview/Detail Markdown table cleanup, Evidence v2 persisted writer MVP, Evidence v2 release read-model compatibility, Evidence v2 migration preview, Evidence v2 migration execute mode, Evidence v2 writer stabilization and hardening, task finish state-doc advisory reporting, task close/audit boundary guidance, task-next handoff-priority recommendation policy, dry-run execute guards for capsule upgrade/remediation writes, reviewer feedback docs alignment, release/package readiness dry-run hardening, release artifact dirty-worktree guarding, multi-ecosystem release target modeling, release artifact evidence refresh, release provider contract, Python release preview provider, Python package smoke dry-run/local mode, Python package smoke boundary hardening, Python release advisory read model, release target configuration preview, release target configuration warning surfacing, Phase 6 multi-agent command context contract, task lifecycle next-action metadata, task complete flow dry-run orchestration, close evidence idempotency/supersedes metadata, handoff patch suggestions, dev Docker validation wrapper, Task Capsule templates, release dry-run service decomposition, Phase 6 reviewer-feedback hardening, actor context CLI option plumbing, dev Docker sync-dist before-hash guarding, close evidence append race recheck, task create collision guard, handoff suggestion fragment polish, task finish EOF normalization, release candidate freeze/artifact refresh, repository skeleton cleanup, npm-installed toy project interface recycle, run scaffold observation match hardening, fresh init/generic project UX hardening, lifecycle status clarity/performance hardening, release candidate 0.2.0-rc.1 publish readiness, Python bridge package preview, Python Trusted Publisher workflow, Python bridge version alignment, init scaffold lifecycle docs alignment, init scaffold lifecycle wording follow-up, init scaffold protocol guidance follow-up, release candidate 0.2.0-rc.2 publish readiness, Phase 7 command/docs surface refactor work through `hadara@0.3.0-rc.1` publish metadata hardening, 0.3 protocol migration adoption support, evidence-preservation blocker fix, and rc.1 final readiness preparation are complete through T-0301; the operator npm publishes for `hadara@0.2.0-rc.1`, `hadara@0.2.0-rc.2`, `hadara@0.2.0-rc.3`, and `hadara@0.3.0-rc.0`, plus Python bridge publishes for `hadara==0.2.0rc1` on TestPyPI/PyPI, are complete. Dashboard/TUI UI work is paused; Phase 6 Operator Workflow Compression & Multi-Agent Compatibility is complete through its planned capsule range plus Phase 6.1 actor plumbing, dist-sync guard, close append race recheck, task create collision guard, handoff fragment polish hardening, the T-0267 finish hygiene hotfix, T-0268 `hadara@0.2.0-rc.0` RC evidence refresh, T-0270 root bootstrap launcher cleanup, T-0271 published-package interface recycle findings, T-0272 high-priority run scaffold fix, T-0273 first-run UX fixes, T-0274 lifecycle/readiness hardening, T-0275 rc.1 publish readiness, the post-completion npm publish verification, T-0276 Python bridge package preview, T-0277 Python Trusted Publisher workflow, T-0278 Python bridge version alignment, T-0279 init scaffold lifecycle docs alignment, T-0280 init scaffold lifecycle wording follow-up, T-0281 init scaffold protocol guidance follow-up, T-0282 rc2 publish readiness plus post-publish verification, T-0289 rc3 readiness/publish, and Phase 7.1 through 7.6 surface-refactor slices.

Post-publish update: T-0301 completed the operator npm publish for `hadara@0.3.0-rc.1`; `npm view` verified version `0.3.0-rc.1`, and no GitHub Release draft was requested. T-0302 then ran installed-package recycle in `hadara-recycle` under `/tmp/hadara-recycle/0.3.0-rc.1/`: npm registry metadata includes the intended discovery fields and public `HADARA` repo links, npx/global install smokes passed, help/lifecycle/commands/docs surfaces worked, protocol migrate dry-run/execute preserved existing task evidence, and a temporary 10-capsule dogfood project passed ready/close/audit for all capsules after standard docs and substantive evidence were supplied. T-0303 fixed the main follow-up candidate by making fresh init create `.hadara/context/HADARA_CONTEXT.md`, docs registry/required-reading surfaces expose it, and project-scoped `protocol migrate --target 0.3.0` plan missing context creation while preserving existing context. T-0304 then made workflow guidance explicit: docs are not deferred until after implementation, read-only discovery/validation can be parallelized, and evidence/capsule/shared-doc/before-hash/finish/close/release writes are serialized. T-0305 fixed `task finish` Task Board row preservation so human/mixed-owned `Notes` and extra cells survive while command-owned cells are synchronized. T-0306 made ready/close/harness blockers more actionable with path, heading, fixHint, example, and structured remediationHint fields while preserving issue codes. T-0307 added compact/current-state-first Required Reading tier guidance to root docs and generated init docs. T-0308 then made `docs required-reading --json` emit additive semantic `tier` metadata while preserving the existing `documents` and `excluded` arrays. T-0309 hardened `protocol migrate --execute` to preflight/coalesce/prepare/commit/rollback planned writes and changed `docs mark --execute` registry writes to temp+rename. T-0310 bumped source/package metadata to `0.3.0-rc.2`, aligned README/release notes/release readiness/helper examples, passed Docker full validation, release artifact/package/clean-checkout smokes, strict gate, release dry-run, publish dry-run, and extra workflow UX smokes; the operator then ran the approval-gated helper, published to npm, and verified `npm view` returned `0.3.0-rc.2`. T-0311 added project-root containment validation to the shared atomic write helper. T-0312 then verified the published rc.2 package from npm: registry metadata and latest dist-tag matched rc.2, `npx` and temp-prefix installed bin reported rc.2, fresh init created context and docs registry artifacts, migration execute preserved legacy evidence, and task finish preserved human Task Board cells. T-0313 then added `.hadara/docs-registry.json` and `docs/DOC_REGISTRY.md` from existing docs-registry service output; docs list now reads a committed registry, required-reading returns tier metadata, and no broad self-migration execute was run. T-0314 hardened `docs patch --execute` by replacing direct target overwrite with the shared atomic text write helper, added failure-preservation/temp-cleanup coverage, aligned stale README rc.2 test expectations, passed Docker full validation, and refreshed `dist`. T-0315 then bumped source metadata to stable `0.3.0`, aligned README/release notes/release readiness/helper guidance, fixed release gate and publish dry-run metadata checks to accept stable `0.x.0`, passed final Docker full validation, release artifact, package smoke, Docker clean-checkout smoke, strict gate, release dry-run, and publish dry-run without publish mutation. T-0316 then published `hadara@0.3.0` to npm through the approval-gated helper, verified `npm view` returned `0.3.0`, and did not request a GitHub Release draft. T-0317 then verified the published stable package from consumer paths: registry metadata/latest dist-tag matched `0.3.0`, temp-prefix installed bin reported `packageVersion: "0.3.0"` and `distLooksStale:false`, fresh basic/standard/governed init created context and docs registry artifacts, migration execute preserved legacy evidence, task finish preserved human/mixed-owned Task Board cells, and a mini lifecycle fixture reached `closed-valid`. Exact `npx hadara@0.3.0 version --json` remained unstable in this workspace due stale global resolution and DNS `EAI_AGAIN`, and governed docs doctor returned non-blocking historical Required Reading warnings.

The Phase 1 init scaffold reset, Phase 2 protocol consistency/remediation layer, close/evidence hardening through T-0170, Phase 3 task operator console through T-0177, Phase 3.5 operator workflow hardening through T-0185, Phase 4 evidence semantics/hardening through T-0192, Phase 5 dashboard/operator console work through T-0196, Phase 5.5 dashboard production-readiness through T-0206, Phase 5.6 dashboard UI/UX reset through T-0214, Phase 5.7 dashboard projection contract/store/core-route/refresh/task/timeline/debt projections, authored frontend merge, and projection validation/visual state coverage through T-0223, plus dashboard refresh/read-model hardening through T-0226, task workflow status-history hardening through T-0227, TUI projection-first operator read-model alignment through T-0228, TUI selected task detail/proof shared-read alignment through T-0229, TUI projection-first task index/cache replacement through T-0230, TUI CLI lazy startup through T-0231, TUI Overview/Detail Markdown table cleanup through T-0232, Evidence v2 persisted writer MVP through T-0233, Evidence v2 release read-model compatibility through T-0234, Evidence v2 migration preview through T-0235, Evidence v2 migration execute mode through T-0236, task finish state-doc advisory reporting through T-0237, task close/audit boundary guidance through T-0238, task-next handoff-priority recommendation policy through T-0239, dry-run execute guards for task upgrade/remediation writes through T-0240, reviewer feedback docs alignment through T-0241, release/package readiness dry-run hardening through T-0242, release artifact dirty-worktree guarding through T-0243, multi-ecosystem release target modeling through T-0244, release artifact evidence refresh through T-0245, release provider contract through T-0246, Python release preview provider through T-0247, Python package smoke dry-run/local mode through T-0248, Python package smoke boundary hardening through T-0249, Python release advisory read model through T-0250, release target configuration preview through T-0251, release target configuration warning surfacing through T-0252, Phase 6 multi-agent command context contract through T-0253, task lifecycle next-action metadata through T-0254, task complete flow dry-run orchestration through T-0255, close evidence idempotency/supersedes metadata through T-0256, handoff patch suggestions through T-0257, dev Docker validation wrapper through T-0258, Task Capsule templates through T-0259, release dry-run service decomposition through T-0260, Phase 6 reviewer-feedback hardening through T-0261, actor context CLI option plumbing through T-0262, dev Docker sync-dist before-hash guarding through T-0263, close evidence append race recheck through T-0264, task create collision guard through T-0265, handoff suggestion fragment polish through T-0266, task finish EOF normalization through T-0267, release candidate freeze/artifact refresh through T-0268, repository skeleton cleanup through T-0270, npm-installed toy project interface recycle through T-0271, run scaffold observation match hardening through T-0272, fresh init/generic project UX hardening through T-0273, lifecycle status clarity/performance hardening through T-0274, release candidate 0.2.0-rc.1 publish readiness through T-0275, the operator npm publish verification for `hadara@0.2.0-rc.1`, Python bridge package preview through T-0276, Python Trusted Publisher workflow through T-0277, Python bridge version alignment through T-0278, init scaffold lifecycle docs alignment through T-0279, init scaffold lifecycle wording follow-up through T-0280, init scaffold protocol guidance follow-up through T-0281, release candidate 0.2.0-rc.2 publish readiness through T-0282, and the operator npm publish verification for `hadara@0.2.0-rc.2` are complete.

Phase 6 planning was staged from local agent-UX review notes. T-0253 established common actor/run/plan/idempotency, write-boundary, next-action, and issue-code vocabulary; T-0254 attached actor and structured next-action metadata to task lifecycle reports; T-0255 added read-only `task complete` workflow compression over finish/ready/close/audit reports; T-0256 added close evidence idempotency/supersedes metadata; T-0257 added read-only Agent Handoff section-fragment suggestions with before-hash and coordinator/shared-doc metadata; T-0258 added the official `hadara dev docker-check` Docker temp-copy validation wrapper with focused/full modes, explicit dist sync, and redacted evidence-ready JSON; T-0259 added Draft-only Task Capsule templates for common task families; T-0260 decomposed release dry-run internals into target configuration, provider advisory, evidence validation, readiness, and diagnostics services without changing `hadara.releaseDryRun.v1` or adding release mutation; T-0261 clarified Docker dist-sync source/output mutation metadata and documented Phase 6.1 follow-up hardening; T-0262 added optional actor/run CLI option plumbing for task lifecycle, handoff suggestion, and dev Docker reports; T-0263 added a matching before-hash guard for `dev docker-check --sync-dist`, with a first-time missing-hash escape hatch and conflict metadata; T-0264 added close evidence append race recheck for stale same-key execute reports; T-0265 added bounded task create collision retry and clear exhausted-retry failure; T-0266 made handoff suggestion fragments more coordinator-usable with exact target before-hash, section title, and suggested replacement Markdown while preserving read-only behavior. The phase goal was workflow compression without weakening dry-run-first governance, hash guards, evidence integrity, close/audit integrity, or future coordinator/worker multi-agent boundaries.

T-0267 followed Phase 6.1 as a small workflow hygiene hotfix: `task finish` generated text writes now normalize trailing whitespace/blank EOF to a single final newline through the shared planning/execute content path, preventing repeated `git diff --check` cleanup and close source-hash churn after `task finish --execute`.

T-0268 froze the next release-candidate metadata at `hadara@0.2.0-rc.0`, added conservative release notes, generalized release metadata readiness from the first RC to `0.x.y-rc.N`, hardened package-smoke handling for empty npm/installed CLI stdout capture, and refreshed package smoke, clean-checkout smoke, current-HEAD release artifact, release dry-run, and release publish dry-run evidence without publish/deploy mutation.

T-0269 was the approval-gated npm publish capsule for `hadara@0.2.0-rc.0`, but that publish candidate is superseded by T-0275 after the T-0272 through T-0274 recycle fixes. T-0269 did not run npm publish, registry mutation, GitHub Release creation, Docker build, PyPI upload, or release execute paths.

T-0270 removed unused root bootstrap launcher files `START.bat`, `start.sh`, `hadara`, and `hadara.cmd` from the current repository skeleton. Hermes/.hadara context files and examples remain in scope for future compatibility work, package metadata still uses `dist/cli/main.js` as the npm bin entrypoint, and pack dry-run passed with a `/tmp` npm cache.

T-0271 installed `hadara@0.2.0-rc.0` from npm inside `hadara-recycle`, created `/tmp/hadara-recycle-toy-0271`, initialized a governed toy project, and exercised representative installed CLI/MCP/dashboard/TUI/release/task/evidence/run interfaces. The package install and most interfaces worked; the main bug candidate is `run scaffold` generating a second-step matcher that expects raw stdout while `run` provides fake-shell observations as JSON. Fresh-init/status polish issues are also recorded in `tasks/T-0271-npm-installed-toy-project-interface-recycle/FINDINGS.md`.

T-0272 fixed the high-priority T-0271 deterministic run bug by making generated `run scaffold` scripts match the fake-shell JSON observation success signal instead of raw stdout. Focused Docker temp-copy tests passed for `tests/unit/run-cli.test.ts` and `tests/unit/agent-loop.test.ts`, and a built CLI smoke in `/tmp/hadara-t0272-smoke` proved multiline stdout scaffold output runs unchanged.

T-0273 fixed first-run UX findings from T-0271: `init --json` now emits `hadara.init.v1`, fresh governed `init doctor --json` no longer false-positives on old profile names, status/TUI parse table-first Project State phase rows, `handoff update --json` emits `hadara.handoff.update.v1`, `handoff suggest` no longer injects HADARA-dev Phase 6 wording into generic projects, and `doctor --json` reports the concrete `.hadara/context/HADARA_CONTEXT.md` context artifact path. Focused Docker tests passed 5 files / 42 tests and built fresh scaffold smoke passed.

T-0274 fixed the remaining T-0271 lifecycle/status hardening findings for `0.2.0-rc.1`: single-task lifecycle/evidence/read commands now use direct `findTaskCapsule()` lookup instead of reading unrelated task capsule documents, `hadara.task.workbench.v1` reports additive `state.readiness` metadata to distinguish current done-level readiness from valid close proof state, dashboard task-detail fast projections share that readiness contract, and `hadara.dev.docker_check.v1` failed-step issues include redacted `exitCode` and `debugHint` while still omitting raw subprocess logs. Focused Docker tests passed for lifecycle/evidence/docker surfaces, dashboard-static standalone passed, and Docker build refreshed workspace `dist`.

T-0275 prepared and published `hadara@0.2.0-rc.1`: package metadata and lockfile target rc.1, the accidental runtime self-dependency on the previous `hadara` RC was removed, README/release notes/release readiness/manual helper examples point at rc.1 and T-0275, and `tasks/T-0275-release-candidate-0-2-0-rc-1-publish-readiness/GITHUB_RELEASE_NOTE.md` remains ready for an optional draft GitHub Release. Focused Docker validation passed 8 files / 73 tests plus feature-smoke 1 file / 3 tests; Docker full check passed 100 files / 680 tests and refreshed `dist`; release artifact, package smoke, clean-checkout smoke, strict release gate, release dry-run, and release publish dry-run passed before mutation; the operator then ran the approval-gated manual helper, npm published successfully, and `npm view hadara@0.2.0-rc.1 version --registry=https://registry.npmjs.org` returned `0.2.0-rc.1`. No GitHub Release draft, Docker image, or PyPI publish was requested.

T-0276 added a local Python bridge package preview under `python/` for future PyPI distribution while keeping the Node/npm runtime primary. The bridge uses isolated Python metadata and `src/hadara` layout, exposes a `hadara` console script that checks Node.js 22+ and `npx`, delegates to pinned `npx -y hadara@0.2.0-rc.1`, and avoids global `hadara` discovery to prevent self-recursion. Focused Python tests passed 7 tests; local build produced sdist/wheel with `build --no-isolation` because host Python lacks `python3-venv`/`ensurepip`; `twine check dist/*` passed. No TestPyPI/PyPI upload, token loading, npm publish, GitHub Release, Docker image, or release mutation ran.

T-0277 prepared PyPI Trusted Publisher publishing for the Python bridge without performing a registry mutation. It added `.github/workflows/python-publish.yml` as a manual-only GitHub Actions workflow with `testpypi` and `pypi` targets, build-first artifact reuse, job-level `id-token: write`, and PyPA publish action usage without checked-in PyPI tokens. `docs/PYPI_TRUSTED_PUBLISHING.md` records exact publisher values for owner `ictseoyoungmin`, repository `HADARA`, workflow filename `python-publish.yml`, environments `testpypi`/`pypi`, and post-publish install smokes. Python tests passed 7 tests; local build produced `hadara-0.0.1` sdist/wheel; `twine check dist/*`, workflow trigger boundary checks, and `git diff --check` passed. No GitHub Actions publish execution, PyPI/TestPyPI upload, token loading, GitHub Release, Docker image, npm publish, or registry mutation ran.

T-0278 aligned the Python bridge package version with the published npm RC before PyPI/TestPyPI publication. `python/pyproject.toml`, `python/src/hadara/version.py`, the Python Publish workflow `expected_version` default, tests, and `docs/PYPI_TRUSTED_PUBLISHING.md` now use the PEP 440 canonical version `0.2.0rc1`, while the delegated npm runtime remains pinned to `hadara@0.2.0-rc.1`. Python tests passed 7 tests; metadata assertion confirmed `pyproject.toml` and `hadara.__version__`; local build produced `hadara-0.2.0rc1` sdist/wheel; `twine check`, wheel install smoke, old-version residue check, and `git diff --check` passed. No PyPI/TestPyPI upload, token loading, GitHub Actions publish execution, npm publish, or registry mutation ran.

T-0279 aligned generated `hadara init` Markdown docs with the current task lifecycle. Fresh scaffolds now include `docs/TASK_WORKFLOW_COMMANDS.md`, generated `AGENTS.md` and `docs/IMPLEMENTATION_SOP.md` register it as required reading, generated SOP includes the explicit evidence/finish/ready/close/audit-close loop, and init doctor treats the workflow doc as a core scaffold file. Focused Docker validation passed 2 files / 24 tests, Docker full check passed 100 files / 681 tests and refreshed `dist`, and a built CLI smoke confirmed fresh basic init plus init doctor succeeds with the workflow doc present.

After the Python Trusted Publisher setup and version alignment, the operator published the Python bridge `hadara==0.2.0rc1` to both TestPyPI and PyPI through the manual GitHub Actions workflow. The Python package remains a preview bridge to the npm runtime, not a Python-native HADARA runtime.

T-0280 followed up on the init scaffold review by removing stale wording from generated `hadara init` docs and current project docs. Generated and root task workflow documentation now reflects the observed lifecycle order `task finish` before `task ready`, then `task close` and `task audit-close`; README and PyPI trusted publishing docs record that `hadara==0.2.0rc1` is published to both TestPyPI and PyPI. Focused Docker validation passed 2 files / 24 tests, three-profile generated scaffold smoke passed, Docker full check passed 100 files / 681 tests and refreshed `dist`, workspace built-CLI init smoke passed, and `git diff --check` passed.

T-0281 followed up on generated init protocol guidance after review. Generated scaffolds and root mirror docs now include common Python/venv/cache/SQLite ignore defaults, explicit evidence integrity rules against hand-editing `evidence.jsonl` or hiding failed/blocked checks, Project-Specific Documents guidance for registering human/agent docs through Required Reading, direct `harness validate --level done` diagnostic guidance, and close-source stability guidance that tells operators to finalize capsule/state docs before close, rerun ready/close/audit after unavoidable close-source edits, and avoid volatile close evidence ids in close-source docs. The standard lifecycle remains `task finish -> task ready -> task close -> task audit-close`. Focused Docker validation passed 2 files / 24 tests, three-profile generated init/doctor smokes passed, direct harness validate smoke returned `ok:true`, Docker full check passed 100 files / 681 tests and refreshed `dist`, workspace built-CLI init smoke passed, and `git diff --check` passed.

T-0282 prepared and published npm `hadara@0.2.0-rc.2` after the init scaffold guidance work. Package metadata and lockfile target rc2, README/release notes/release readiness/manual helper examples point to rc2 and T-0282, and `tasks/T-0282-release-candidate-0-2-0-rc-2-publish-readiness/GITHUB_RELEASE_NOTE.md` is ready for an optional draft GitHub Release. Docker `npm run dev:docker-sync-build` passed 100 files / 681 tests and refreshed `dist`; built CLI version smoke reported `0.2.0-rc.2` with `distLooksStale:false`; strict release gate passed; rc2 package smoke and clean-checkout smoke evidence passed; npm pack dry-run reported `hadara-0.2.0-rc.2.tgz`; the manual helper regenerated release artifact/package/clean-checkout evidence from a clean committed worktree, published to npm, and verified `npm view` returned `0.2.0-rc.2`. No GitHub Release draft, Docker publish, PyPI/TestPyPI publish, installer execution, or MCP release/package execution was requested.

T-0283 documented the dogfooding-backed rc3 proof reliability plan under `docs/specs/rc3-proof-reliability/`. The plan explicitly uses the `0.2.0-rc.2` comparative Bookmark API dogfooding result and reviewer judgment to prioritize evidence writer idempotency/concurrency hardening before proof status/explain/freshness MVP, CI gate MVP, and rc3 readiness/recycle work. T-0284 completed the P0 writer hardening: evidence append returns the exact writer result, explicit `--idempotency-key` same-key replays return existing records without appending duplicate Markdown/JSONL rows, keyless writes remain append-only, and task evidence writes are guarded by `.hadara/local/locks/evidence/<task>.lock/`. T-0285 completed the P1 proof MVP: `hadara proof status --task <id> --json` and `hadara proof explain --task <id> --json` produce read-only task-readiness proof verdicts, evidence blockers/warnings, supporting evidence, and close-proof freshness. T-0286 completed the P2 CI gate MVP: `hadara ci gate --mode advisory|strict [--task <id>] --json` aggregates protocol, evidence, proof, and deferred release checks with advisory vs strict blocker behavior. T-0287 prepared `hadara@0.2.0-rc.3` as a source publish candidate, refreshed package/clean-checkout/release-artifact evidence, proved release dry-run readiness, confirmed release publish dry-run performs no mutation, and ran fresh init/recycle smokes for basic, standard, governed, idempotent evidence replay, proof status, and CI gate advisory. T-0288 closed the rc3 review's blocking gaps and hardening items: a strict CI gate empty-scope guard (`CI_GATE_NO_DONE_TASKS`/`CI_GATE_TASK_NOT_FOUND`/`--allow-empty`), a real multi-process evidence append regression test, append-lock stale metadata/diagnostics, proof freshness `checkedSources` reflecting the full close-relevant source set, and idempotent non-JSON evidence UX; a post-hardening rc3 release readiness refresh remains before publish. `session start`, project graph/projection generalization, mutation plan engine, protocol migration, multi-agent coordination, and release target provider expansion remain deferred until supported by later findings.

## Current Status

- Latest completed task is T-0457 Agent UX Validation Wrapper Error Semantics.
- The requested agent UX refactor dogfood loop has reached five completed capsules: T-0453 through T-0457.
- Next candidate work is lifecycle/status/finalize progress or fast-path semantics for long silent mounted-workspace runs.
- T-0457 made `validation run` launch failures agent-readable through structured `execution.failureKind`, `commandStarted`, `execution.error`, and fallback `nextActions`. Evidence: `ev:T-0457:63bc490c0f524cc0b5b748e3`, `ev:T-0457:28fb374a36e641bab90bd53d`, `ev:T-0457:ded129c4252440a593372c75`.
- T-0456 made `evidence add-command --help` non-mutating before `--task` parsing. Evidence: `ev:T-0456:3575c0472d5b464585261a79`, `ev:T-0456:dbf8b1a1dc8d4707b5d9469c`, `ev:T-0456:2c697ca98ea7410a9a9f23d9`.
- T-0455 added `sources.evidenceList.validationAttempts` to `task status` so agents can see per-check validation state without raw evidence inspection. Evidence: `ev:T-0455:ec70182bf0f9491292013cf1`, `ev:T-0455:d17cc01c4fd4444da2e9ace8`, `ev:T-0455:d4b3ea9ddcc549fda9eaeeb5`.
- T-0454 added stable validation attempt check keys and automatic same-check failed/blocked attempt resolution for `hadara validation run`. Evidence: `ev:T-0454:385fa69b38dc4641839a69bb`, `ev:T-0454:10fe55713fac48b2907d76b6`, `ev:T-0454:0287e6d080ec411880afc44b`, `ev:T-0454:fe8b5a505bd94cbaa6805dc4`; failed/blocked probes are resolved by later passed evidence.
- T-0453 started the direct HADARA dogfood UX refactor line by decoupling `validation run` evidence capture from default `TASK.md` Validation row writes. Evidence: `ev:T-0453:facedecf71cf4747adfdd522`, `ev:T-0453:bff69d665fcb466fb9bc910b`, `ev:T-0453:6a76b8b335fb4151b6d9f92a`.
- T-0431 implemented read-only `hadara docs read-map --task T-XXXX --json` and `hadara docs inbox --json`, added `hadara.docs.readMap.v1` and `hadara.docs.inbox.v1`, command registry/schema fixtures, focused tests, refreshed workspace `dist`, and built CLI smokes passed in `ev:T-0431:a81383c6d7894693a45a95ed`.
- T-0430 implemented registry-first `hadara docs register --path <path> --json` over `.hadara/docs-registry.json`, added `hadara.docs.register.v1`, command registry/schema fixtures, focused tests, refreshed workspace `dist`, and built CLI smoke passed in `ev:T-0430:1933b10f80184f8abb9540cb`.
- T-0429 implemented non-overlapping generated templates: `AGENTS.md` owns Required Reading/safety only, `.hadara/context/HADARA_CONTEXT.md` is routing-only, and `docs/HADARA_WORKFLOW.md` owns lifecycle/context/evidence/document timing guidance. Docker build plus focused init tests passed, workspace `dist` was refreshed, and built governed init/doctor template smoke returned `ok:true` in `ev:T-0429:ab675a5933c84286b8d255fc`.
- T-0428 implemented the accepted 0.4 init scaffold file sets, `.hadara/scaffold.json`, `.hadara/slot-registry.json`, registry seed/context routing, and 0.4 doctor checks; Docker build plus focused init tests passed, workspace `dist` was refreshed, and built governed init/doctor smoke returned `ok:true` in `ev:T-0428:f09b011734c84cab8034facf`.
- T-0427 registered canonical 0.4 manifest documents in `.hadara/docs-registry.json` and `docs/DOC_REGISTRY.md`, added SOP conditional reading rows for the 0.4 README and worker plan, confirmed the removed nested package path is not registered, recreated the `hadara-dev` Docker container, refreshed workspace `dist`, and validated with `ev:T-0427:8f087c4cf64747628829a5dc`.
- T-0426 accepted the final 0.4 document set, including the 24-capsule post-acceptance implementation budget with self-review hardening, polish/cleanup, final review/docs cleanup, and release-line work intentionally outside that budget. Focused docs validation passed in `ev:T-0426:496e55c598814f8d8a09cff6`.
- Stable `0.3.4` readiness remains pending as a separate release-line task; it should use T-0417 readiness, T-0418 publish, T-0422 installed-package proof, and T-0423 clean package-recycle helper proof as inputs when release work resumes.
- Stable `hadara@0.3.0` publish and installed-package consumer recycle are complete through T-0316/T-0317.
- Phase 8 / `0.3.1` planning is staged under `docs/specs/0.3.1/`; Phase 8.1 status token/document ownership governance, Phase 8.2 task handoff close-state governance, Phase 8.3 installed-package findings cleanup, Phase 8.4 state consistency projection read model, Phase 8.5 advisory verify/doctor/CI integration, Phase 8.6 rc1 review/hardening cleanup, T-0325 CloseState derived-state cleanup, T-0326 rc1 release-readiness preparation, T-0327 approval-gated publish, T-0328 installed-package recycle, and T-0329 post-rc1 docs cleanup are complete.
- `0.3.1-rc.1` source/readiness is complete through T-0326; npm package visibility, tarball inspection, and corrected dist-tags are confirmed through T-0327; installed-package recycle is complete through T-0328; post-rc1 shared-doc wording cleanup is complete through T-0329.
- Skeleton repository exists.
- Seed CLI exists.
- MockProvider contract exists.
- ScriptedProvider exists for deterministic harness/replay provider behavior.
- Provider fallback executor exists for deterministic chat fallback orchestration.
- Provider adapter preparation contracts exist as `hadara.provider.config.v1` and `hadara.provider.call.v1`, with schema fixtures and helper APIs that reject stored secret values, deny unknown config input fields, schema-assert outputs, and summarize provider calls without prompt/response content.
- Fake shell preflight harness exists for deterministic tool observations without real shell execution.
- Minimal deterministic agent loop harness exists for ScriptedProvider responses plus fake shell observations.
- Task Capsule creation exists.
- Evidence append writes Markdown summaries, `evidence.jsonl` indexes, and managed public artifact copies.
- Handoff update exists.
- AGENTS.md and IMPLEMENTATION_SOP now capture reusable HADARA session protocol rules, required-reading registration, init profile matrix, standard task workflow loop, and scaffold document structure standards.
- Hermes/Agent Harness context export exists as a seed command.
- Doctor CLI JSON output exists as `hadara doctor --json`.
- Task list/show CLI JSON output exists as `hadara task list --json` and `hadara task show <task-id> --json`.
- Policy check CLI JSON output exists as `hadara policy check-shell <command> --json`.
- Policy execution preflight exists as `hadara policy preflight-shell <command> --json`.
- Hermes detect/export CLI JSON output exists as `hadara hermes detect --json` and `hadara hermes export-context --json`.
- Evidence collect CLI JSON output exists as `hadara evidence collect --json`.
- Evidence collect visibility can be selected with `--private` or `--visibility public|private`.
- Harness Task Capsule validation exists as `hadara harness validate --task <id> --json`.
- Harness Task Capsule validation enforces core Markdown format markers for Task Capsule continuity.
- Harness Task Capsule validation requires `evidence.jsonl` so completed work cannot miss the evidence index.
- Harness replay skeleton exists as `hadara harness replay <scenario.jsonl> --json`.
- Config/path resolver has realpath containment, environment priority, Windows path normalization, and project data boundary tests.
- Policy evaluator has a minimal tokenizer, safe command allowlist, destructive command denial tests, and shell execution preflight.
- Tool runtime work has started with fake shell observations gated by policy preflight.
- Agent loop work has started with bounded deterministic `hadara run --script ... --fake-shell-fixtures ... --json`.
- Workspace file boundary hardening is complete with a shared realpath resolver applied to evidence artifact copy, harness replay, and deterministic run file inputs.
- Evidence artifact redaction registry exists: public artifacts must be UTF-8 text and pass severity-threshold high-risk secret-pattern scanning before committed copy, with `hadara.redaction.report.v1` findings available from the core redaction service and safe public artifact policy diagnostics available as pattern ids, severities, counts, and byte counts.
- Evidence list read model exists as `hadara.evidence.list.v1`, with a shared report builder used by `hadara evidence list --json` and read-only MCP `hadara.evidence.list`; malformed JSONL lines degrade to warnings, parsed records are normalized before output, private evidence paths are stripped, and taskId mismatches are dropped with warnings.
- Phase 4 evidence semantics and post-Phase-4 hardening are complete through T-0192: existing `hadara.evidence.v1` records stay valid while normalized semantic read models classify evidence strength, summarize proof meaning, feed evidence lint, task protocol doctor, done-level harness validation, Dashboard/TUI contracts, and strict release readiness evidence gates. Evidence v2 writer MVP is complete through T-0233, release/smoke evidence compatibility is complete through T-0234, dry-run migration preview is complete through T-0235, and hash-guarded per-task migration execute is complete through T-0236; `EVIDENCE.md` rewrite, init scaffold changes, mass migration rollout policy, and MCP write expansion remain follow-up work.
- Evidence proof semantics foundation exists: `src/evidence/normalizer.ts` converts existing v1 records into `hadara.evidence.normalized.v1` read-model records, and `src/evidence/semantics.ts` classifies evidence strength, summarizes proof meaning, analyzes Done evidence sufficiency, accepts failed-evidence resolution from exact markers, legacy v1 same-category passed evidence, or explicit residual-risk documentation, warns on private-only substantive evidence, and exposes release proof predicates while preserving mixed v1/v2 compatibility.
- Evidence lint semantic integration exists: `hadara evidence lint --task <id> --json` keeps `hadara.evidence.lint.v1`, adds `summary.semantics`, maps actionable semantic Done-task errors/warnings from the shared analyzer, keeps legacy v1 compatibility information in summary counts instead of noisy lint issues, and preserves existing JSONL/Markdown drift checks.
- Protocol and harness semantic gates exist: task-scoped protocol doctor surfaces semantic evidence issues through the evidence area, and done-level harness validation blocks weak/unresolved failed/unexplained blocked Done evidence while keeping private-only substantive evidence as a warning for first rollout.
- Dashboard/TUI evidence semantic contract exists: selected-task Dashboard and TUI consumers must use shared semantic read surfaces, derive proof statuses from semantic summary/issues, avoid raw `evidence.jsonl`/`EVIDENCE.md` parsing for proof strength, and keep UI/API work read-only and additive.
- Evidence v2 writer/migration planning exists: `docs/EVIDENCE_V2_WRITER_MIGRATION_PLAN.md` defines the planned persisted v2 shape, dry-run-first per-task migration, hash-guarded execute posture, mixed-version tolerance, and explicit non-goals for automatic rewrites, init changes, MCP writes, UI work, and release enforcement.
- Release evidence strict gates exist: package-smoke, clean-checkout smoke, and release-artifact readiness checks now use strict release proof helpers over normalized legacy evidence plus artifact validation, requiring public passed evidence linked to schema-valid/source-ok artifacts with expected category/mode instead of accepting summary wording alone.
- Release dry-run readiness diagnostics exist: `hadara release dry-run --json` now includes a compact `readiness` block with blocker/warning counts and next actions, plus `diagnostics.stageTimings` and `slowStageWarnings` so operators can distinguish stale evidence from slow strict-gate/evidence scans. The command remains read-only and does not publish, create GitHub Releases, build Docker images, or execute installers.
- Release artifact dirty-worktree guarding exists: `hadara release artifact --execute` now verifies git worktree cleanliness before staging package files or running `npm pack`. This prevents release artifact evidence from recording a fresh-looking `gitCommit` while artifact contents include uncommitted workspace changes. T-0245 refreshed release artifact evidence from clean commit `2eff19c8ab63b635804352d2c71803226d592749`.
- Phase 6 common command context contract exists: `src/core/actor-context.ts`, `src/core/plan-context.ts`, and `src/core/next-action.ts` define actor roles, default actor resolution, multi-agent issue codes, write boundaries, stale-plan risk, next-action metadata, and unreviewed plan metadata. Fixture schemas `hadara.actor_context.v1`, `hadara.plan_context.v1`, and `hadara.next_action.v1` are registered for future workflow-compression reports without changing existing command behavior.
- Phase 6 task lifecycle next-action metadata exists: `hadara.task.finish.v1`, `hadara.task.ready.v1`, `hadara.task.close.v1`, and `hadara.task.audit_close.v1` now include additive actor context, structured `nextActions`, and optional `primaryNextAction` fields with write boundary, recommended actor role, before-hash, and stale-plan metadata. T-0262 added optional `--agent-id`, `--run-id`, `--actor-role`, and `--parent-run-id` CLI plumbing while preserving default unknown/local/operator/null actor values when absent.
- Phase 6 task complete flow dry-run exists: `hadara task complete --task <id> --json` emits `hadara.task.complete_flow.v1`, composes finish/ready/close/audit read models, reports actor, current stage, lifecycle steps, shared-doc state, conflicts, issues, and one primary next action while incomplete. It is read-only; `--execute` returns a blocked complete-flow report and performs no writes.
- Phase 6 close evidence idempotency/supersedes exists: `hadara.task.close.v1` reports `closeEvidenceWrite` idempotency metadata; same task/source/report close execute no-ops instead of appending uncontrolled duplicate evidence; changed source/report close proofs append with `close-proof`, idempotency, actor, and supersedes metadata; `hadara.task.audit_close.v1` reports latest non-superseded close evidence id, superseded ids, duplicate counts, and a compact idempotency verdict.
- Phase 6.1 close evidence append race recheck exists: `task close --execute` recomputes close evidence write metadata from the latest task `evidence.jsonl` immediately before append, exposes optional `closeEvidenceWrite.executeRecheck`, and no-ops stale same-key execute reports instead of appending duplicate close evidence. This is a local recheck, not a global lock service.
- Phase 6 handoff patch suggestion exists: `hadara handoff suggest --task <id> --json` emits `hadara.handoff.suggestion.v1`, reads `docs/AGENT_HANDOFF.md` and the selected task snapshot, reports target before-hash, shared-doc/coordinator metadata, section fragments, and issues, and performs no writes. `--execute` returns `HANDOFF_SUGGEST_EXECUTE_UNSUPPORTED`.
- Phase 6.1 handoff suggestion fragment polish exists: each `handoff suggest` section fragment now repeats the exact `targetBeforeHash`, names the `sectionTitle`, and includes `suggestedReplacementMarkdown` while preserving compatibility `suggestedMarkdown`; `patchPreview.content` names the target handoff file and section for manual coordinator review.
- Phase 6 dev Docker validation wrapper exists: `hadara dev docker-check [--focused <test...>] [--full] [--sync-dist --before-hash <hash>] --json` emits `hadara.dev.docker_check.v1`, runs Docker temp-copy validation with `npm ci`, focused/full checks, explicit dist sync, redacted source/workspace metadata, privacy booleans, step statuses, dist hashes, and an evidence-ready summary.
- Phase 6 Task Capsule templates exist: `hadara task create --from <template-id> --title <title> --json` emits `hadara.task.create.v1` and applies Draft-only template defaults for `release-read-model`, `evidence-v2`, `lifecycle-hardening`, `operator-workflow`, `protocol-remediation`, and `ui-polish`; templates do not mark Done, attach evidence, run validation, or close tasks.
- Phase 6.1 task create collision guard exists: task creation retries bounded sequential id collisions when the selected task directory appears before mkdir, skips ids already present in Task Board, returns `TASK_CREATE_COLLISION_RETRIES_EXHAUSTED` when retries are exhausted, and preserves Draft-only template behavior. This is a collision guard, not a durable global task allocator.
- Phase 6 release dry-run service decomposition exists: `release-dry-run.ts` now assembles the unchanged `hadara.releaseDryRun.v1` report from dedicated target-configuration, provider-advisory, evidence-validation/freshness, readiness-summary, and diagnostics services. npm remains effective primary, Python advisories remain non-blocking, `releaseTargetConfiguration.autoPromotion` remains false, and no publish/token/GitHub Release/Docker/PyPI/registry mutation behavior was added.
- Phase 6 reviewer-feedback hardening exists: `hadara.dev.docker_check.v1` now distinguishes project source mutation from workspace output mutation during `--sync-dist`, reports `projectSourceMutation:false`, `outputMutation`, and dist-sync before-hash availability/output-change metadata, while preserving compatibility `projectMutation:false`. Phase 6.1 deferred hardening is specified in `docs/specs/agent-ux/HADARA_Phase6_1_Reviewer_Feedback_Hardening_Spec.md`.
- Phase 6.1 actor CLI option plumbing exists: task finish/ready/close/audit-close/complete, handoff suggest, and dev docker-check accept optional actor metadata flags and reflect them in existing `actor` report fields without adding scheduling, task assignment, hidden shared-doc writes, or automatic routing.
- Phase 6.1 dev Docker sync-dist before-hash guarding exists: `dev docker-check --sync-dist` requires a matching `--before-hash` for existing workspace `dist/cli/main.js` before copying Docker-built output, blocks missing/stale reviewed hashes with `HADARA_DIST_SYNC_BEFORE_HASH_REQUIRED` or `HADARA_DIST_SYNC_BEFORE_HASH_MISMATCH`, reports `reviewedBeforeHash`, `beforeHashMatched`, `allowMissingBeforeHash`, and `conflictDetected`, and only allows missing pre-sync hash through explicit `--allow-missing-before-hash`.
- Evidence semantics hardening exists: normalized v1 records now expose source-line, fingerprint, id-source, and id-stability metadata; evidence lint preserves actual JSONL line numbers when generating semantic ids; later same-category failed-evidence resolution is documented as a v1 compatibility-only fallback; release dry-run reuses strict release proof candidate selection before version/hash freshness checks; Dashboard/TUI contracts treat private-only proof as an auditability warning rather than a Done blocker.
- Dashboard live read binding exists: the served dashboard now tries `/api/status` first, falls back to the static fixture, then falls back to inline JSON; it displays source provenance and loaded timestamps, and `Refresh Status` performs read-only refetch only.
- Dashboard operator-console layout exists: the served dashboard now renders top source/health controls, an Agent Lane, Workstream panel, deferred Evidence Lens placeholder, and Bottom Inspector while preserving read-only labels and live/fallback binding.
- Dashboard selected-task evidence lens exists: task selection reads shared task workbench, evidence lint, and evidence list reports through read-only dashboard APIs; proof status derives from semantic issue codes and summary counts, private-only is an auditability warning, and generated legacy ids are labeled as non-durable compatibility identity.
- Dashboard timeline read model exists: `hadara.dashboard.timeline.v1` generates deterministic read-only Workstream events from existing read models and `/api/timeline` serves them without polling, streaming, persistence, or private raw paths.
- Dashboard bootstrap read model exists: `hadara.dashboard.bootstrap.v1` and `/api/dashboard/bootstrap` provide a first-paint aggregate over operations status, task summary, active-run/debt summaries, timeline overview, optional compact selected-task proof, source metadata, and disabled cache metadata without deep evidence payloads, mutation, polling, streaming, browser storage, or TTL cache behavior.
- Dashboard progressive bootstrap frontend exists: the served dashboard reads `/api/dashboard/bootstrap` first, falls back to `/api/status` then fixture/inline data, shows cache status, keeps the previous successful in-memory view on refresh failure, and avoids browser project-state storage.
- Dashboard task-detail aggregate exists: `hadara.dashboard.task_detail.v1` and `/api/dashboard/task-detail?taskId=` compose selected-task fast workbench data, evidence lint, sanitized evidence list, and task-scoped timeline reports with backend proof derivation and read-only command guidance; the frontend no longer fans out selected-task detail across separate workbench/evidence/timeline reads, and detail clicks no longer rerun global docs/profile workbench or global timeline/status reads.
- Dashboard timeline identity hardening exists: evidence timeline events prefer normalized evidence ids and expose fingerprint, source line, id source, and id stability metadata while keeping legacy generated ids explicitly unstable on reorder and `artifact-N` ids fallback-only.
- Dashboard process-memory TTL cache exists: served dashboard bootstrap, task-detail, and timeline aggregate reads use route-level memory cache metadata with `hit`, `miss`, `stale`, `bypass`, or `disabled` status; `?cache=bypass` recomputes without overwriting the cached entry; `/api/dashboard/cache/status` exposes metadata only; no `.hadara/local`, database, file watcher, browser project-state storage, evidence source, mutation, or private raw path behavior was added.
- Dashboard degraded UX and performance-budget guidance exists: the served dashboard exposes source/cache/load phase metadata, marks refresh failures as degraded while retaining the previous in-memory view when available, exposes a read-only debug snapshot, and documents advisory responsiveness targets in `docs/DASHBOARD_PERFORMANCE_BUDGET.md` without adding browser project-state persistence or timing-sensitive unit thresholds.
- Optional dashboard polling exists: the served dashboard has an off-by-default `Auto Refresh` toggle that uses in-memory `setTimeout` scheduling through the existing read-only refresh path, backs off on degraded reads, pauses while hidden, exposes debug metadata, and does not add browser project-state persistence, SSE/WebSocket, shell/provider/MCP writes, task/evidence/release mutation, or streaming claims.
- Dashboard production readiness review exists: `docs/DASHBOARD_PRODUCTION_READINESS_REVIEW.md` records route inventory, schema inventory, read-only/private-path/browser-storage/cache/polling/performance boundary audit, residual risks, and a final readiness conclusion for Phase 5.5.
- Dashboard performance measurement exists: `docs/DASHBOARD_PERFORMANCE_MEASUREMENT.md` records Playwright Docker `/tmp` copy observations for shell HTML fetch, bootstrap, task-detail, and timeline route timings across bypass/miss/hit cache states; direct bind-mounted workspace measurement was not used because the dashboard server did not return promptly enough for stable timings.
- Dashboard production hardening follow-up exists: aggregate cache keys are isolated by redacted project fingerprints, bootstrap/task-detail/timeline reports include redacted project source metadata while keeping v1 `source.projectRoot` compatibility, sidebar navigation switches in-page views beyond Home, and long status/source chips are constrained to reduce first-viewport crowding.
- Dashboard Phase 5.6 UI/UX reset exists: the served dashboard uses the Phase 5.6 design language, a Preact single-asset bundle, per-view operator console content, clearer active/next command affordances, narrative activity feed, verdict-first evidence lens, contextual metrics, developer JSON disclosure, visual regression fixtures, and Playwright/axe-core visual+a11y gate while preserving read-only/no-browser-storage governance boundaries.
- Dashboard Phase 5.6 UX diagnosis documents the slow-mount loading problem: `/mnt/f` uncached live bootstrap fell from about 26s to about 17s after timeline dedup, but still remains structurally slow because broad capsule filesystem scans happen on the live bootstrap request path.
- Dashboard Phase 5.7 projection redesign is staged: `docs/specs/dashboard/HADARA_Dashboard_Read_Model_Performance_Redesign.md` defines the follow-up architecture for `/api/dashboard/core`, local `.hadara/local/cache/dashboard` projections, background refresh/serve warmup, incremental task summaries, cached timeline/debt projections, frontend core/heavy merge, and validation of stale/refreshing/degraded visual states.
- Dashboard local projection store exists as an internal service in `src/services/dashboard-projection-store.ts`: it writes `hadara.dashboard.projection_record.v1` records only under ignored `.hadara/local/cache/dashboard`, uses redacted project references, rejects raw project-root paths, replaces files through temp-file/rename, and remains excluded from context export. Route/frontend usage is deferred to T-0218 and later Phase 5.7 slices.
- Dashboard core route exists as `/api/dashboard/core` over `hadara.dashboard.core.v1`: it builds a cheap live core report from bounded docs and available local projection summaries, writes/reads `.hadara/local/cache/dashboard/core/index.json`, supports `?cache=bypass`, and avoids broad Task Capsule directory scans on the request path. Background refresh, source-signal invalidation, frontend migration, incremental task projections, and timeline/debt projections remain deferred to T-0219 through T-0223.
- Dashboard background refresh/status exists for Phase 5.7: `hadara dashboard serve` triggers non-blocking core projection warmup, `/api/dashboard/refresh` starts or observes a coalesced background refresh, and `/api/dashboard/projection/status` exposes metadata-only projection presence/freshness/refresh state without cached report bodies. Incremental task source signals and timeline/debt projection materialization remain deferred.
- Dashboard serve-start projection warmup is deliberately core-only and delayed so first `/api/dashboard/core` requests are not queued behind immediate task/timeline/debt scans on the same Node event loop; explicit `/api/dashboard/refresh` remains the full projection rebuild path but now yields between task, heavy, and core stages. Individual broad scan stages can still be costly on `/mnt/f`, so deeper chunking or worker offload remains a documented future core refactor if needed.
- Dashboard incremental task projection exists as `hadara.dashboard.task_projection_index.v1` under `.hadara/local/cache/dashboard/source-signals/tasks.json`: background refresh tracks per-task `TASK.md` and `evidence.jsonl` mtime/size signals, records changed/reused task ids, reuses unchanged summaries without rereading task bodies, and lets `/api/dashboard/core` prefer task projection summaries when present. Timeline/debt projection materialization remains deferred to T-0221.
- Dashboard timeline/debt projections exist for Phase 5.7: background refresh writes `.hadara/local/cache/dashboard/timeline/overview.json` and `.hadara/local/cache/dashboard/debt/summary.json`; `/api/dashboard/timeline` and `/api/dashboard/debt` are projection-first routes that return cached projections or honest missing warnings without foreground heavy recomputation. Legacy `/api/timeline` and `/api/debt` remain compatibility live-read routes until frontend migration.
- Dashboard authored frontend source now loads `/api/dashboard/core` before bootstrap/status fallback and independently backfills debt/timeline from projection routes. T-0223 extends the visual/a11y gate so committed redacted fixtures stub `/api/dashboard/core`, `/api/dashboard/timeline`, `/api/dashboard/debt`, and `/api/dashboard/projection/status`, with projection-ready/detail/stale/refreshing/missing/offline/degraded screenshot/a11y states and static schema/redaction checks. The Phase 5.7 follow-up rebuilt the served dashboard bundle through Docker and the Docker Playwright/axe visual gate passed for all projection states.
- Dashboard handoff summary parsing is table-aware for core and operations status read models: Markdown table headers and delimiters are skipped, while data rows are summarized as readable cell text for Home/timeline cards.
- Dashboard projected timeline reads defensively sanitize stale cached Markdown table header summaries at route time, replacing known handoff/next-work header summaries with bounded data-row reads from `docs/AGENT_HANDOFF.md` instead of requiring a projection rebuild before Home activity becomes meaningful.
- Dashboard evidence cards display canonical evidence `kind`, `result`, optional semantic strength, and visibility so records with valid legacy `hadara.evidence.v1` fields do not appear as `unknown`.
- Dashboard refresh/read-model hardening exists through T-0224: `docs/specs/dashboard/HADARA_Dashboard_Refresh_Refactor_Spec.md` defines refresh stage contracts, dashboard core/status validation extraction is table-first over `docs/AGENT_HANDOFF.md`, `/api/dashboard/refresh` uses async task-signal refresh plus core-fed timeline and aggregate-only debt projection stages, built route smoke confirmed latest validation fields no longer fall back to old T-0096 validation history, and done-level harness now blocks `TASK.md` Created/Updated metadata placeholders.
- Dashboard cooperative refresh progress exists through T-0225: `/api/dashboard/refresh` and `/api/dashboard/projection/status` expose current stage, processed/total, and last yield metadata; task projection refresh reports batch progress; `/api/dashboard/core` returns current stale/pending projection metadata without awaiting refresh completion; and the served dashboard Refresh control triggers projection refresh while keeping the current UI visible.
- Dashboard refresh responsiveness measurement exists through T-0226: projection status now exposes per-stage timing metadata (`stageStartedAt`, `stageFinishedAt`, `stageDurationMs`, `stageDurations`, and `slowStageWarnings`), `scripts/dashboard-refresh-responsiveness.mjs` records core p50/p95 during refresh, task-signals progress, stage durations, and optional `/tmp` comparison, and `docs/DASHBOARD_REFRESH_RESPONSIVENESS_MEASUREMENT.md` documents operational interpretation. Built measurement smoke showed workspace task-signals at 3780 ms with core p50/p95 49.6/62.0 ms, while `/tmp` task-signals was 147 ms with core p50/p95 0.5/1.6 ms.
- Dashboard work is paused after Phase 5.7 refresh/read-model hardening. Deferred dashboard items are refresh stage budget/streaming task scan, stronger projection freshness manifests, mounted-filesystem performance optimization, optional worker-thread projection rebuild, and deeper tab-by-tab productization only if needed.
- Task workflow status-history and Markdown section-reader hardening exists through T-0227: `task finish --execute` now appends or repairs a final Done row in `TASK.md` Status History, done-level harness validation rejects capsules whose latest Status History row is not Done with `TASK_STATUS_HISTORY_NOT_DONE`, and task/protocol/read-model section extraction now uses a shared heading-line Markdown section reader so inline ``## Status`` text is not treated as a section heading. T-0226 and the T-0196 dashboard bootstrap fixture capsule were aligned with the new gate.
- TUI shared operator read-model alignment exists through T-0228: `docs/specs/tui/HADARA_TUI_Shared_Operator_Read_Model_Spec.md` defines the projection-first terminal architecture, direct shared-service usage, no semantic Markdown parsing rule, stale/pending/degraded display requirements, and deferred TUI replacements for task detail, timeline/debt, and cache task indexing. `TuiReadModel` now includes shared dashboard core/projection status reports, ordinary TUI core reads can avoid projection writes, and snapshots display source/refresh/pending state.
- TUI selected task shared detail alignment exists through T-0229: selected task proof/evidence semantics now flow through `createDashboardTaskDetailReport()`, `TuiReadModel.selectedTask` carries `hadara.dashboard.task_detail.v1` plus shared proof data, and snapshot proof display prefers the shared proof note/status while the existing Markdown document viewer remains compatible until the next task index/cache replacement slice. Built `/mnt/f` snapshot smoke still took 42.56s, so T-0230 must target TUI task index/cache replacement.
- TUI projection-first task index/cache replacement exists through T-0230: TUI broad task lists now prefer the dashboard task projection and Task Board rows before falling back to legacy capsule scans, selected task document reads use the summary capsule path directly, fast cache validation no longer scans `tasks/` membership or recomputes every task hash, and `hadara tui --snapshot` uses the fast profile with selected proof lint deferred. The TUI read-model/render portion now measures around 160 ms on `/mnt/f`, while the built CLI snapshot smoke still takes about 4.05s because `dist/cli/main.js` startup eagerly loads broader CLI modules; T-0231 should target TUI CLI lazy loading.
- TUI CLI lazy startup exists through T-0231: `src/cli/main.ts` now lazy-loads command handlers after top-level dispatch instead of importing every CLI surface before command selection. Built `/mnt/f` `hadara tui --snapshot --compact --width 100 --height 26` now completes in 1.37s, meeting the 2s target while preserving full Docker validation.
- TUI Overview/Detail Markdown table cleanup exists through T-0232: concise Overview preview extraction skips Markdown table headers/delimiters, summarizes data rows for Goal/Step/Summary-style columns, supports multi-column evidence tables, reuses shared table-aware handoff section parsing for fast TUI `Next Recommended` status, and keeps inline-code or escaped pipe characters inside Detail table cells so table-first Task Capsule/Handoff docs do not render as raw header rows or bogus extra columns.
- Evidence v2 persisted writer MVP exists through T-0233: canonical evidence appends now write `hadara.evidence.v2` JSONL records with persisted durable ids, fingerprints, id metadata, category/outcome, visibility, artifacts, tags, and legacy v1 metadata; v1/v2 mixed reads are supported by evidence list/lint/normalizer, semantic gates, harness validation, task read/workbench/close, Dashboard task detail/timeline, TUI consumers, CLI evidence output, and MCP attach safety tests.
- Evidence v2 release read-model compatibility exists through T-0234: package-smoke, clean-checkout smoke, and release artifact evidence attachment now use the canonical v2 text artifact writer while preserving `artifacts/package-smoke`, `artifacts/clean-checkout-smoke`, and `artifacts/release-artifact` paths; release dry-run/strict release gates read v1/v2 mixed evidence and accept v2 persisted release proof when linked artifact validation passes.
- Evidence v2 migration preview exists through T-0235: `hadara evidence migrate --task <id> --to v2 --json` returns `hadara.evidence.migration_preview.v1` with `beforeHash`, per-line planned v2 records, deterministic planned ids/fingerprints, skipped records, warning issues, and execution metadata. Built CLI smoke on T-0015 planned 5 transforms with no writes.
- Evidence v2 migration execute exists through T-0236: `hadara evidence migrate --task <id> --to v2 --execute --before-hash <hash> --json` rewrites only the selected task `evidence.jsonl` through temp-file/rename when the hash matches and no blocking skipped records exist; existing v2 lines are preserved, v1 lines become planned v2 records, `EVIDENCE.md` and unrelated files are not rewritten, and mismatch/no-hash/skipped-record cases fail closed. This is operator-selected maintenance, not a recommended broad migration default; durable v2 ids are visible in JSONL/read models, not in the current Markdown evidence table.
- Evidence v2 writer stabilization and hardening exists through T-0332: `hadara evidence add-command` now accepts explicit `--category`, `--outcome`, repeated `--resolves`, and repeated `--supersedes` metadata while preserving legacy `result`; command evidence remains non-executing; incompatible explicit `--result`/`--outcome` pairs fail with `EVIDENCE_RESULT_OUTCOME_MISMATCH` through the CLI and core writer append boundary; v2 failed-evidence resolution now prefers exact markers from passed or recorded later evidence only, and same-category passed evidence is legacy-v1-only; evidence writer task lookup requires `TASK.md` and rejects ambiguous same-id valid capsules.
- Task finish state-doc advisory reporting exists through T-0237: `hadara.task.finish.v1` now includes `stateDocs` for `docs/DEVELOPMENT_SLICES.md`, `docs/PROJECT_STATE.md`, and `docs/AGENT_HANDOFF.md`, showing current/pending/missing state, task mention signal, and recommendations while preserving the `task finish --execute` write boundary of only `TASK.md` and `docs/TASK_BOARD.md`.
- Task close/audit boundary guidance exists through T-0238: `hadara.task.close.v1` now includes additive `lifecycle` metadata that names the validation/close/audit phases, close evidence loop boundary, close-evidence-only write boundary, and read-only audit command; `hadara.task.audit_close.v1` now includes additive `auditVerdict` metadata for post-close verdict, current/recorded hash match status, close evidence validity, and read-only status.
- Task-next handoff-priority recommendation policy exists through T-0239: `hadara.task.next.v1` now uses a handoff-first policy, exposes `summary.policy`, `recommendations[].sourceKind`, `sources.agentHandoff.nextRecommendedStep`, and non-primary `backlog` rows, so current handoff work is primary while old Partial Task Board rows remain visible as fallback/backlog.
- Dashboard and TUI operator surfaces are aligned around shared read models, projection-first state, and evidence semantics: Dashboard browser APIs call shared services, TUI calls the same services directly, selected-task proof semantics come from shared evidence/task-detail reports, and UI surfaces remain read-only observation layers rather than source-of-truth writers.
- Dashboard/TUI UI work is paused after T-0232. Do not continue polishing dashboard tabs, terminal visual details, or mounted-filesystem performance paths unless a concrete operator blocker appears. Deferred UI/performance items are streaming task scan for dashboard refresh, stronger dashboard projection freshness manifests, TUI fast/full mode contract documentation, deeper tab-by-tab TUI productization, and visual/detail polish only after core workflows progress.
- The next high-value roadmap direction should be selected from current roadmap/handoff state. Evidence v2 writer/compatibility/preview/execute/stabilization, finish state-doc advisories, and close/audit boundary guidance are now in place; do not mass-migrate historical capsules by default. Plausible next slices include durable v2 id visibility/Markdown ergonomics, release target configuration hardening, task capsule upgrade/remediation dry-run follow-up, release/package readiness hardening, or Hermes/MCP integration polish if an operator needs it.
- Dashboard projection contract exists: `hadara.dashboard.core.v1` is registered as a fixture schema and documented as the Phase 5.7 first-actionable projection contract with explicit freshness, completeness, refresh-state, pending-section, and stale-section metadata while keeping `/api/dashboard/bootstrap` compatibility additive.
- Private evidence manifests exist for readable project-boundary private evidence source artifacts: raw private bytes are copied only to the ignored private portable store, `hadara.privateEvidence.v1` manifests record SHA-256 hashes, byte counts, retention, and deferred encryption metadata, and manifest writes are privately audited. External absolute private source paths are not copied into the private portable store by default. Committed Task Capsule files and context export exclude private raw content, source paths, and private store paths.
- Context export read model exists as `hadara.context.export.v1`; read-only MCP `hadara.context.export` returns an in-memory payload with `contextPath: null` and does not write `.hadara/context/HADARA_CONTEXT.md`, while CLI `hadara hermes export-context` remains the file-writing path. Context export includes `docs/IMPLEMENTATION_SOP.md` as the authoritative workflow source, and `summaryOnly` currently returns a warning instead of silently pretending to summarize.
- Tools list read model exists as `hadara.tools.list.v1`; `hadara tools list --json` and read-only MCP `hadara.tools.list` report the current CLI help surface, read-only MCP tools, opt-in evidence attach status, `availability`/`risk` metadata, and disabled shell/provider/release/broad-write MCP surfaces from a neutral capability registry.
- Strict CLI argument helper parsing exists for string, required string, integer, and boolean flag options.
- Agent loop evidence attachment exists: deterministic fake-shell observations from `hadara run --task ...` can be attached as managed public command-log artifacts and reported in run JSON output.
- ScriptedProvider now consumes scripted steps in order for deterministic replay semantics.
- Task Capsule creation now includes an empty `evidence.jsonl` required by harness validation.
- Init profiles exist as `hadara init --profile basic|standard|governed --json`; `standard` is the default for ordinary projects, `basic` creates core session docs plus `docs/TASK_WORKFLOW_COMMANDS.md`, and `governed` adds security/refactor/roadmap governance docs for long-lived operational projects. `hadara.init.v1` reports created/existing scaffold files without mixing text logs into JSON. Generated SOP and AGENTS content is profile-aware, all profiles register the task workflow command guide, and root `docs/IMPLEMENTATION_SOP.md` records HADARA-dev as governed while keeping HADARA-dev-specific docs as conditional project-specific rows.
- Generated `hadara init` markdown docs now use table-first stable frames for Project State, Handoff, Architecture, AGENTS, Test Strategy, Security Model, Decisions, Refactor Log, and Roadmap where repeated records are expected; generic init output no longer carries default Hermes/MCP/dashboard/provider roadmap assumptions, and generated `.gitignore` keeps HADARA local/private state ignored without ignoring top-level project `data/`.
- Init follow-up commands exist as `hadara init doctor --json`, `hadara init upgrade --profile <profile> --json [--execute]`, `hadara init register-doc --path <path> --when <text> --purpose <text> --json [--require-exists] [--execute]`, and `hadara init enable-integration --integration hermes|mcp --json [--execute]`. They use `hadara.init.followup.v1`, keep write-like behavior dry-run by default, create missing scaffold docs, merge known generated profile metadata during upgrades, report profile metadata drift when higher-profile docs appear without metadata updates, validate Required Reading paths/table cells, use temp-file/rename rollback for multi-file integration guidance writes, clarify that integration enablement registers guidance rather than runtime behavior, and `hadara init` no longer eagerly creates `.hadara/local/portable` runtime-store directories.
- Project Protocol Consistency Phase 2 planning has been assimilated into tracked docs: new Task Capsule scaffold v2 frames are implemented in T-0152, and follow-up slices are planned for task-level protocol doctor checks, project-doc consistency, profile drift remediation guidance, safe dry-run-first remediations, and `hadara.protocol.consistency.v1` JSON contracts.
- Task Capsule creation now emits table-first v2 frames for metadata, plan, required-reading context, files, acceptance, routine/special tests, risks, decisions, evidence visibility/JSONL, and task-local handoff while harness/operational-debt checks still accept legacy capsule frames for existing work.
- Protocol consistency doctor exists for task, docs, profile, and all scopes: `hadara protocol doctor --task <id> --json` emits task-scoped `hadara.protocol.consistency.v1` reports for capsule drift; `hadara protocol doctor --scope docs --json` emits docs-scope reports for required project docs, SOP Required Reading missing paths, Task Board versus capsule drift, semantic handoff current-state fields, Project State current/latest task markers, Development Slices status/evidence drift, Decisions evidence sanity, Test Strategy validation baseline drift, SOP scaffold structure drift, mixed profile doc-set drift, and latest-completed handoff drift; `hadara protocol doctor --scope profile --json` emits profile-scope drift diagnostics with manual remediation hints for doc-set, profile metadata, and Required Reading alignment; and `hadara protocol doctor --scope all --json` plus default `hadara protocol doctor --json` emit an all-scope report that aggregates docs, profile, and active-task detail. Reports are read-only and diagnostic-first: `ok: true` means no blocking errors, warnings can still describe drift, and manual `remediations` are optional repair guidance. Profile reports expose `summary.profile.declared`, `detected`, `target`, and `source`; target is derived from metadata plus committed doc-set evidence. `hadara protocol remediate --fix <name> --json` supports four dry-run-first bounded fixes: Task Board row insertion, Decisions table frame insertion, Project State profile row upsert, and missing task `evidence.jsonl` creation. Planned writes expose `summary.beforeHash`; execute mode requires `--execute --before-hash <hash>` with the reviewed dry-run hash before writing, then writes through temp-file/rename with rollback-attempt issue reporting, checks per-action planned before hash/existence before writing, preserves existing Project State Metadata rows while upserting the HADARA Profile row, and warns/skips instead of appending into malformed Task Board frames or inserting duplicate semantic Decisions tables. `--task` and `--scope` are mutually exclusive for doctor reports.
- Protocol consistency and remediation schema fixtures are registered as `hadara.protocol.consistency.v1` and `hadara.protocol.remediation.v1`, with focused service/CLI contract tests covering task/docs/profile doctor reports, remediation reports, report-level `summary.beforeHash`, execute before-hash guards, and per-action hash/existence fields.
- Latest completed task: T-0159 Protocol Consistency JSON Contract registered protocol consistency/remediation schema fixtures and contract tests.
- Latest completed task: T-0160 Protocol Doctor All Scope added all-scope protocol doctor aggregation and default `protocol doctor --json` behavior.
- Latest completed task: T-0161 Markdown Table Helper Extraction added `src/services/markdown-table.ts`, removed duplicated protocol/profile/harness Markdown row parsers, and refreshed built output after Docker validation.
- Latest completed task: T-0162 Doctor Remediation Hint Unification added additive safe-auto `suggestedFix` hints and remediation objects to protocol doctor reports while preserving dry-run-first `protocol remediate --fix` execution.
- Latest completed task: T-0163 Task Capsule Upgrade Scaffold Command added dry-run-first `hadara task upgrade-scaffold --task <id> --json [--execute]` with append/create-only safe writes, ambiguous-frame skips, before-hash/existence checks, and `hadara.task.upgrade_scaffold.v1` fixture-level schema. T-0240 later hardened execute mode so planned writes expose `summary.beforeHash` and require `--execute --before-hash <hash>`.
- Latest completed task: T-0164 Protocol Surface Docs Alignment aligned CLI help, README, JSON contract docs, schema docs, schema index notes, and Phase 2 planning docs with the implemented protocol doctor/remediation/task upgrade surfaces.
- Latest completed task: T-0165 Evidence Lint and Doctor Validation added read-only `hadara evidence lint --task <id> --json`, registered `hadara.evidence.lint.v1`, surfaced evidence lint failures through task protocol doctor, and documented the close validation/evidence fixed-point redesign.
- Latest completed task: T-0166 Task Close Plan Report added read-only `hadara task close --task <id> --json` reports with done validation, evidence lint, task protocol doctor checks, close evidence loop-boundary metadata, and machine-readable `nextActions`.
- Latest completed task: T-0167 Task Close Execute MVP enabled `hadara task close --task <id> --execute --json` to append canonical close evidence only after done validation, evidence lint, and task protocol doctor blockers pass.
- Latest completed task: T-0168 Task Ready Preflight added read-only `hadara task ready --task <id> --level done --json` reports with friendly blocker summaries, check booleans, and nextActions before close.
- Latest completed task: T-0169 Evidence Command UX added `hadara evidence add-command --task <id> --summary <text> --result <result> --json` as a canonical command-log evidence writer without shell execution or stdout/stderr capture.
- Latest completed task: T-0170 Close UX Polish and Audit Semantics clarified task close report/source hash semantics, improved execute-mode nextActions, added append result paths, and added read-only `hadara task audit-close --task <id> --json`.
- Latest completed task: T-0171 Task Workbench Status Report added read-only `hadara task status --task <id> --json` as a Phase 3 workbench projection over close, evidence, protocol, and Task Board sources without duplicate done-level harness validation.
- Latest completed task: T-0172 Workbench Suggested Action Engine centralized `task status` nextActions with action priorities, source issue codes, remediation/audit kinds, and dry-run/execute command pairing.
- Latest completed task: T-0173 Workbench Schema Contract registered fixture-level `hadara.task.workbench.v1` with schema index/runtime loader coverage and raw service report validation.
- Latest completed task: T-0174 Worker-Friendly Text Output added grouped non-JSON `task status` and `task audit-close` output for worker-facing State/Evidence/Protocol/Close/Audit/Suggested-next summaries.
- Latest completed task: T-0175 Dashboard TUI MCP Read Projection Prep added `docs/TASK_WORKBENCH_READ_MODEL_CONTRACT.md` and aligned dashboard/MCP guidance for future read-only selected-task consumers.
- Latest completed task: T-0176 Evidence From Command Design added `docs/EVIDENCE_FROM_COMMAND_DESIGN.md` and clarified that shell-executing evidence capture remains unimplemented/design-only.
- Latest completed task: T-0177 Task Workbench Hardening fixed true Task Board status projection, normalized optional nextAction fields, split close evidence presence from valid closure, and documented `task.status` `ok` semantics.
- Latest completed task: T-0178 Runtime Version CLI Origin Doctor added read-only `hadara version --verbose --json` as `hadara.runtime.version.v1` for CLI entry, project root, package/git/node metadata, and build freshness diagnostics.
- Latest completed task: T-0179 Docker Dev Sync-Build Script added repo-level `dev:docker-check` and `dev:docker-sync-build` helpers for Docker temp-copy validation, workspace dist refresh, and runtime version smoke.
- Latest completed task: T-0180 Task Finish / Status Sync MVP added dry-run-first `hadara task finish --task <id> --json` as `hadara.task.finish.v1`, with bounded execute writes for `TASK.md` and `docs/TASK_BOARD.md` status/path only.
- Latest completed task: T-0181 Task Next Recommendation added read-only `hadara task next --json` as `hadara.task.next.v1`, preferring planned Development Slices and falling back to Task Board with capsule/create guidance.
- Latest completed task: T-0182 Schema Stability Classification documented stable/additive/compatibility/deprecated/experimental field classes and annotated `hadara.task.workbench.v1` compatibility aliases.
- Latest completed task: T-0183 Focused Test Command UX added `npm run test:focused -- <path>` guidance and script support for narrow Vitest file runs.
- Latest completed task: T-0184 Task Finish Write Safety Hardening added hash/existence metadata, temp-file/rename writes, malformed frame/no-op guards, and safer task-next createCommand quoting.
- Latest completed task: T-0185 Task Workflow Command Semantics Audit added `docs/TASK_WORKFLOW_COMMANDS.md`, aligned README/AGENTS/SOP/CLI JSON contract guidance, and locked the standard task loop with docs regression tests.
- Latest completed task: T-0186 Evidence Proof Semantics Foundation added compatibility-first evidence normalizer/semantics modules and focused tests without writer migration.
- Latest completed task: T-0187 Evidence Lint Semantic Integration added additive evidence lint semantic summaries/issues and kept legacy v1 compatibility in summary counts instead of noisy issues.
- Latest completed task: T-0188 Protocol and Harness Semantic Gates added task-scoped protocol semantic issue coverage and done-level harness semantic blockers/warnings.
- Latest completed task: T-0189 Dashboard/TUI Evidence Semantic Contract documented selected-task evidence semantic sources, proof status derivation, and no-raw-evidence-parsing boundaries for Dashboard/TUI consumers.
- Latest completed task: T-0190 Evidence v2 Writer and Migration Plan documented v2 writer/migration design, schema/test strategy posture, and dry-run-first non-goals without implementing writer changes.
- Latest completed task: T-0192 Evidence Semantics Hardening added normalized evidence identity stability metadata, actual JSONL source-line preservation in evidence lint, strict release proof candidate selection for release dry-run, private-only auditability-warning UI semantics, and gate-vs-dry-run release strictness docs.
- Latest completed task: T-0193 Dashboard Live Read Binding made the served dashboard live-read-first over `/api/status`, preserved fixture/inline fallback, added source provenance UI, and kept refresh read-only.
- Latest completed task: T-0194 Dashboard Operator Console Layout reworked the dashboard shell into Agent Lane, Workstream, Evidence Lens placeholder, and Bottom Inspector sections with read-only action language.
- Latest completed task: T-0195 Dashboard Selected Task Evidence Lens added read-only dashboard workbench/evidence-lint routes and bound selected-task proof status to shared evidence semantics.
- Latest completed task: T-0196 Dashboard Timeline Read Model added a schema-registered deterministic dashboard timeline report and `/api/timeline` route consumed by the Workstream panel.
- Latest completed task: T-0197 Dashboard Bootstrap Read Model added a schema-registered first-paint dashboard bootstrap aggregate and `/api/dashboard/bootstrap` route with disabled cache metadata, compact selected-task proof, and invalid selected-task degradation.
- Latest completed task: T-0198 Dashboard Progressive Bootstrap Frontend changed the dashboard first read to `/api/dashboard/bootstrap`, preserved status/fixture/inline fallback, added cache status display, kept previous successful in-memory state on refresh failure, and removed stale Workstream copy.
- Latest completed task: T-0199 Dashboard Task Detail Aggregate Endpoint added a schema-registered selected-task aggregate and `/api/dashboard/task-detail?taskId=` route, then switched the frontend Evidence Lens to that route instead of workbench/evidence/timeline fan-out.
- Latest completed task: T-0200 Dashboard Timeline Identity Hardening made evidence timeline events use normalized evidence identity metadata including fingerprint, sourceLine, idSource, and idStability.
- Latest completed task: T-0225 Dashboard Cooperative Refresh Progress added progress metadata, non-blocking core stale/pending semantics, and projection-triggered UI refresh.
- Active / next task: Select the next roadmap slice after dashboard refresh/read-model hardening.
- Phase 3 source plan: `docs/specs/HADARA_Phase3_Task_Operator_Console_Development_Plan.md`.
- Task workbench status report exists as `hadara task status --task <id> --json`; centralized suggested-action generation and fixture-level schema registration are implemented. T-0177 hardened true Task Board status projection, optional nextAction normalization, and close evidence/valid-closure semantics.
- Phase 3 task operator console capsule sequence T-0171 through T-0176 is complete: status projection, suggested actions, schema contract, text output, read-consumer projection prep, and evidence-from-command design boundary.
- Planned close-model follow-ups are tracked as T-0166 through T-0169: task close plan report, close execute MVP with close evidence append only, task ready preflight, and command-log evidence writer UX.
- Harness validation supports `--level draft|done`; done-level validation requires Done status, completed acceptance, evidence records, and updated handoff sections.
- Done-level harness validation now also checks `docs/TASK_BOARD.md` for exactly one row for the validated task, `Done` status, and the expected Task Capsule path, catching duplicate or stale append/update results before completion.
- Done-level harness validation now rejects completed Task Capsules whose standard Markdown files still contain initial scaffold placeholders or default empty content, while draft-level validation remains structural.
- Done-level harness validation now rejects completed Task Capsules whose `TASK.md` Created/Updated metadata is missing, blank, `TBD`, or not formatted as `YYYY-MM-DD`.
- Run scenario scaffolding exists as `hadara run scaffold --task <id> --command <command>`, generating deterministic ScriptedProvider and fake-shell fixture JSON files under `.hadara/scenarios/`.
- Runtime validation now rejects unsupported permission modes and evidence result values; harness validation enforces evidence JSONL enum values.
- JSON-mode CLI parse and validation failures return a shared `hadara.cli.error.v1` fallback envelope.
- CLI JSON output policy is documented in `docs/CLI_JSON_CONTRACT.md`.
- Policy safe command classification requires exact token matches; suffixes are not implicitly safe.
- Agent loop run results now fail when fake-shell observations fail, including non-zero fake-shell exit codes.
- Run scenario scaffolding now rejects duplicate scenario files instead of silently reusing stale content.
- CLI handler extraction pass is complete: `src/cli/main.ts` is a top-level dispatcher and command groups live in focused `src/cli/*` modules.
- Project handoff is compacted: current state lives in `docs/AGENT_HANDOFF.md`, with historical task and validation history in dedicated history docs.
- Old Draft task capsules have been reclassified: T-0003 is Superseded, and T-0006 is Partial with remaining Hermes/MCP bridge scope deferred to the roadmap.
- Read-only Hermes/MCP bridge contract is documented in `docs/MCP_BRIDGE_CONTRACT.md`; stdio server and read tools are implemented.
- MCP JSON-RPC stdio server exists as `hadara mcp serve`; it supports discovery/lifecycle requests and read-only tools for task list/read, handoff read, project state read, policy evaluate, harness validate, evidence list, context export, and tools list.
- MCP bridge contract tests validate JSON text payload wrapping, notification no-response behavior, dispatch issue-code mapping, and CLI JSON parity for task list, policy evaluate, and harness validate.
- MCP evidence attach contract is documented in `docs/MCP_EVIDENCE_ATTACH_CONTRACT.md`; the tool is implemented only for explicit opt-in mode.
- MCP evidence attach can be enabled explicitly with `hadara mcp serve --enable-evidence-attach`; default MCP startup remains read-only and does not advertise the tool.
- MCP evidence attach safety tests cover JSON payload shape, safe public artifact copies, workspace boundary rejection, public artifact secret rejection, and invalid input mapping.
- MCP initialize metadata now reflects default read-only mode versus evidence attach-enabled mode, including `hadara/evidenceAttach`, `hadara/writes`, and disabled shell/provider flags. `serverInfo.version` is read from HADARA package metadata instead of stale bootstrap metadata.
- MCP evidence attach now requires per-call approval metadata with an actor and reason before writing evidence.
- MCP evidence attach write attempts are audited to the private portable audit store on both success and report-level failure.
- Operations Status JSON exists as `hadara status --json` and `hadara ops status --json` with schema `hadara.ops.status.v1` for future dashboards and external agents.
- Operations Status JSON reports warning issues for missing source documents or validation baselines, keeps stable dashboard-facing task count keys, exposes raw status counts separately, and parses explicit project phase markers.
- Operations Status JSON now includes dashboard-facing `health`, true raw status counts, and normalized status counts; `docs/DASHBOARD_READ_MODEL_CONTRACT.md` maps dashboard panels to field paths.
- Dashboard design references live under `docs/design/`; the current mockup is reference-only and does not implement UI behavior.
- A minimal static dashboard reference exists at `docs/design/dashboard/index.html`; it consumes the static sample status fixture with an inline fallback and has no backend, live CLI execution, MCP connection, file writes, or build step.
- The comfort dark mockup is now the preferred dashboard visual baseline for shell layout, visual hierarchy, palette, card grouping, and navigation feel; `hadara.ops.status.v1` remains the authoritative data contract.
- Static dashboard fixture binding smoke coverage verifies dashboard `data-field` attributes map to sample fixture-backed or derived `hadara.ops.status.v1` values.
- `hadara dashboard serve` serves the static sample-backed dashboard and fixture through allowlisted routes only; it does not execute live status commands, connect to MCP, write files, stream events, or persist browser state.
- `hadara dashboard serve` now exposes read-only local dashboard API routes for `/api/status`, `/api/tasks`, `/api/evidence?taskId=<task-id>`, `/api/active-run`, and `/api/debt`, backed by existing shared read-model services and still without shell execution, provider calls, MCP writes, task mutation, evidence writes, live streaming, or browser-state persistence.
- Terminal TUI mockups exist under `.mockup/tui` and `.mockup/tui-final`; T-0099 aligned those learnings into main docs as a future read-only local work console over existing read models.
- Dashboard serving is hardened for GET/HEAD-only static responses, no-store/no-sniff/content-security headers, and traversal-like route rejection.
- Harness evidence index validation now requires canonical `hadara.evidence.v1` records to include non-empty `time`, `summary`, and `visibility`, and recent timestamp-only dashboard evidence records have been migrated.
- Static dashboard server responses now fail predictably for missing project roots or allowlisted files, returning safe 404 responses, and request handling catches unexpected response generation errors as 500 responses.
- Architecture and roadmap docs now distinguish implemented bootstrap capabilities, partial self-hosting surfaces, and deferred full-dogfooding work.
- Roadmap is frozen around the v0.3 Operations Layer: single active agent/session, stable CLI JSON, read-only MCP, evidence/handoff continuity, context export, compatibility fixture, and static operations dashboard remain in scope while multi-agent concurrency, broad MCP writes, MCP shell/release/package execution, live dashboard streaming, and real provider execution as the default path remain out of scope.
- Context export now includes roadmap and development slice ordering and instructs external agents to prefer HADARA CLI JSON or read-only MCP surfaces before falling back to raw repository documents.
- A Hermes-like compatibility fixture exists for the v0.3 external-agent flow; it replays exported context expectations and read-only MCP calls for project state, task list/read, handoff read, policy evaluate, and harness validate while confirming write/execution-like MCP tools are unavailable.
- CLI/MCP read-model parity has started: project and handoff read logic now lives in a shared service used by Operations Status JSON and MCP project/handoff tools, with contract tests comparing MCP payloads against shared services and CLI/domain report builders.
- Single active run state exists as a local project manifest at `.hadara/local/state/active-run.json`; Operations Status JSON exposes a read projection with resume guidance and a stale handoff warning when the active task id is missing from `docs/AGENT_HANDOFF.md`.
- Active run read surfaces exist as `hadara run-state show --json`, `hadara run-state resume --json`, read-only MCP `hadara.active.run.read`, and read-only MCP `hadara.active.run.resume`; active-run writes remain deferred.
- Active-run resume guidance resolves canonical Task Capsule paths from `activeRun.taskId`, warns with `ACTIVE_RUN_CAPSULE_MISMATCH` when local manifest capsule paths are stale, and uses canonical paths in `resume`/`mustRead` when the Task Capsule exists.
- Operational debt tracking exists in `docs/OPERATIONAL_DEBT.md` and `src/services/operational-debt.ts`; it promotes `known_issue.log` themes into structured records, reports capsule size indicators, and warns on premature acceptance checks before Done status or evidence.
- Operations state robustness is hardened: malformed active run local state degrades status JSON with warnings instead of throwing, active runs referencing missing Task Capsules are reported, premature acceptance uses valid evidence records, and shared Markdown section extraction matches heading lines only.
- Reusable Docker development workflow is documented: the `hadara-dev` container can stay running, dependency-heavy work happens in `/tmp/hadara`, and new Task Capsules should be created through the HADARA CLI with `--project /workspace`. For HADARA-dev CLI work, Docker is the preferred validation/build path; after CLI changes, agents must refresh `/workspace/dist` from `/tmp/hadara/dist` before final built-CLI smokes and must not assume container-global `/usr/local/bin/hadara` is the latest development build.
- V1.0 planning has been split into concrete references: `docs/V1_0_CAPSULE_BACKLOG.md` tracks future capsule candidates and `docs/V1_0_IMPLEMENTATION_SCHEMAS.md` preserves detailed schema/file/test notes from the v1.0 technical plan.
- V1.0 planning docs now explicitly separate T-0066 through T-0070 current implementation from future expansion targets, including compatibility fixture location, partial service parity, active-run local schema/path, operational debt persistence caveats, and degraded-read robustness scope.
- TUI native runtime planning is now reflected in the main docs: `docs/V1_0_IMPLEMENTATION_SCHEMAS.md` preserves the full unabridged `docs/specs/HADARA_TUI_Mockup_Parity_HADARA_Native_Runtime_Design.md` body, while `docs/V1_0_CAPSULE_BACKLOG.md`, `docs/SCHEMAS.md`, and `docs/TEST_STRATEGY.md` record the actionable cache, visual parity, mouse/resize, compatibility-adapter, schema posture, and validation requirements.
- Schema layer planning exists in `docs/SCHEMAS.md`; `src/schemas/schema-index.json` registers JSON Schema fixtures for evidence list, context export, tools list, active-run read models, private evidence manifests, and release gate reports. Broad runtime schema validation and release gates remain deferred.
- Active-run projection/resume schema fixtures exist for `hadara.active_run.projection.v1` and `hadara.active_run.resume.v1`.
- Active-run projection/resume reports now use limited runtime schema validation through `src/core/schema.ts`; malformed active-run local state degrades with `ACTIVE_RUN_MANIFEST_INVALID`, while active-run report schema assertion failures degrade with `ACTIVE_RUN_REPORT_SCHEMA_INVALID`.
- Task read-model logic now lives in shared `src/services/task-read-model.ts`; CLI task JSON compatibility exports and read-only MCP task list/read both use the shared service.
- Policy report logic now lives in shared `src/services/policy-service.ts`; CLI policy check/preflight compatibility exports and read-only MCP policy evaluate use the shared service.
- Cleanup follow-up notes now document redaction policy observability gaps, schema strictness levels, task.read embedded evidence normalization gaps, and PolicyService authorization limitations before release-gate work.
- Task read evidence embedding now reuses evidence-list normalization; `task.read` returns normalized `evidenceIndex` records and a sanitized `files["evidence.jsonl"]` view instead of raw private evidence paths or unredacted summaries.
- Read-only MCP `hadara.task.read` excludes private evidence metadata by default; callers must pass `includePrivate: true` to receive sanitized private evidence metadata. `files["evidence.jsonl"]` is a sanitized read-model view, not raw file bytes.
- Harness validation report logic now has shared `src/services/harness-service.ts`; CLI `harness validate` and read-only MCP `hadara.harness.validate` both use the shared service boundary.
- Operations Status JSON report logic now has shared `src/services/operations-status-service.ts`; CLI `hadara status` and `hadara ops status` use the shared service boundary while `src/cli/status-json.ts` remains a compatibility export.
- Active-run projection and resume guidance reuse `src/services/active-run-state.ts` across Operations Status, CLI run-state reads, and read-only MCP active-run tools.
- Operational debt read surfaces exist as `hadara debt list --json`, `hadara debt show <id> --json`, read-only MCP `hadara.debt.list`, and read-only MCP `hadara.debt.show`.
- Operations Status JSON includes operational debt aggregate counts for total/open/status/severity/high-open debt.
- A read-only release-gate report exists as `hadara release gate --mode advisory|strict --json`; advisory mode warns and keeps `ok: true` for open high-severity debt, strict mode reports `ok: false` if open high-severity debt exists, and neither mode executes release, packaging, shell, provider, or deployment actions.
- Release-gate reporting now includes a read-only planning/checklist release report for package bin metadata, validation scripts, Node 22 policy, CI clean install behavior, clean-checkout smoke planning, generated artifact boundaries, documented remote CI observation evidence, and operational debt. Readiness failures use check-specific issue codes, are warning-only in advisory mode, and are blocking in strict mode. The release gate does not create clean checkouts, run package/install smoke, diff generated artifacts, call GitHub, or execute remote CI.
- Release-gate package metadata readiness now derives the expected current-version marker from `package.json` and accepts versioned HADARA package-smoke tarball examples without pinning the exact current RC number, reducing metadata drift on later RC bumps.
- Remote GitHub Actions CI has been observed on `main`: run #109 for commit `8b4f33d1bf926d051cf63e13ca2de222bfc22d8c` completed successfully with `npm ci` and `npm run check`; local Docker validation remains the primary reproducible completion evidence.
- High-severity operational debt OD-0003 and OD-0008 are mitigated by current required-reading/context safeguards and done-level evidence validation. Operational debt aggregates now report no open high-severity debt, so strict release gate can pass when the documented readiness checks pass.
- Clean-checkout package smoke planning is explicit in `docs/TEST_STRATEGY.md`: the current sequence is `npm ci`, `npm run build`, `npm run check`, `doctor --json`, `ops status --json`, and strict release-gate smoke in a disposable copy. The release gate checks these planning markers while still performing no packaging, publishing, archive/checksum generation, deployment, GitHub calls, MCP release/package execution, or committed artifact writes.
- Executable package-smoke artifact-boundary planning is explicit in `docs/TEST_STRATEGY.md`: future package smoke execution must use a disposable workspace, keep package artifacts/raw logs temporary or private by default, commit only reduced public UTF-8 evidence summaries under `tasks/<task-id>/artifacts/package-smoke/` after redaction policy checks, and report `hadara.packageSmoke.v1`-style metadata without private paths or raw package/log content. The read-only release gate checks these markers and still performs no package-smoke execution.
- Package-smoke command-surface planning is explicit in `docs/TEST_STRATEGY.md`: the future primary command is `hadara package smoke`, with `hadara release smoke` avoided as the primary surface; flags and semantics are documented for dry-run planning, JSON reports, task/evidence attachment, disposable workspace/source selection, temporary retention, timeout, private logs, cleanup/failure behavior, and the default non-callable MCP boundary. The read-only release gate checks these markers through `PACKAGE_SMOKE_COMMAND_SURFACE` and still performs no package-smoke execution.
- Package metadata is now in release-candidate mode: the package name remains `hadara`, version is `0.2.0-rc.2`, `private` is `false`, the package whitelist is `dist/`, `README.md`, `LICENSE`, and `package.json`, and MIT is recorded through root `LICENSE`. T-0282 refreshes this metadata state after the init scaffold protocol guidance follow-up; the release gate checks these markers through `PACKAGE_METADATA_RELEASE_READINESS`. The current published npm release candidate is `hadara@0.2.0-rc.2`; the operator ran the approval-gated T-0282 helper and npm view verified `0.2.0-rc.2`.
- T-0275 supersedes the T-0269 `0.2.0-rc.0` publish candidate. The operator authenticated to npm outside the repo, ran the approval-gated manual helper, typed the helper confirmation, and the helper recorded publish evidence verifying `npm view` returned `0.2.0-rc.1`.
- A preview Python bridge package now lives under `python/`: package name `hadara`, version `0.2.0rc1`, dedicated README, `src/hadara` modules, and focused tests. It is a PyPI bridge package only; the official runtime remains the npm package `hadara@0.2.0-rc.1`. TestPyPI and PyPI publishes for `hadara==0.2.0rc1` are complete through the manual Trusted Publisher workflow.
- npm now contains the first HADARA release candidate: `hadara@0.1.0-rc.0`, with tarball URL `https://registry.npmjs.org/hadara/-/hadara-0.1.0-rc.0.tgz`. T-0143 performed npm-only publish after fresh evidence and explicit manual confirmation; no GitHub Release, tag push, Docker image, installer script, USB flow, or README release/install cleanup was performed in that capsule.
- Installer script surface and schema planning is explicit in `docs/RELEASE_READINESS.md`: POSIX and Windows installer script paths, portable launcher paths, POSIX/Windows/USB/WSL install locations, Node 22 and WSL Windows-shim checks, and `hadara.install.plan.v1` dry-run report shape are documented. Public install-plan target paths are redacted path-reference objects rather than raw strings, `mode: execute` is schema-reserved only, and T-0129 dry-run implementation must reject execute mode or return `INSTALL_EXECUTION_DISABLED`. The read-only release gate checks these markers through `INSTALLER_SCRIPT_SURFACE_SCHEMA` while still performing no installer execution, install mutation, PATH/profile mutation, package smoke, publish, release artifact, GitHub Release, Docker image, or registry mutation.
- Installer dry-run planning exists as `hadara install plan --json`; it emits schema-valid `hadara.install.plan.v1` reports for Linux/Windows/WSL/USB targets, keeps `posix` as a compatibility alias for Linux-style installs, uses Linux-style default install suggestions for WSL, describes future confirmed execute/apply writes with `wouldWrite: true`, redacts public source/target paths, requires explicit `--usb-root` or `--target` for USB planning, reports `USB_ROOT_REQUIRED` when USB root is missing, reports Node 22 and WSL Windows-shim checks, and returns `INSTALL_EXECUTION_DISABLED` for `--mode execute` without installer mutation. No installer scripts, portable launchers, install directories, package artifacts, or MCP installer execution surfaces are created.
- Install matrix smoke planning is explicit in `docs/RELEASE_READINESS.md` and `docs/TEST_STRATEGY.md`: Linux source/package, WSL source, Windows source/package, USB Windows, USB WSL, and installed-CLI major-feature smoke rows are defined, with Docker/Linux not replacing real Windows validation, USB roots requiring explicit user selection, package-install rows blocked until package smoke/release artifacts exist, reduced public evidence boundaries, and raw logs/private paths kept temporary or private/local. The read-only release gate checks these markers through `INSTALL_MATRIX_SMOKE_PLAN` while still performing no install-matrix execution.
- Core feature smoke runner exists as `hadara smoke run --profile core --json`; it emits schema-valid `hadara.featureSmoke.v1` reduced reports for doctor, status, task list, tools list, TUI snapshot, and advisory release gate through service/read-model calls. The runner is read-only, sets `executionMode: "service-read-model"`, `binaryExecuted: false`, `launcherChecked: false`, and `packageInstallChecked: false`, performs no subprocess/package/install/release mutation, avoids raw logs and raw TUI output, validates registered sub-report schemas, and keeps `release-readiness` reserved but deferred with `FEATURE_SMOKE_PROFILE_DEFERRED`.
- Package-smoke report schema and fixtures exist as `hadara.packageSmoke.v1`; deterministic fixtures cover success, step failure, redacted path, private/raw omission, and public reduced evidence cases. The schema requires redacted workspace/source path references, provider metadata naming the current implementation as npm-specific through `npm-package-smoke`, explicit false privacy markers for raw logs/package contents/private paths/env secrets/private store paths, and false release mutation/publish execution markers.
- Package-smoke dry-run planning exists as `hadara package smoke --dry-run --json`; it emits schema-valid `hadara.packageSmoke.v1` reports previewing npm source validation, disposable workspace planning, planned npm pack/install/core-smoke/evidence steps, and reduced artifact metadata. It is read-only, keeps `npmPackExecuted`, `packageInstallExecuted`, `featureSmokeExecuted`, `releaseMutationExecuted`, and `publishExecuted` false, redacts absolute source/workspace paths, attaches no evidence by default, and performs no `npm pack`, package install, subprocess execution, artifact write, release mutation, publish, GitHub Release, Docker image build, provider call, or MCP package-smoke behavior.
- Local package-smoke execution exists as explicit `hadara package smoke --execute --json`; it is currently an npm provider smoke that runs `npm pack` for source-checkout input, installs the tarball into an isolated disposable prefix using npm's `-g --prefix` mechanics without system-global install, invokes installed command-form `hadara doctor --json` and `hadara smoke run --profile core --json`, cleans up by default, and emits a reduced schema-valid `hadara.packageSmoke.v1` local report. It does not publish, mutate release state, install globally, create GitHub Releases, build Docker images, expose an MCP package-smoke surface, include raw npm logs, include raw package contents, or expose private absolute paths/private store paths in public output. Public reduced evidence attachment is available only when explicitly requested with `--attach-evidence --task <task-id>`.
- Clean-checkout source smoke exists as explicit `hadara smoke clean-checkout --execute --json`; it copies source into a disposable checkout, runs `npm ci`, `npm run build`, `npm run check`, built CLI `doctor --json`, built CLI `ops status --json`, and built CLI strict release gate, then cleans up by default and emits a reduced schema-valid `hadara.cleanCheckoutSmoke.v1` report. It keeps the source workspace unchanged, uses built `node dist/cli/main.js` as the source-checkout internal fallback, keeps installed user-facing `hadara` command-form validation separate for package/install smoke, and performs no package install, publish, release mutation, GitHub Release, Docker image build, or public raw-log retention. Public reduced evidence attachment is available only when explicitly requested with `--attach-evidence --task <task-id>`.
- Smoke evidence integration exists for package-smoke and clean-checkout smoke execution: explicit `--attach-evidence --task <task-id>` writes reduced UTF-8 public summaries under `tasks/<task-id>/artifacts/package-smoke/` or `tasks/<task-id>/artifacts/clean-checkout-smoke/`, updates `EVIDENCE.md` and `evidence.jsonl`, and runs the public artifact redaction policy before committing artifact content. It does not retain raw logs publicly, include package contents/private paths/private store paths, execute release gates automatically, publish, create release artifacts, call GitHub, or expose MCP smoke execution.
- Release artifact building exists as explicit `hadara release artifact --execute --json`; it stages a whitelisted package containing `dist/`, `README.md`, `LICENSE`, and `package.json`, runs `npm pack`, generates SHA-256 checksum and manifest files, verifies package contents, and emits a reduced schema-valid `hadara.releaseArtifact.v1` report. Default output is disposable, explicit `--output <dir>` is local retained output, and the command does not publish, create GitHub Releases, build Docker images, execute installer/install-matrix behavior, expose MCP release execution, or include public raw logs/private paths/private store paths.
- Release gate evidence freeze exists: `hadara release gate --mode advisory|strict --json` remains read-only but now reads existing Task Capsule evidence records for package-smoke, clean-checkout smoke, and release-artifact readiness. Strict mode blocks on missing package-smoke, clean-checkout smoke, or release-artifact evidence with stable issue codes, advisory mode warns, and install-matrix evidence enforcement remains deferred until an executable install-matrix smoke surface exists. `hadara.smokeEvidenceSummary.v1` and `hadara.releaseArtifact.manifest.v1` are registered schema fixtures before optional summary/manifest consumers rely on them.
- CI/release workflow target decisions are documented: npm package is the primary release target, GitHub Release with tarball/checksum/manifest is secondary, and Docker image publishing is deferred unless the product/server runtime surface changes. Required token names are documented as `NPM_TOKEN`, `GITHUB_TOKEN`, or `HADARA_GITHUB_RELEASE_TOKEN` without storing values, and publish/deploy remains explicit approval only.
- Release dry-run now exposes a multi-ecosystem release target descriptor model while preserving the current npm-primary implementation. The active descriptors are npm package, GitHub Release, and deferred Docker image; if `pyproject.toml` exists, a `python-package-preview` descriptor may appear as read-only metadata only. HADARA does not currently build Python distributions, run pip/twine smoke, load PyPI credentials, publish to PyPI, build Docker images, or create GitHub Releases.
- Final release dry-run exists as read-only `hadara release dry-run --json`; it emits schema-valid `hadara.releaseDryRun.v1`, checks the strict release gate, cross-checks linked public package-smoke, clean-checkout smoke, and release-artifact evidence artifacts, verifies release artifact package version and manifest hash, checks git commit metadata when present, and plans descriptor-backed npm package/GitHub Release/Docker image targets without publish, GitHub Release creation, Docker build, registry mutation, token loading, or release mutation. After T-0245, built release dry-run returns `ok:true`, readiness `ready`, blockers 0, and `RELEASE_ARTIFACT_EVIDENCE` passed for T-0245. `hadara release artifact --execute --attach-evidence --task <task-id>` attaches a reduced public `hadara.releaseArtifact.v1` report under `tasks/<task-id>/artifacts/release-artifact/`.
- T-0250 Python release advisory read model exists: release dry-run now emits additive `providerAdvisories` for the Python preview provider with `status:"preview"`, `smokeEvidence:"present"|"missing"|"stale"`, and `blocking:false`. Python package-smoke evidence is surfaced for operator awareness only; it does not satisfy npm package-smoke evidence, does not affect release readiness, does not promote Python to primary, and adds no PyPI token, upload, publish, or registry mutation behavior.
- T-0251 release target configuration preview exists: release dry-run now emits additive `releaseTargetConfiguration` with `.hadara/release-targets.json` as the future read-only config path, effective primary fixed to `npm-package`, explicit npm primary / Python preview / Docker deferred target roles, and `autoPromotion:false`. A project config requesting a non-npm primary is reported as unsupported preview metadata while effective primary remains npm; pyproject discovery alone still cannot promote Python primary, and no PyPI, Docker, GitHub, registry, token, or publish mutation behavior is added.
- T-0252 release target configuration warning surfacing exists: release dry-run now maps `releaseTargetConfiguration.issues` into a non-blocking `RELEASE_TARGET_CONFIGURATION` warning check, increments `readiness.warnings`, and preserves exact config issue codes in `diagnostics.advisories` with `blocking:false`. Unsupported primary requests and invalid `.hadara/release-targets.json` remain advisory only; `ok` and effective primary target remain governed by npm-primary release readiness. The parser remains preview-level and only interprets `primaryTarget`; future real config support should define `hadara.releaseTargetConfig.v1` with supported fields, ignored fields, unsupported-field warnings, non-blocking warning semantics, and migration behavior.
- Release provider contract exists: `ReleaseProvider` exposes provider support states for `detect`, `buildPlan`, `smokePlan`, `artifactPlan`, and `publishPlan`; `NpmReleaseProvider` wraps existing npm-primary descriptor/planning behavior; and `PythonReleaseProvider` is preview/detect-only until T-0247 adds read-only metadata/backend/command previews. `hadara release dry-run --json` includes additive `providerCapabilities` without executing Python builds, pip/twine smoke, PyPI token loading, publish, GitHub Release creation, Docker build, registry mutation, or release mutation.
- Python release preview provider exists: `pyproject.toml` is parsed read-only for PEP 621 `[project]` and Poetry `[tool.poetry]` static name/version metadata; build backend preview reports `setuptools`, `poetry`, `hatch`, `flit`, or `unknown`; and Python descriptors list planned commands `python -m build`, `twine check`, and `pip install wheel` with `willExecute:false`. No Python command execution, PyPI token loading, publish, artifact mutation, or evidence attachment is performed by this preview.
- The Python `pyproject.toml` parser is intentionally lightweight and preview-only. It reads simple string and string-array values from `[project]`, `[tool.poetry]`, and `[build-system]` for advisory detection only; before Python release readiness, artifact gates, or publish behavior depend on this data, replace or wrap it with a formal TOML parser and stricter schema validation.
- Python package smoke dry-run/local mode exists behind explicit `hadara package smoke --provider python`. Dry-run emits schema-valid planned `python-build`, `twine-check`, and `pip-install-wheel` steps with all execution/publish flags false. Local mode can run those steps through the existing reduced package-smoke runner boundary in a disposable workspace, recording temporary artifacts/install tree metadata without PyPI token loading, publish, MCP execution, public raw logs, package contents, or private paths.
- T-0249 Python package smoke boundary hardening exists: package-smoke reports now include additive `networkPolicy` metadata. Default mode is `environment-inherited` with `enforced:false`; `--network-policy offline` is Python best-effort only, adding `python -m build --no-isolation` and pip `--no-index --no-deps` planning/execution flags without OS-level network isolation. Explicit Python local smoke can attach reduced public package-smoke evidence, and release dry-run keeps the current npm package-smoke proof npm-compatible so Python evidence remains advisory and cannot satisfy the npm release gate.
- Final publish/deploy readiness exists as approval-gated `hadara release publish --mode dry-run|execute --json`; it emits schema-valid `hadara.releasePublish.v1`, checks release dry-run readiness, release-candidate package metadata, approval metadata, and token presence by token name only, privately audits execute-mode requests, and blocks before GitHub Release creation, Docker image build, artifact upload, GitHub API calls, or MCP release execution. T-0143 used an explicit manual npm-only helper path outside MCP to publish `hadara@0.1.0-rc.0`; T-0144 aligned the root README with npm RC install/use and source-checkout development while keeping GitHub Release, installer scripts, USB launchers, Docker image publishing, and live release/install automation deferred.
- Release/install/package-smoke sequencing is now reflected in tracked planning docs (`docs/DEVELOPMENT_SLICES.md`, `docs/V1_0_CAPSULE_BACKLOG.md`, and `docs/TEST_STRATEGY.md`) for T-0126 through final publish/deploy script work: package-smoke command surface, package metadata, install scripts, POSIX/Windows/USB launcher paths, install matrix smoke, evidence-backed release gates, release artifacts, and explicit publish/deploy approval. A local-only ignored supporting plan may exist at `docs/specs/HADARA_Release_Install_Package_Smoke_Capsule_Plan.md` for workspace agents, but it is intentionally not committed to GitHub.
- A deterministic dogfooding E2E fixture exists for a HADARA-on-HADARA workflow: the primary fixture starts from in-memory context export, creates and completes a temporary Task Capsule, checks policy for `npm run check` without shell execution, attaches public evidence, updates handoff, and proves done-level harness validation while avoiding provider calls, MCP writes, release/package execution, and remote CI. Its built CLI smoke separately verifies generated context export JSON surface compatibility through `hermes export-context --json`, which may return `.hadara/context/HADARA_CONTEXT.md`.
- Shell policy internals now have focused tokenizer, exact safe command preset, command-risk, and permission-matrix modules while preserving the existing `src/policy/policy.ts` compatibility surface. Release-risk commands are denied outside release mode and require explicit approval in release mode; auto/trusted network-risk commands require approval.
- Strict `hadara release gate --mode strict --json` failures set process exit code 6 when the release-gate report is not ok.
- Policy matrix feedback verification confirms `npm publish` is denied in auto/trusted modes, asks in release mode, auto/trusted network commands ask with high risk, and strict release-gate failures exit 6.
- Structured event records exist as `hadara.event.v1`; private audit JSONL writes now keep existing compatibility fields and include a nested redacted structured event for future audit/debug read surfaces, and event helpers expose optional schema assertion.
- Provider preparation helpers exist for schema-backed provider config references and provider call reports; they deny unknown runtime config input fields, assert registered schemas before returning, and do not load secrets, call provider SDKs, perform network calls, or create provider execution surfaces.
- CLI write-boundary preflight exists as `hadara write preflight <command...> --json` with schema `hadara.write.preflight.v1`; it reports expected project/private-portable write paths for task create, evidence collect, handoff update, planned run-state writes, and planned debt writes without executing the target command.
- TUI read-model aggregation exists as an internal TypeScript service in `src/tui/read-model.ts`; it composes existing shared read models for terminal rendering without adding writes, shell execution, provider calls, or MCP calls.
- TUI snapshot rendering exists as an internal TypeScript service in `src/tui/snapshot.ts`; it renders Overview, Tasks, Detail, and Help panels as deterministic no-color fixed-size text snapshots without interactive input, cache writes, shell execution, provider calls, MCP calls, or Task Capsule mutation.
- TUI mockup-parity module splitting has started: `src/tui/constants.ts`, `src/tui/layout.ts`, and `src/tui/markdown.ts` carry panel/document metadata, fixed-width terminal layout primitives, and Markdown document rendering from `.mockup/tui/app.js`, while `src/tui/snapshot.ts` now renders a mockup-style read-only HADARA Work Console frame.
- TUI snapshot rendering is deterministic by default: rendered text hides volatile `generatedAt` unless `includeGeneratedAt` is requested, default mockup width policy clamps to 78x24, compact mode is explicit, and Markdown wrapping/table layout uses visible terminal width for Korean/wide-character text.
- TUI interactive state exists as an internal pure TypeScript module in `src/tui/state.ts`; it supports panel switching, task selection/search, detail document tab selection, local scroll offsets, refresh/detail refresh requests and completion signals, quit requests, and renderer/read-model option mapping without terminal raw mode, CLI entry point, cache writes, shell execution, provider calls, MCP calls, or Task Capsule mutation.
- TUI raw terminal shell exists as an internal TypeScript module in `src/tui/terminal.ts`; it supports decoded keyboard input, injected input/output streams, redraws through the snapshot renderer, refresh/detail-refresh effect handling, opt-in cache-backed read-model loading, and clean shutdown/raw-mode restoration without shell execution, provider calls, MCP calls, evidence writes, handoff updates, or Task Capsule mutation.
- TUI local cache exists as an internal TypeScript service in `src/tui/cache.ts`; it writes `hadara.tui.cache.v1` records only under `.hadara/local/tui/read-model-cache.json`, records source signals for Task Board, `tasks/`, handoff, active-run local state, selected task, and selected evidence, indexes task summaries with mtime/size/SHA-256 invalidation data, supports full/fast/detail/none refresh modes, keeps selected-detail refresh scoped to the selected cached capsule, reuses unchanged task hashes during fast validation, disables cache when private evidence metadata is requested, and remains excluded from context export.
- A public read-only TUI CLI entry point exists as `hadara tui`, with `hadara tui --snapshot` for non-interactive smoke rendering and opt-in interactive cache acceleration through `hadara tui --cache`. Non-TTY interactive runs are refused with guidance to use snapshot mode; default TUI, `--no-cache`, and snapshot mode remain cache-free, and the command introduces no task/evidence/handoff mutation, shell execution, provider calls, MCP calls, dashboard/server behavior, or release/package behavior.
- TUI visual parity now includes explicit HADARA and high-contrast ANSI themes, deterministic no-color snapshot preservation, status/log line rendering, loading frames before full/detail refreshes, richer overview/task/detail rendering, and mockup-style keyboard ergonomics including Tab/Shift-Tab/Home/End/search completion/Korean keyboard quit. Task selection and search state now visibly drive rendered task rows, while TUI write actions, shell execution, provider calls, MCP calls, dashboard serving, and release behavior remain deferred.
- TUI frame and viewer parity has been refined after operator feedback: color themes now use the mockup's true-color RGB palette instead of 256-color approximations, badges/keycaps compose foreground and background colors correctly, detail viewer document content receives color-mode styling for headings, check markers, bullets, numbers, rules, and body text, keyboard document scroll state drives rendered document slices, and compact document tab labels match the mockup while no-color output remains deterministic.
- TUI cache fast validation now avoids unnecessary source-signal reads by reusing cached hashes when `mtimeMs` and `size` are unchanged for Task Board, handoff, active-run, selected task, and selected evidence signals; changed metadata still triggers file reads and SHA-256 recalculation.
- TUI loading and terminal ergonomics now open the raw terminal shell immediately on a loading read model before the first project read, render multi-tick loading frames for startup/full/detail refreshes, enable SGR mouse panel/task/doc-tab clicks, and redraw the current frame on terminal resize without adding writes, shell execution, provider calls, MCP calls, dashboard behavior, or release behavior.
- Production TUI responsiveness has a fast interactive read path: `TuiReadModelOptions.profile = 'fast'` loads task/status/active-run/selected-detail surfaces while deferring advisory debt, release-gate, tools, and write-preflight reads with `TUI_HEAVY_READS_DEFERRED`; terminal startup, refresh, and selected-detail loads use this profile. `hadara-cli-test` measurement against `/workspace` improved terminal startup/refresh/detail paths from the previous 18-24s range to about 2.8-3.1s, while full snapshot/read-model aggregation remains available for deterministic full reads.
- TUI mockup UX parity has been tightened: fast-profile deferred advisory reads are displayed as deferred instead of false zero/ok signals, task-list rendering consumes interaction scroll/search state, search-active copy shows the cursor, task rows visibly keep selection in-window, and SGR mouse task-row clicks open refreshed Detail without confusing wide task-table clicks for left navigation.
- TUI mouse targeting now uses renderer-derived hitboxes emitted by `src/tui/snapshot.ts` for panel navigation, task rows, and Detail document tabs; `src/tui/terminal.ts` consumes those hitboxes for SGR mouse clicks instead of duplicating fixed coordinate geometry and safely ignores SGR mouse release coordinate payloads. Task tab cursor/window movement follows the mockup visible-row offset policy, and Overview work cards now render at their actual column widths so intermediate wide terminal sizes keep Previous Work borders, label colors, and clean internal ellipsis behavior.
- TUI Markdown viewer and live Overview parity now more closely match the mockup: Detail document rendering supports heading underlines, numbered lists, checklists, bullets, and aligned Markdown tables with divider rows; `markdownPreview` is heading-aware; Overview Current Work and Previous Work follow the latest two task read-model rows with document-derived summaries; and the built interactive `hadara tui` path supports an asynchronous loading pulse with a worker-backed read-model loader when compiled worker files are available.
- TUI terminal input, Detail viewer scrolling, and narrow color clipping have been hardened after operator feedback: production input decoding now recognizes application-cursor and modifier arrow escape sequences in addition to basic arrows, Detail document scroll clamps at the renderer-derived bottom so repeated Down at EOF does not create hidden over-scroll, and ANSI-aware fitting preserves color escape sequences when colored TUI text is clipped in narrow terminal widths.
- TUI Overview and Tasks parity has been tightened after follow-up operator feedback: Tasks visible rows now derive from the same available-height policy as Detail and terminal page movement uses that same count; Overview Resume Signals is reduced to the mockup-style health/tasks plus validation lines; Current/Previous Work `Next`/`Proof` text follows the mockup fallback order over existing read-model data; and active Tasks search treats numeric `1`/`2`/`3`/`4` as query text instead of panel shortcuts until search exits.
- Evidence CLI handling lives in `src/cli/evidence.ts`.
- Policy CLI handling lives in `src/cli/policy.ts`.
- Hermes CLI handling lives in `src/cli/hermes.ts`; handoff CLI handling lives in `src/cli/handoff.ts`.
- Real provider adapters are not implemented; only schema-backed preparation contracts and safe report helpers exist.
- Dashboard is locally servable through a static CLI helper with read-only local API routes, live-renders `/api/status` first with fixture and inline fallback provenance, uses a Phase 5 operator-console layout, can show selected-task proof status from shared workbench/evidence semantic reports, and consumes a deterministic dashboard timeline read model.
- Production TUI public CLI entry is implemented as a read-only local work console through `hadara tui` and `hadara tui --snapshot`; optional TUI cache persistence is local-only under `.hadara/local/tui/`; interactive production loading uses an asynchronous pulse while read models load; TUI task/evidence/handoff writes, shell execution, provider calls, MCP calls, dashboard/server behavior, and release/package behavior remain deferred.
- Broad MCP write tools are not implemented beyond the explicitly enabled, approval-recorded, audited evidence attach tool.
- Operational debt records remain static and non-persisted; debt mutation and executable release automation remain deferred.

## Single Source of Truth

- Current state: `docs/PROJECT_STATE.md`
- Work queue: `docs/TASK_BOARD.md`
- Next-session handoff: `docs/AGENT_HANDOFF.md`
- Task details: `tasks/T-*/`
