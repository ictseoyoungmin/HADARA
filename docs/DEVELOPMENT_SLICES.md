# DEVELOPMENT_SLICES

HADARA development should proceed in small, evidence-backed slices. Each slice must live in a Task Capsule, run meaningful validation, update `EVIDENCE.md` and `evidence.jsonl`, and refresh `docs/AGENT_HANDOFF.md` before stopping.

| Order | Slice | Capsule | Purpose | Done Evidence |
|---|---|---|---|---|
| 1 | Harness validate JSON | T-0009 | Stabilize `hadara harness validate --task <id> --json` for external agents and CI. | Build/tests pass, CLI JSON sample recorded. |
| 2 | Harness replay skeleton | T-0010 | Add deterministic replay schema and ScriptedProvider-backed no-op scenario checks. | Done: 7 test files passed, 34 tests passed, replay JSON smoke recorded. |
| 3 | CLI JSON normalization | T-0011, T-0012, T-0013, T-0014, T-0015 | Apply common JSON envelope and exit codes to core CLI commands. | Done: doctor, task list/show, policy, Hermes, and evidence collect JSON. |
| 4 | Evidence artifact handling | T-0016 | Copy public artifacts into managed task/session evidence storage without leaking private paths. | Done: public artifact copy tests and private path suppression checks. |
| 5 | Policy execution preflight | T-0017 | Connect policy evaluator to ShellTool design before shell execution exists. | Done: allowed, approval-required, and denied preflight tests/smokes. |
| 6 | Provider fallback executor | T-0018 | Add fallback orchestration on top of the existing ProviderClient contract. | Done: contract tests with MockProvider/ScriptedProvider. |
| 7 | Shell preflight harness | T-0019 | Add deterministic fake shell observations gated by policy preflight before real shell execution. | Done: Docker check and fake shell policy gate tests. |
| 8 | Task Capsule format validation | T-0020 | Enforce Markdown format markers so handoff/context compression cannot silently drift capsule structure. | Done: Docker check and format drift regression tests. |
| 9 | Agent loop minimal harness | T-0021 | Implement a bounded deterministic loop using ScriptedProvider responses and fake shell observations first. | Done: Docker check, CLI run smoke, evidence index validation, and T-0021 harness validation. |
| 10 | Workspace file boundary | T-0023 | Ensure CLI file inputs are confined to project-root realpaths before broader tool/file surfaces grow. | Done: Docker check passed, 18 test files passed, 74 tests passed, T-0023 harness validation passed. |
| 11 | Evidence artifact redaction | T-0024 | Prevent public committed artifacts from carrying secrets or binary private data. | Done: Docker check passed, 18 test files passed, 78 tests passed, T-0024 harness validation passed. |
| 12 | CLI args parser | T-0025 | Split growing option parsing into strict reusable helpers. | Done: Docker check passed, 19 test files passed, 84 tests passed, T-0025 harness validation passed. |
| 13 | Agent loop evidence attachment | T-0026 | Attach deterministic fake-shell loop outputs as evidence without real shell execution. | Done: Docker check passed, T-0026 harness validation passed, run JSON smoke recorded evidence metadata. |
| 14 | Deterministic ScriptedProvider and Capsule Evidence Index | T-0027 | Ensure deterministic scripts are consumed in order and new capsules include required evidence indexes. | Done: Docker check passed, T-0027 harness validation passed, task-create smoke confirmed empty `evidence.jsonl`. |
| 15 | Init profiles protocol docs | T-0028 | Improve first-use HADARA initialization with protocol docs and profile selection. | Done: Docker check passed, T-0028 harness validation passed, full-profile Hermes export smoke passed. |
| 16 | Done-level harness validation | T-0029 | Add completion gates beyond structural Task Capsule validation. | Done: Docker check passed, done-level harness validation passed. |
| 17 | Run scenario scaffold | T-0030 | Generate deterministic run script and fake-shell fixture files for demo/task workflows. | Done: Docker check passed, built CLI scaffold/run smoke returned `ok: true`. |
| 18 | CLI handler extraction | T-0031 | Reduce `src/cli/main.ts` density by extracting cohesive command helpers. | Done: Docker check passed, init/run scaffold smoke passed, `main.ts` reduced from 667 to 459 LOC. |
| 19 | CLI harness handler extraction | T-0032 | Continue CLI dispatcher reduction by extracting harness validate/replay handling. | Done: Docker check passed, harness validate/replay smokes passed, `main.ts` reduced from 459 to 419 LOC. |
| 20 | CLI evidence handler extraction | T-0033 | Continue CLI dispatcher reduction by extracting evidence collect handling. | Done: Docker check passed, evidence collect JSON/text smokes passed, `main.ts` reduced from 419 to 387 LOC. |
| 21 | CLI policy handler extraction | T-0034 | Continue CLI dispatcher reduction by extracting policy check/preflight handling. | Done: Docker check passed, policy check/preflight JSON/text smokes passed, `main.ts` reduced from 387 to 358 LOC. |
| 22 | CLI Hermes and handoff handler extraction | T-0035 | Continue CLI dispatcher reduction by extracting Hermes and handoff handling. | Done: Docker check passed, Hermes JSON/text and handoff smokes passed, `main.ts` reduced from 358 to 332 LOC. |
| 23 | CLI remaining handler extraction | T-0036 | Finish current CLI dispatcher extraction pass by moving init, doctor, task, mcp, and run handling. | Done: Docker check passed, remaining command smokes passed, `main.ts` reduced from 332 to 115 LOC. |
| 24 | Runtime validation and harness semantics | T-0037 | Harden reviewed runtime validation gaps before expanding external bridge surfaces. | Done: Docker check passed, hardening smokes passed, 105 tests passed. |
| 25 | CLI JSON error envelope | T-0038 | Return stable JSON envelopes for early CLI parse and validation failures. | Done: Docker check passed, JSON error smokes passed, 108 tests passed. |
| 26 | Policy safe command exactness | T-0039 | Prevent safe command prefix matches from being classified as safe. | Done: Docker check passed, exactness smoke passed, 109 tests passed. |
| 27 | Handoff compaction policy | T-0040 | Keep project handoff current by moving old history into dedicated history docs. | Done: Docker check passed, compact handoff and history docs added. |
| 28 | Old Draft task reclassification | T-0041 | Mark overtaken early Draft tasks so external agents follow the current roadmap instead. | Done: T-0003 Superseded, T-0006 Partial, validation passed. |
| 29 | Hermes/MCP read-only contract | T-0042 | Define CLI JSON and read-only MCP bridge contracts before server implementation. | Done: contract docs added, Hermes guidance updated, validation passed. |
| 30 | MCP JSON-RPC server skeleton | T-0043 | Add stdio MCP server skeleton with no write tools, shell execution, provider calls, or file mutation. | Done: Docker check passed, 23 test files passed, 115 tests passed, built CLI initialize smoke returned read-only metadata. |
| 31 | MCP read tools implementation | T-0044 | Implement read-only tools from `docs/MCP_BRIDGE_CONTRACT.md`. | Done: Docker check passed, 24 test files passed, 122 tests passed, built CLI tools/call smoke returned MCP JSON text payload. |
| 32 | MCP bridge harness tests | T-0045 | Validate MCP tool outputs against existing CLI JSON contracts. | Done: Docker check passed, 25 test files passed, 128 tests passed, MCP bridge contract tests added. |
| 33 | Evidence attach tool contract | T-0046 | Define a future write-capable evidence attach contract, still without shell execution. | Done: contract doc added, policy/evidence safety gates documented, Docker check passed. |
| 34 | Evidence attach guard tests | T-0047 | Ensure future evidence attach contract does not accidentally advertise write-capable MCP behavior before implementation. | Done: Docker check passed, 26 test files passed, 131 tests passed, evidence attach remains unadvertised and unimplemented. |
| 35 | Gated MCP evidence attach implementation | T-0048 | Implement `hadara.evidence.attach` behind explicit operator opt-in while preserving default read-only MCP behavior. | Done: Docker check passed, 26 test files passed, 132 tests passed, opt-in tools/list smoke advertised evidence attach. |
| 36 | MCP evidence attach safety tests | T-0049 | Validate workspace boundary, public artifact redaction, and evidence JSON parity for opt-in evidence attach. | Done: Docker check passed, 27 test files passed, 137 tests passed, safety contract tests added. |
| 37 | MCP write audit log | T-0050 | Add private portable audit records for opt-in MCP evidence attach write attempts. | Done: Docker check passed, 27 test files passed, 142 tests passed, done-level harness validation passed. |
| 38 | MCP phase/mode config | T-0051 | Keep MCP initialize metadata aligned with default read-only and evidence attach-enabled modes. | Done: Docker check passed, 27 test files passed, 142 tests passed, done-level harness validation passed. |
| 39 | MCP evidence attach approval record | T-0052 | Require per-call approval actor and reason for opt-in MCP evidence attach writes. | Done: Docker check passed, 27 test files passed, 142 tests passed, done-level harness validation passed. |
| 40 | Operations Status JSON | T-0053 | Provide a single dashboard/external-agent JSON snapshot before implementing UI. | Done: Docker check passed, status JSON tests and CLI smokes passed, done-level harness validation passed. |
| 41 | Operations Status JSON cleanup | T-0054 | Harden status JSON degradation signals and stable dashboard-facing task count fields. | Done: Docker check passed, 28 test files passed, 147 tests passed, done-level harness validation passed. |
| 42 | Dashboard read model contract | T-0055 | Define dashboard consumption contract after Operations Status JSON exists. | Done: Docker check passed, 28 test files passed, 148 tests passed, sample fixture and field mapping added. |
| 43 | Minimal static dashboard | T-0056 | Build a minimal static dashboard consuming sample status JSON. | Done: Docker check passed, 29 test files passed, 150 tests passed, static fixture/dashboard smoke added. |
| 44 | Dashboard mockup adoption | T-0057 | Promote the comfort dark mockup to the dashboard visual baseline while preserving `hadara.ops.status.v1` data binding. | Done: Docker check passed, 29 test files passed, 152 tests passed, static mockup shell tests added. |
| 45 | Dashboard fixture binding smoke | T-0058 | Verify the adopted mockup shell renders fixture fields and fallback data without drift. | Done: Docker check passed, 29 test files passed, 155 tests passed, static data-field binding smoke added. |
| 46 | Dashboard served from HADARA CLI | T-0059 | Serve the static dashboard through a HADARA CLI entry point. | Done: Docker check passed, 29 test files passed, 155 tests passed, `hadara dashboard serve` static asset helper added. |
| 47 | Dashboard serve boundary hardening | T-0060 | Harden the static dashboard serving boundary before live dashboard integration. | Done: Docker check passed, 29 test files passed, 155 tests passed, method/header/traversal regressions added. |
| 48 | Evidence index schema hardening | T-0061 | Prevent manual evidence records from drifting away from `hadara.evidence.v1`. | Done: Docker check passed, 29 test files passed, 158 tests passed, required-field regressions added. |
| 49 | Dashboard server failure semantics | T-0062 | Make static dashboard server failures predictable before live dashboard work. | Done: Docker check passed, 29 test files passed, 158 tests passed, missing-root and internal-error regressions added. |
| 50 | Architecture/roadmap state reconciliation | T-0063 | Align high-level docs with current implemented, partial, and deferred capabilities. | Done: architecture and roadmap docs updated; build and done-level validation passed. |
| 51 | Roadmap v0.3 Operations Layer Freeze | T-0064 | Freeze v0.3 as a read-only operations layer before product-wide v1.0 expansion. | Done: ROADMAP and DEVELOPMENT_SLICES updated; exclusions explicit; validation passed. |
| 52 | Context Export MCP Instructions | T-0065 | Ensure exported context instructs external agents to use HADARA CLI/MCP surfaces first. | Done: context export source docs and instruction regression tests pass. |
| 53 | Compatibility Fixture | T-0066 | Prove Hermes-like external agent read-only workflow against HADARA MCP/CLI JSON. | Done: fixture replay contract test passes and write/execution-like MCP tools remain unavailable. |
| 54 | CLI/MCP Service Parity Refactor | T-0067 | Move duplicated CLI/MCP read logic into shared services/read models. | Done: project/handoff read services are shared and CLI/MCP parity tests pass. |
| 55 | Single Active Run State | T-0068 | Track active task/run without queue or multi-agent assumptions. | Done: active run manifest, stale handoff detection, status projection, and resume projection tests pass. |
| 56 | Operational Debt Track | T-0069 | Convert known_issue.log into structured product/development debt track. | Done: debt schema, capsule-size indicators, and premature acceptance checks pass. |
| 57 | Operations State Robustness Fix | T-0070 | Harden local operations state and debt checks before provider preparation. | Done: active-run corruption, missing task, premature acceptance, and section extraction regressions pass. |
| 58 | Core v1.0 Technical Plan Refresh | T-0072 | Align v1.0 planning docs with T-0066 through T-0070 implementation/design mismatch notes. | Done: technical plan, schema notes, backlog, board, state, and handoff updated; validation passed. |
| 59 | Redaction Hardening | T-0074 | Promote public evidence redaction from simple regex checks to a registry/report model. | Done: redaction registry/report tests cover AWS, GitHub, JWT, private key, npm, capture-group preservation, and no-capture replacement cases. |
| 60 | Redaction Policy Follow-up | T-0075 | Separate redaction scan/report output from public evidence policy blocking and settle near-term MCP planning names. | Done: threshold helper, public artifact policy update, planning docs, and Docker checks passed. |
| 61 | Evidence List Read Model | T-0076 | Add stable CLI/MCP/dashboard evidence list read models before timeline/dashboard integration. | Done: shared `hadara.evidence.list.v1` report builder, CLI JSON, read-only MCP tool, and malformed JSONL degraded-read tests pass. |
| 62 | Context Export MCP Read Tool | T-0077 | Add read-only MCP context export as memory payload without writing generated context files. | Done: `hadara.context.export` MCP tests pass, memory report returns `contextPath: null`, and file mutation is absent. |
| 63 | Tools List Read Model | T-0078 | Expose current CLI/MCP capabilities and disabled surfaces to external agents. | Done: neutral capability registry, shared `hadara.tools.list.v1`, fuller CLI surface discovery, read-only MCP `hadara.tools.list`, availability/risk metadata, opt-in evidence attach status, and disabled shell/provider/release/broad-write surfaces pass Docker validation. |
| 64 | Schema Layer Planning | T-0079 | Define the schema registry and first JSON schema fixtures before broad validation gates. | Done: `docs/SCHEMAS.md`, schema index fixture, and first JSON Schema fixtures for evidence list, context export, and tools list pass Docker validation. |
| 65 | Service Parity Expansion | T-0080, T-0081, T-0084, T-0085 | Expand shared services/read models beyond project/handoff into task, evidence, policy, harness, context, and status. | Done: task, policy, harness validate, context export, evidence/tools read models, and Operations Status JSON now live behind shared service/read-model boundaries. |
| 65a | Read-model Cleanup Follow-up Notes | T-0082 | Capture cleanup gaps before release gates: redaction policy observability, schema strictness levels, task.read evidence normalization, and PolicyService authorization boundaries. | Done: cleanup notes captured in docs and policy check mode input matches policy evaluate string/default behavior. |
| 65b | Task Read Evidence Normalization | T-0083 | Reuse evidence-list normalization for task.read embedded evidence data. | Done: task.read evidenceIndex and files["evidence.jsonl"] use normalized evidence records; focused task/evidence/MCP tests pass. |
| 65c | Harness Validate Service Parity | T-0084 | Route harness validate report generation through a shared service boundary and tighten task.read private evidence defaults. | Done: CLI/MCP harness validate use `createHarnessValidateReport`; task.read private evidence is opt-in via `includePrivate`; focused tests pass. |
| 65d | Operations Status Service Parity | T-0085 | Route Operations Status JSON report generation through a shared service boundary. | Done: CLI status commands use `createOpsStatusReport` from `src/services/operations-status-service.ts`; focused status tests and full check pass. |
| 66 | Active Run Read Surfaces | T-0086 | Formalize read-only run-state CLI and MCP active-run/resume tools while deferring run-state writes. | Done: `run-state show/resume --json`, `hadara.active.run.read`, and `hadara.active.run.resume` tests pass; active-run writes remain deferred. |
| 66a | Active Run Resume Hardening | T-0088 | Harden read-only active-run resume guidance and schema fixtures after review. | Done: canonical Task Capsule path resolution, `ACTIVE_RUN_CAPSULE_MISMATCH` warnings, active-run schema fixtures, and read-only resume wording pass validation. |
| 66b | Active Run Runtime Schema Validation | T-0092, T-0094 | Add runtime schema validation for mutable active-run projection/resume read models before stricter release gates depend on them. | Done: active-run projection/resume reports pass runtime schema validation, malformed local-state degraded reports remain valid, report schema failures use a distinct warning code, and Docker checks pass. |
| 67 | Operational Debt Release Gates | T-0087 | Promote operational debt into read surfaces and release-gate modes. | Done: `hadara debt list/show --json`, read-only MCP `hadara.debt.list/show`, ops debt aggregates, advisory warning checks, and strict high-open debt blocking reports pass. |
| 67a | Redaction Policy Observability Tests | T-0089 | Cover safe redaction diagnostics for public evidence artifact policy before broader evidence/security inspection surfaces. | Done: safe artifact policy report helper covers pattern/severity/count/byte metadata, medium diagnostics do not block, and high/critical findings still block. |
| 68 | Policy Matrix Refactor | T-0090, T-0093 | Split tokenizer, command risk, permission matrix, presets, and preflight policy. | Done: focused policy tests and full Docker check pass with matrix regressions for read/test/build/write/network/destructive/release, release-risk blocking, network approval, strict release-gate exit codes, and T-0093 feedback verification. |
| 69 | Private Evidence Manifest | T-0091, T-0094 | Track private evidence metadata, hashes, retention, and context-export exclusion. | Done: private evidence manifests record hashes, retention, deferred encryption metadata, and audit events while committed files and context export exclude private content and paths; raw source copying is project-boundary-limited by default. |
| 70 | Logger and Audit Event Model | T-0095 | Add structured event schema and clarify stdout/stderr/audit/debug log boundaries. | Done: `hadara.event.v1` helpers and schema fixture exist, audit JSONL writes preserve compatibility fields while embedding structured events, and Docker checks pass. |
| 71 | Real Provider Adapter Preparation | T-0096 | Prepare provider schema and adapter contract only after the operations layer freeze and service/read-model parity work. | Done: provider config/call schema fixtures and helper APIs reject stored secrets, deny unknown config input fields, assert schema-valid outputs, omit prompt/response content, and pass Docker validation without real provider execution. |
| 72 | Dashboard Read Integration | T-0097 | Serve local read APIs for status/tasks/evidence/active-run/debt without shell or write behavior. | Done: dashboard route tests pass for `/api/status`, `/api/tasks`, `/api/evidence?taskId=<task-id>`, `/api/active-run`, and `/api/debt`; full Docker check passes. |
| 73 | CLI Write Boundary Preflight | T-0098 | Add expected-write preflight reports for CLI-owned write commands. | Done: `hadara.write.preflight.v1`, `hadara write preflight <command...> --json`, and focused tests list files before task/evidence/handoff/run-state/debt writes without executing target writes. |
| 73a | TUI Design Alignment | T-0099 | Incorporate `.mockup/tui` and `.mockup/tui-final` learnings into main docs before production TUI work. | Done: TUI design notes, architecture, roadmap, backlog/schema notes, and state docs consistently describe a read-only terminal work console without production implementation. |
| 73b | TUI Read-Model Aggregator | T-0100 | Implement the first integrated terminal UI service over shared read models without renderer, write, shell, provider, MCP, or release behavior. | Done: internal aggregate composes status/tasks/detail/evidence/active-run/debt/release/tools/write-preflight preview, focused tests pass, and no-write boundary is covered. |
| 73c | Task Board Append Done-Level Guard | T-0101 | Prevent completed capsules from leaving duplicate or stale `docs/TASK_BOARD.md` rows. | Done: done-level harness validation checks exactly one row, Done status, and capsule path for the validated task. |
| 73d | TUI Snapshot Renderer | T-0102 | Render Overview/Tasks/Detail/Help from the TUI aggregate model without interactive input or write behavior. | Done: deterministic no-color fixed-size snapshots pass for all panels, narrow clipping, and no-write behavior. |
| 73e | TUI Mockup Parity Module Port | T-0103 | Split `.mockup/tui/app.js` rendering concepts into TypeScript constants, layout, Markdown, and snapshot modules before interactive runtime work. | Done: mockup-style Work Console snapshots, document tabs, layout helpers, and Markdown renderer pass focused and full Docker validation. |
| 73f | TUI Snapshot Polish | T-0104 | Harden deterministic snapshot output, width policy, and wide-character Markdown rendering before interactive state work. | Done: generatedAt is hidden by default, compact mode is explicit, Korean snapshot/Markdown regressions pass, and full Docker validation passes. |
| 73g | TUI Interactive State | T-0105 | Add keyboard/search/refresh state transitions over the snapshot-ready renderer without writes or shell/provider/MCP behavior. | Done: pure state-transition tests cover panels, task selection/search, document tabs, scroll, refresh request/complete, quit, and no-write behavior; full Docker validation passes. |
| 73h | TUI Raw Terminal Shell | T-0106 | Add local terminal key decoding, redraw loop, clean shutdown, and optional mouse over the tested state/renderer without write behavior. | Done: internal injected-stream terminal shell covers key decoding, redraw, refresh/detail-refresh effects, raw-mode restoration, quit handling, and no-write boundary tests before any public CLI entry point is added. |
| 73i | TUI Public CLI Entry Point | T-0107 | Expose the read-only terminal work console through `hadara tui` while keeping smoke rendering available for non-interactive validation. | Done: `hadara tui` routes to the internal terminal shell for TTY sessions, `hadara tui --snapshot` renders a read-only frame, capability discovery marks it read-only, focused tests and full Docker validation pass. |
| 73j | TUI Native Runtime Docs Assimilation | T-0108 | Reflect the full TUI mockup parity/native runtime design into main v1.0 planning docs without omission. | Done: `docs/V1_0_IMPLEMENTATION_SCHEMAS.md` preserves the unabridged design body, and backlog/schema/test docs carry the follow-up capsule, schema posture, and validation requirements. |
| 73k | TUI Local Cache and Incremental Refresh | T-0109 | Add local TUI cache acceleration under `.hadara/local/tui/` with task index invalidation and opt-in CLI use. | Done: internal `hadara.tui.cache.v1` cache records, source-signal invalidation, full/fast/detail/none refresh modes, opt-in `hadara tui --cache`, private-evidence cache disable, context-export exclusion, focused tests, TypeScript build, and 1000-capsule benchmark pass. |
| 73l | TUI Visual Parity and Loading States | T-0110 | Apply mockup visual feel, themes, loading frames, and keyboard ergonomics to the read-only production TUI. | Done: HADARA/contrast themes, no-color default snapshots, status/log line, loading frames, card/task/detail polish, Tab/Shift-Tab/Home/End/search completion/Korean quit handling, focused tests, and full Docker validation pass. |
| 73m | TUI Frame Color and Viewer Parity Fix | T-0111 | Refine TUI visual parity after operator feedback on frame line color and legacy-feeling document viewer. | Done: theme output now uses mockup true-color RGB, badge/keycap foreground/background composition is fixed, detail viewer lines are colorized, keyboard document scrolling renders long docs, compact mockup tab labels are applied, cache source-signal fast validation reuses unchanged hashes after mtime/size checks, no-color output remains stable, focused tests and full Docker validation pass. |
| 73n | TUI Loading Animation Mouse and Resize Ergonomics | T-0112 | Make TUI open immediately with loading frames and add basic mouse/resize ergonomics without writes. | Done: startup/loading frames render before initial/full/detail loads, SGR mouse panel/task/doc-tab clicks work, resize redraws current frame, focused tests and full Docker validation pass. |
| 73o | TUI Async Loading and Read-Model Performance Refactor | T-0113 | Fix the remaining production TUI responsiveness gap versus the mockup by moving expensive aggregate reads off startup/detail paths. | Done: fast TUI read-model profile defers debt/release/tools/write-preview advisory reads, interactive startup/refresh/detail use the fast profile, missing `TASK.md` task cache entries degrade to zero-size index records, focused/full Docker checks pass, and measured `/workspace` terminal paths improved from 18-24s to about 2.8-3.1s. |
| 73p | TUI Mockup UX Parity Audit Fix | T-0114 | Close high-impact mockup UX mismatches before deeper async/worker loading work. | Done: fast-profile deferred advisory reads render as deferred, task-list windows follow interaction scroll/search state, active search copy shows a cursor, task-row mouse clicks open refreshed Detail, wide table clicks no longer hit left nav, and focused/full Docker checks pass. |
| 73q | TUI Renderer-Derived Mouse Hitboxes and Detail Tab Fix | T-0115 | Make mouse targeting, Task cursor windowing, and intermediate-width Overview card rendering follow the mockup-derived TUI frame behavior. | Done: snapshot renderer emits internal panel/task/document hitboxes, terminal SGR mouse handling consumes those hitboxes, SGR release payload digits are ignored instead of becoming panel keys, Task tab cursor/window movement follows the mockup visible-row offset policy, Overview work cards keep Previous Work borders and label colors at intermediate wide widths, focused TUI tests pass, full Docker check passes, and the read-only boundary is unchanged. |
| 73r | TUI Markdown Viewer and Live Overview Parity | T-0116 | Make the production Detail Markdown viewer, Overview current/previous work summaries, and loading pulse match the mockup while keeping read-model-first boundaries. | Done: Markdown headings/lists/tables match mockup semantics, heading-aware previews feed Overview summaries, latest two task rows drive Current/Previous Work, built interactive TUI uses async loading pulse with worker-backed reads when available, focused TUI tests pass, full Docker check passes, and the read-only boundary is unchanged. |
| 73s | TUI Arrow Input and Narrow Color Clipping Fix | T-0117 | Fix terminal-specific arrow-key decoding, hidden Detail viewer over-scroll, and narrow-window color clipping after T-0116 operator feedback. | Done: production decoder supports basic, application-cursor, and modifier arrow sequences; Detail document scroll clamps at the rendered bottom; ANSI-aware fitting preserves color while clipping narrow text; focused input/layout/state/terminal tests pass; full Docker check passes; read-only boundary is unchanged. |
| 73t | TUI Tasks Height and Overview Copy Parity | T-0118 | Align Tasks height and Overview copy/search behavior with the mockup after follow-up operator feedback. | Done: Tasks visible rows derive from Detail's available-height policy, terminal page movement uses the same row count, Resume Signals is concise, Work-card Next/Proof fallback order matches the mockup, numeric panel keys can be typed while Tasks search is active, focused TUI tests pass, full Docker check passes, and the read-only boundary is unchanged. |
| 74 | Release and Packaging Track | T-0119 | Define install, CI, clean-checkout, and release-gate behavior. | Done: release gate reports package/bin/script, Node/CI, clean-checkout smoke, generated artifact policy, and operational debt checks; focused/full Docker checks and built CLI advisory/strict smokes pass. |
| 75 | Dogfooding E2E Fixture | T-0120 | Replay a HADARA-on-HADARA workflow from context export to done-level validation. | Done: deterministic fixture replays in-memory context export, policy check, evidence attachment, handoff update, and done-level harness validation. |
| 76 | Done-level Capsule Scaffold Guard | T-0121 | Prevent completed Task Capsules from retaining initial scaffold/default Markdown content. | Done: done-level harness validation rejects scaffold/default leftovers, draft-level validation remains structural, focused harness/dogfooding tests pass, and full Docker check passes. |
| 77 | Remote CI/Release Observation | T-0122 | Confirm remote CI behavior after local Docker validation and before v1.0 release gate freeze. | Done: GitHub Actions CI run #109 on `main` succeeded for commit `8b4f33d1bf926d051cf63e13ca2de222bfc22d8c`; release-gate readiness checks documented remote CI observation without calling GitHub. |
| 78 | Operational Debt High Severity Mitigation | T-0123 | Reduce the remaining high operational debt gate before treating strict release readiness as green. | Done: OD-0003 and OD-0008 are mitigated by existing protocol/done-level safeguards, debt aggregate `highOpen` is 0, and strict release gate passes when all documented readiness checks pass. |
| 79 | Clean Checkout Package Smoke Planning | T-0124 | Make clean-checkout package smoke expectations explicit before executable package-smoke work. | Done: TEST_STRATEGY documents the smoke sequence and non-execution boundaries; the read-only strict release gate requires those markers before passing the clean-checkout smoke check. |
| 80 | Executable Package Smoke Artifact Boundary Design | T-0125 | Define executable package-smoke workspace, artifact, redaction/audit, and evidence boundaries before implementation. | Done: TEST_STRATEGY documents the boundary; the read-only strict release gate reports `PACKAGE_SMOKE_ARTIFACT_BOUNDARY` as the check and maps missing markers to `PACKAGE_SMOKE_ARTIFACT_BOUNDARY_UNCLEAR` issues while still performing no package-smoke execution. |
| 81 | Package Smoke Command Surface Design | T-0126 | Define `hadara package smoke` command naming, flags, approval, cleanup, timeout, failure, and evidence semantics before implementation. | Done: TEST_STRATEGY documents the command surface; strict release gate reports `PACKAGE_SMOKE_COMMAND_SURFACE` and maps missing markers to `PACKAGE_SMOKE_COMMAND_SURFACE_UNCLEAR` while still performing no package-smoke execution. |
| 82 | Package Metadata Release Readiness | T-0127 | Decide package name, version policy, `private` transition, `files` whitelist target, license path, and installed `hadara` verification. | Done: TEST_STRATEGY records metadata decisions without publishing; strict release gate reports `PACKAGE_METADATA_RELEASE_READINESS` and maps missing markers to `PACKAGE_METADATA_RELEASE_READINESS_UNCLEAR`; T-0142 later performed the actual `private: false` and `files` transition. |
| 83 | Installer Script Surface and Schema | T-0128 | Define POSIX/Windows installer surfaces, portable launchers, default install locations, Node 22/WSL checks, and install report schemas. | Done: `docs/RELEASE_READINESS.md` records installer/launcher contracts, install locations, redacted install-plan path references, and execute-reserved policy; `hadara.install.plan.v1` schema fixture is registered; strict release gate reports `INSTALLER_SCRIPT_SURFACE_SCHEMA` without executing installers. |
| 84 | Installer Dry-run Implementation | T-0129 | Implement dry-run install planning for installer scripts and portable launchers, splitting POSIX/Windows/portable subcapsules if needed. | Done: `hadara install plan --json` emits schema-valid `hadara.install.plan.v1` dry-run reports with redacted public paths, Linux-style WSL defaults, explicit USB-root requirement, planned `wouldWrite` actions, execute-disabled behavior, and no install mutation. |
| 85 | Install Matrix Smoke Plan | T-0130 | Define Linux, Windows, WSL, npm package, and USB portable install matrix rows before execution. | Done: `docs/RELEASE_READINESS.md` and `docs/TEST_STRATEGY.md` define Linux source/package, WSL source, Windows source/package, USB Windows/WSL, and installed-CLI major-feature rows; strict release gate reports `INSTALL_MATRIX_SMOKE_PLAN` without executing install smoke. |
| 86 | Major Feature Smoke Runner | T-0131 | Implement a bounded core feature smoke runner with `core` and `release-readiness` profiles. | Done: `hadara smoke run --profile core --json` emits schema-valid `hadara.featureSmoke.v1` service/read-model reports for doctor, status, task list, tools list, TUI snapshot, and advisory release gate; it explicitly does not execute an installed binary, launcher, or package install, and `release-readiness` is reserved but deferred. |
| 87 | Package Smoke Schema and Fixture | T-0132 | Register and fixture `hadara.packageSmoke.v1` before package-smoke execution. | Done: schema/index/runtime registration and deterministic fixtures prove reduced package-smoke reports omit raw package contents, raw npm logs, env secrets, private paths, and private store paths while release gate remains read-only. |
| 88 | Package Smoke Dry-run Implementation | T-0133 | Implement `hadara package smoke --dry-run --json` without `npm pack`, install, artifact writes, or evidence attachment by default. | Done: dry-run reports preview source, disposable workspace, planned package/install/core-smoke/evidence steps, artifact metadata, and issues while keeping all execution markers false and public paths redacted. |
| 89 | Local Package Smoke Execution | T-0134 | Implement explicit local package smoke with `npm pack`, isolated prefix install, `hadara doctor --json`, core feature smoke, cleanup, and reduced report. | Done: `hadara package smoke --execute --json` requires explicit execution, runs npm pack plus isolated prefix install in a disposable workspace, invokes installed `hadara doctor --json` and installed core smoke, cleans up by default, emits reduced schema-valid reports, and avoids publish, release mutation, global install, Docker image build, GitHub Release, public raw logs, and default evidence attachment. |
| 90 | Clean Checkout Smoke Implementation | T-0135 | Implement clean-checkout source smoke in a disposable copy while keeping installed CLI smoke as a separate `hadara` command-form requirement. | Done: `hadara smoke clean-checkout --execute --json` copies source into a disposable checkout, runs `npm ci`, build, check, built CLI doctor/status/strict release gate, cleans up by default, emits `hadara.cleanCheckoutSmoke.v1`, and keeps source workspace unchanged with no package install, publish, release mutation, evidence attachment, or public raw logs. |
| 91 | Smoke Evidence Integration | T-0136 | Attach reduced package/install smoke evidence safely while keeping raw logs private or temporary. | Done: `--attach-evidence --task <task-id>` writes reduced public package-smoke and clean-checkout smoke summaries under task-local smoke artifact directories after redaction checks, without raw logs, package contents, private paths, release mutation, publish, or MCP smoke execution. |
| 92 | Release Artifact Builder | T-0137 | Build tarball/checksum/manifest and verify package contents without publishing. | Done: `hadara release artifact --execute --json` stages a whitelisted package, runs npm pack, generates SHA-256 checksum and manifest files in disposable or explicit output, verifies package contents, and emits `hadara.releaseArtifact.v1` without publish, GitHub Release, Docker image, MCP release execution, or public raw logs. |
| 93 | Release Gate Evidence Freeze | T-0138 | Move release gate from plan markers toward evidence-backed readiness while keeping it read-only. | Done: strict mode now requires existing public package-smoke, clean-checkout smoke, and release-artifact evidence records; advisory mode warns; install-matrix evidence enforcement remains deferred until execution exists. |
| 94 | CI/Release Workflow Target Decision | T-0139 | Decide primary and secondary release targets, recommended as npm package first and GitHub Release second, with Docker deferred. | Done: npm package is primary, GitHub Release with tarball/checksum/manifest is secondary, Docker is deferred, token names are documented without values, and publish/deploy remains approval-only. |
| 95 | Final Deployment Script Dry Run | T-0140 | Create dry-run release scripts that verify workspace, metadata, checks, smoke evidence, install matrix evidence, and artifact manifest. | Done: `hadara release dry-run --json` emits schema-valid reduced output, cross-checks linked public package-smoke, clean-checkout smoke, and release-artifact evidence artifacts, verifies release artifact package version and manifest hash, checks git commit metadata when present, and plans npm/GitHub/Docker targets without publish, GitHub Release creation, Docker build, registry mutation, token loading, or release mutation. |
| 96 | Final Publish/Deploy Script | T-0141 | Implement explicit approval-gated publish/deploy script after dry-run and evidence gates are stable. | Done: `hadara release publish --mode dry-run|execute --json` emits `hadara.releasePublish.v1`, checks release dry-run readiness, package metadata, approval metadata, and token presence by name only, privately audits execute requests, and blocks before publish/GitHub/Docker mutation. |
| 97 | Package Metadata Transition Plan | T-0142 | Move package metadata from bootstrap/private mode to the first release-candidate metadata state after publish-readiness dry-run boundaries exist. | Done: root metadata is `0.1.0-rc.0`, `private: false`, with `dist/`, `README.md`, `LICENSE`, and `package.json` in `files`; package-smoke, clean-checkout, release-artifact, release dry-run, and release publish dry-run evidence were regenerated; no publish/GitHub/Docker/registry mutation or MCP executable release surface was added. |
| 98 | Manual RC Npm Publish | T-0143 | Execute the first npm-only release-candidate publish after release-candidate metadata and evidence gates pass. | Done: `hadara@0.1.0-rc.0` is published to npm and registry verification returns the npm tarball URL; GitHub Release draft/tag push, Linux/WSL install scripts, Windows install scripts, USB install scripts, and README release/install cleanup are deferred to the next capsule. |
| 99 | README Release Install Docs Cleanup | T-0144 | Align root README with the published npm RC, installed CLI verification, source-checkout development, license, and current release/install boundaries. | Done: README documents npm RC install/npx usage, source checkout development, current CLI/planning surfaces, MIT license, and deferred installer/GitHub Release/USB/Docker boundaries without release mutation. |
| 100 | MCP Initialize Package Version Metadata | T-0145 | Keep MCP initialize server metadata aligned with current HADARA package metadata after release-candidate transition. | Done: MCP initialize reports `serverInfo.version` from package metadata, focused MCP tests and full Docker check pass, and read-only/evidence-attach metadata behavior is preserved. |
| 101 | Release Metadata Hardcoding Cleanup | T-0146 | Remove exact current-RC coupling from release readiness source checks before the next RC bump. | Done: release metadata readiness derives the current version marker from package metadata, package-smoke tarball examples are version-flexible, future RC regression coverage passes, and strict release gate remains green. |
| 102 | Init Scaffold Protocol Alignment | T-0147 | Align generated `hadara init` docs with general HADARA protocol and current scaffold boundaries. | Done: default init no longer creates Hermes files, generates `.gitignore`, emits structured general AGENTS/SOP/security/test docs, root SOP carries the same generalized structure standard, and focused/full validation passes. |
| 103 | Init Profile Scale Matrix Refactor | T-0148 | Replace misleading init profile names with general scale profiles and make generated SOP/AGENTS profile-aware. | Done: supported profiles are `basic`, `standard`, and `governed`; default standard generates medium planning/validation docs, governed adds security/refactor/roadmap docs, unsupported profile names are rejected, and focused/full validation passes. |
| 104 | Init Generated Markdown Table Frame Alignment | T-0149 | Apply the phase 1 init refactoring plan to generated markdown table frames, README entry surface, and generated `.gitignore` boundaries. | Done: generated docs assert canonical table headers, generic init docs avoid Hermes/MCP/dashboard/provider defaults, `.gitignore` omits top-level `data/`, README init profiles are current, focused/full Docker validation passes, and built CLI profile smokes pass. |
| 105 | Init Scaffold Doctor and Migration Guard | T-0150 | Detect older initialized projects with stale Hermes defaults, old profile names, missing `.gitignore`, broad `data/` ignore rules, or prose-only scaffold docs. | Done: read-only `hadara init doctor --json` reports stable scaffold issue codes without overwriting user files. |
| 106 | Init Lazy Runtime Store Creation | T-0150 | Avoid eager creation of local/private runtime-store directories during `hadara init`. | Done: init scaffold generation writes project docs and `.gitignore` without creating `.hadara/local/portable`; runtime stores remain lazily created by the commands that need them. |
| 107 | Init Profile Upgrade Command | T-0150 | Support `basic -> standard -> governed` scaffold expansion without overwriting user-edited docs. | Done: `hadara init upgrade --profile <profile> --json` plans missing docs by default; `--execute` creates only missing generated files with missing-file-only writes. |
| 108 | Required Reading Registration Command | T-0150 | Automate adding project-specific specs/contracts to the SOP Required Reading table after the table structure is stable. | Done: `hadara init register-doc --path <path> --when <text> --purpose <text> --json` dry-runs and `--execute` updates the Required Reading table idempotently. |
| 109 | Optional Integration Enable Commands | T-0150 | Add explicit enable flows for optional Hermes/MCP or other integration docs instead of making them init defaults. | Done: `hadara init enable-integration --integration hermes|mcp --json` dry-runs and `--execute` creates/registers integration docs deliberately while default `hadara init` remains generic. |
| 110 | Init Follow-up Hardening | T-0151 | Tighten T-0150 semantics after review: generated SOP wording, profile metadata merge, profile metadata drift, strict registration checks, safe table/path inputs, and non-runtime integration guidance. | Done: focused init tests cover stale wording removal, governed release wording removal, upgrade metadata merge, `INIT_PROFILE_METADATA_MISMATCH`, invalid paths/table cells, `--require-exists`, no partial integration writes, and rollback on multi-file commit failure. |
| 111 | Task Capsule Scaffold Frame Alignment | T-0152 | Start Phase 2 Project Protocol Consistency by modernizing newly generated Task Capsule files into table-first v2 frames. | Done: v2 scaffold frames implemented, legacy validation compatibility preserved, focused checks passed with 3 files / 50 tests, full check passed with 57 files / 421 tests, and done-level harness returned `ok: true`. |
| 112 | Task Capsule Consistency Doctor | T-0153 | Add a read-only per-capsule consistency report for missing files, status drift, pending acceptance at Done, evidence JSONL gaps, stale handoff, and placeholder drift. | Done: `hadara protocol doctor --task <id> --json` emits `hadara.protocol.consistency.v1`; focused tests passed with 2 files / 7 tests, full Docker check passed with 60 files / 429 tests, built CLI smoke returned `ok: true`, and done-level harness returned `ok: true`. |
| 113 | Project Docs Consistency Doctor MVP | T-0154 | Establish the docs-scope protocol doctor command and core project-doc drift checks. | Done: `hadara protocol doctor --scope docs --json` emits read-only `hadara.protocol.consistency.v1` reports for required project docs, SOP Required Reading missing paths, Task Board versus capsule drift, and latest-completed handoff drift; focused tests passed with 3 files / 13 tests, full Docker check passed with 60 files / 432 tests, built CLI smoke returned `ok: true`, and done-level harness returned `ok: true`. |
| 113a | Project Docs Consistency Doctor Completion | T-0155 | Complete the planned T-0154 project-doc coverage before moving to profile/remediation slices. | Done: docs-scope doctor now checks semantic handoff fields, Project State markers, Development Slices drift, Decisions evidence sanity, Test Strategy validation baseline, SOP scaffold structure, mixed profile doc-set diagnostics, and `--task`/`--scope` mutual exclusion; focused tests passed with 3 files / 15 tests, full Docker check passed with 60 files / 434 tests, built CLI smokes returned `ok: true` for docs/task reports, and done-level harness returned `ok: true`. |
| 114 | Profile Drift Remediation Guide | T-0156 | Detect profile doc-set drift and provide manual remediation hints without broad user-doc rewrites. | Done: `hadara protocol doctor --scope profile --json` emits `summary.profile` declared/detected/target/source plus manual remediation objects for doc-set, profile metadata, and Required Reading drift; focused tests passed with 2 files / 18 tests, full Docker check passed with 60 files / 440 tests, built CLI smokes passed, and safe-auto writes remain T-0157. |
| 115 | Safe Protocol Remediation MVP | T-0157 | Add dry-run-first bounded remediation for low-risk exact edits such as missing table frames, empty `evidence.jsonl`, profile row updates, and missing Task Board rows. | Done: `hadara protocol remediate --fix ... --json [--execute]` supports four allowlisted fixes; focused tests passed with 2 files / 13 tests, full Docker check passed with 61 files / 450 tests, and built CLI temp-fixture smoke verified dry-run no-write plus bounded execute writes. |
| 116 | Safe Protocol Remediation Hardening | T-0158 | Harden T-0157 remediation before schema consumers rely on it. | Done: execute writes now use temp-file/rename with rollback-attempt issue reporting, planned-content hash/existence conflict checks, section-bounded Project State Metadata profile upsert, and warning-and-skip guards for malformed Task Board or legacy Decisions tables; focused tests passed with 2 files / 18 tests, full Docker check passed with 61 files / 455 tests, and built CLI smoke passed after refreshing `/workspace/dist`. |
| 117 | Protocol Consistency JSON Contract | T-0159 | Register schema fixtures and contract tests for `hadara.protocol.consistency.v1` and protocol remediation reports. | Done: schema fixtures and registry entries for `hadara.protocol.consistency.v1` and `hadara.protocol.remediation.v1` exist; focused Docker checks passed with 4 files / 34 tests; full Docker check passed with 61 files / 455 tests; built CLI protocol doctor/remediation smokes passed after refreshing `/workspace/dist`. |
| 118 | Protocol Doctor All Scope | T-0160 | Align CLI/service behavior with the existing `hadara.protocol.consistency.v1` `scope: all` contract. | Done: `hadara protocol doctor --scope all --json` and default `hadara protocol doctor --json` emit all-scope reports aggregating docs, profile, and active-task detail; focused Docker tests passed with 2 files / 27 tests; full Docker check passed with 61 files / 459 tests; built CLI all-scope smokes passed after refreshing `/workspace/dist`. |
| 119 | Markdown Table Helper Extraction | T-0161 | Extract shared generated-table parsing/formatting helpers used by protocol consistency, profile diagnostics, harness validation, and remediation while preserving current issue codes and behavior. | Done: `src/services/markdown-table.ts` added, duplicated parsers removed from protocol/profile/harness code, focused Docker tests passed with 3 files / 34 tests, full Docker check passed with 62 files / 466 tests, and `/workspace/dist` was refreshed. |
| 120 | Doctor Remediation Hint Unification | T-0162 | Make protocol doctor reports expose additive remediation hints for safe-auto cases already supported by `protocol remediate --fix`. | Done: doctor issues now expose safe-auto `suggestedFix` hints and remediations for Project State profile metadata, missing Task Board rows, missing `evidence.jsonl`, and missing Decisions table frames; focused Docker tests passed with 3 files / 38 tests; full Docker check passed with 62 files / 467 tests; built CLI docs-scope smoke returned safe-auto hints. |
| 121 | Task Capsule Upgrade Scaffold Command | T-0163 | Add dry-run-first `hadara task upgrade-scaffold --task <id> --json [--execute]` for non-destructive insertion of missing v2 Task Capsule frame sections. | Done: `hadara.task.upgrade_scaffold.v1` fixture and command implemented; execute uses append/create-only writes with before-hash/existence checks and ambiguous-frame skips; focused Docker tests passed with 3 files / 11 tests; full Docker check passed with 63 files / 472 tests; built CLI dry-run smoke passed. |
| 122 | Protocol Surface Docs Alignment | T-0164 | Align CLI help, schema notes, and protocol docs with implemented `all` scope, fixture-level schema posture, and current `protocol remediate --fix` command surface. | Done: CLI help now lists default protocol doctor, docs/profile/all scope, and task upgrade-scaffold; README, CLI JSON contract, schema docs, schema index notes, and Phase 2 planning docs are aligned; full Docker check passed with 63 files / 472 tests; built CLI help smoke passed. |
| 123 | Evidence Lint and Doctor Validation | T-0165 | Catch hand-edited or drifted Task Capsule evidence before close and document the close validation/evidence fixed-point redesign. | Done: `hadara evidence lint --task <id> --json` emits `hadara.evidence.lint.v1`, task protocol doctor surfaces evidence lint errors, focused Docker checks passed with 3 files / 22 tests, and close/evidence loop rules are documented. |
| 124 | Task Close Plan Report | T-0166 | Add read-only `hadara task close --task <id> --json` with validation/evidence plan and loop-boundary `nextActions`. | Done: dry-run task close report emits `hadara.task.close.v1`, runs done validation/evidence lint/task doctor read-only checks, includes close evidence loop-boundary metadata and nextActions, and focused Docker checks passed with 3 files / 7 tests. |
| 125 | Task Close Execute MVP | T-0167 | Add `hadara task close --task <id> --execute --json` that appends close evidence through the canonical writer after blockers pass. | Done: execute mode appends only canonical `command-log` close evidence after validation/lint/doctor blockers pass; status, Task Board, Project State, and handoff automatic writes remain out of scope; focused Docker checks passed with 2 files / 4 tests. |
| 126 | Task Ready Preflight | T-0168 | Add friendly `hadara task ready --task <id> --level done --json` readiness report before close. | Done: read-only task ready reports reuse close preflight checks, expose friendly blocker summaries/check booleans/nextActions, register `hadara.task.ready.v1`, and focused Docker checks passed with 3 files / 6 tests. |
| 127 | Evidence Command UX | T-0169 | Add `hadara evidence add-command --task <id> ... --json` for command-log evidence without shell execution. | Done: `evidence add-command` forces `command-log`, uses the canonical evidence writer, performs no shell execution/capture, and focused Docker checks passed with 3 files / 19 tests. |
| 128 | Close UX Polish and Audit Semantics | T-0170 | Clarify close hash semantics, improve execute nextActions, record append result paths, and add read-only close audit. | Done: `task close` separates diagnostic report and close-relevant source hashes, execute success reports appended/audit actions, `task audit-close` emits `hadara.task.audit_close.v1`, and focused Docker checks passed with 3 files / 8 tests. |
| 129 | Task Workbench Status Report | T-0171 | Start Phase 3 with read-only `hadara task status --task <id> --json` over existing close, evidence, protocol, and Task Board sources. | Done: workbench service and CLI route added; focused Docker test passed with 1 file / 3 tests; full Docker check passed with 67 files / 486 tests; built CLI status smoke returned `hadara.task.workbench.v1`. |
| 130 | Workbench Suggested Action Engine | T-0172 | Centralize next-action generation across ready, close, evidence, protocol, remediation, and audit sources. | Done: `WorkbenchNextAction` service added with remediation/audit/action priority and dry-run/execute pairing; focused Docker tests passed with 2 files / 8 tests; full Docker check passed with 68 files / 491 tests. |
| 131 | Workbench Schema Contract | T-0173 | Register fixture-level `hadara.task.workbench.v1` schema and tighten audit-close/workbench contract coverage. | Done: workbench schema fixture, schema index/runtime registration, schema docs, and focused validation added; focused Docker tests passed with 3 files / 9 tests; full Docker check passed with 68 files / 491 tests. |
| 132 | Worker-Friendly Text Output | T-0174 | Make `task status` and `task audit-close` concise in non-JSON mode with State/Evidence/Protocol/Close/Suggested-next groups. | Done: status/audit text section tests passed; full Docker check passed with 68 files / 491 tests; built CLI status text smoke passed. |
| 133 | Dashboard TUI MCP Read Projection Prep | T-0175 | Prepare read-only future consumers to use the workbench projection without raw file parsing or writes. | Done: `docs/TASK_WORKBENCH_READ_MODEL_CONTRACT.md` added; SOP, dashboard contract, and MCP bridge contract aligned; full Docker check passed with 68 files / 491 tests. |
| 134 | Evidence From Command Design | T-0176 | Design high-risk shell-executing evidence capture separately from current non-executing command-log UX. | Done: `docs/EVIDENCE_FROM_COMMAND_DESIGN.md` added; SOP, security model, and CLI JSON contract clarify design-only status; full Docker check passed with 68 files / 491 tests. |
| 135 | Task Workbench Hardening | T-0177 | Harden Phase 3 workbench semantics after operator review. | Done: true Task Board projection, workbench drift issues, optional nextAction normalization, split close evidence state, contract docs, focused unit tests, full Docker check, and built CLI smoke passed. |
| 136 | Runtime Version / CLI Origin Doctor | T-0178 | Add read-only `hadara version --verbose --json` so operators can see the current CLI entry, project root, package/git/node metadata, and stale build signal. | Done: runtime report, schema registration, focused tests, full Docker check, and built CLI smoke passed. |
| 137 | Docker Dev Sync-Build Script | T-0179 | Provide a repo-level Docker sync/build/check helper so agents stop hand-assembling temp-copy, npm install/check, dist refresh, and smoke steps. | Done: `dev:docker-check` and `dev:docker-sync-build` scripts added, documented, tested, and run successfully. |
| 138 | Task Finish / Status Sync MVP | T-0180 | Add a dry-run-first task finish/status sync with bounded execute for `TASK.md` and `docs/TASK_BOARD.md` status/path only. | Done: `hadara task finish --task <id> --json` returns `hadara.task.finish.v1`, execute applies only `TASK.md`/Task Board bounded writes, duplicate rows block, Docker sync-build passed with 71 files / 509 tests, and built CLI dry-run/execute smokes passed. |
| 139 | Task Next Recommendation | T-0181 | Add read-only `hadara task next --json` recommendations from Task Board, Development Slices, and handoff state. | Done: `hadara task next --json` returns `hadara.task.next.v1`, prefers planned Development Slices, falls back to Task Board, reports missing-capsule `createCommand`, and Docker sync-build passed with 72 files / 513 tests. |
| 140 | Schema Stability Classification | T-0182 | Document schema field stability classes such as stable, additive, compatibility alias, deprecated, and experimental. | Done: `docs/SCHEMAS.md` defines field classes, `hadara.task.workbench.v1` annotates `state.closed` as compatibility alias for `state.closedValid`, and Docker sync-build passed with 73 files / 514 tests. |
| 141 | Focused Test Command UX | T-0183 | Add or document a focused test command path that actually runs selected Vitest files. | Done: `test:focused` script added as `vitest run`, SOP/Test Strategy document `npm run test:focused -- tests/unit/<file>.test.ts`, focused smoke ran 1 file / 2 tests, and Docker sync-build passed with 74 files / 516 tests. |
| 142 | Task Finish Write Safety Hardening | T-0184 | Harden `task finish --execute` with before/after hashes, temp-file/rename writes, malformed frame refusal, no-op guards, and safer `task next` createCommand quoting. | Done: task finish planned writes include expected existence/hash metadata, execute uses temp-file/rename with conflict/no-op guards, malformed Task Board/TASK.md frames block, quoted task next createCommand is tested, and Docker sync-build passed with 74 files / 518 tests. |
| 143 | Task Workflow Command Semantics Audit | T-0185 | Clarify task workflow command roles, write boundaries, dry-run behavior, and `ok` semantics before Phase 4 read-surface/operator UI work. | Done: `docs/TASK_WORKFLOW_COMMANDS.md` added; README, AGENTS, SOP, and CLI JSON contract aligned; focused docs regression passed with 1 file / 3 tests; Docker sync-build passed with 75 files / 521 tests. |
| 144 | Evidence Proof Semantics Foundation | T-0186 | Add compatibility-first semantic read-model foundations over existing `hadara.evidence.v1`: normalizer, minimal taxonomy, evidence strength classifier, task proof analyzer, and release proof predicate stub without changing evidence writers or migrating history. | Done: `src/evidence/normalizer.ts` and `src/evidence/semantics.ts` added; focused Docker tests passed with 2 files / 15 tests; Docker sync-build passed with 77 files / 536 tests and built CLI smoke ok:true. |
| 145 | Evidence Lint Semantic Integration | T-0187 | Add semantic summary and semantic issues to `hadara.evidence.lint.v1` additively while preserving current JSONL/Markdown drift checks. | Done: lint reports additive `summary.semantics`, maps actionable semantic errors/warnings for Done tasks, keeps legacy v1 presence in summary instead of noisy issues, focused Docker tests passed with 5 files / 30 tests, and Docker sync-build passed with 77 files / 541 tests. |
| 146 | Protocol and Harness Semantic Gates | T-0188 | Surface task-scoped evidence semantic issues through protocol doctor and add minimal done-level harness blockers for weak, unresolved failed, and unexplained blocked Done tasks. | Done: task protocol doctor surfaces semantic evidence issues, done-level harness validation blocks weak semantic evidence, private-only substantive evidence remains a warning, focused Docker tests passed with 4 files / 55 tests, and Docker sync-build passed with 77 files / 544 tests. |
| 147 | Dashboard/TUI Evidence Semantic Contract | T-0189 | Define the selected-task semantic evidence view consumed by Dashboard/TUI so UI layers do not parse raw evidence meaning independently. | Done: Dashboard and workbench contracts define selected-task evidence semantic sources, proof status derivation, no raw evidence parsing, and read-only/additive boundaries; focused docs test passed with 1 file / 1 test; Docker sync-build passed with 78 files / 545 tests. |
| 148 | Evidence v2 Writer and Migration Plan | T-0190 | Design persisted evidence v2 taxonomy, writer behavior, Markdown frame strategy, dual-read compatibility, and dry-run-first migration separately from semantic read-model work. | Done: `docs/EVIDENCE_V2_WRITER_MIGRATION_PLAN.md` added; schema/test strategy docs aligned; focused docs test passed with 1 file / 1 test; Docker sync-build passed with 79 files / 546 tests; no writer/migration implementation or automatic rewrite added. |
| 149 | Release Evidence Strict Gate | T-0191 | Apply the release proof predicate to release readiness checks after semantic predicates are tested and legacy transition behavior is clear. | Done: release gate package-smoke, clean-checkout smoke, and release-artifact evidence checks now require strict schema-valid/source-ok linked artifacts with expected category/mode; summary-only regression coverage added; Docker sync-build passed with 79 files / 547 tests. |
| 150 | Evidence Semantics Hardening | T-0192 | Harden evidence identity, failed-resolution compatibility wording, release dry-run candidate selection, and Dashboard/TUI proof-status contracts before UI implementation. | Done: normalized records expose `sourceLine`, `fingerprint`, `idSource`, and `idStability`; evidence lint preserves actual JSONL line numbers; release dry-run reuses strict release proof candidate selection before freshness checks; private-only proof is documented as an auditability warning; Docker sync-build passed with 79 files / 548 tests. |
| 151 | Dashboard Live Read Binding | T-0193 | Bind the served dashboard to live `/api/status` with fixture/inline fallback, visible source provenance, and a `Refresh Status` control whose only behavior is re-reading status and updating provenance. | Done: served dashboard loads `/api/status` first, falls back to fixture/inline JSON with source provenance, keeps refresh read-only, focused dashboard test passed with 1 file / 12 tests, and Docker sync-build passed with 79 files / 550 tests. |
| 152 | Dashboard Operator Console Layout | T-0194 | Rework the dashboard shell into a read-only operator console for current state, active/next work, gate/issues, evidence posture, and next copyable action guidance. | Done: dashboard renders Agent Lane, Workstream, Evidence Lens placeholder, and Bottom Inspector over existing read models, keeps read-only/copy-command wording, focused dashboard test passed with 1 file / 13 tests, and Docker sync-build passed with 79 files / 551 tests. |
| 153 | Dashboard Selected Task Evidence Lens | T-0195 | Bind selected-task proof/readiness badges to task workbench and shared evidence semantic reports. | Done: dashboard selected-task lens reads workbench, evidence lint, and evidence list reports through read-only APIs, derives proof status from semantic issue codes/summary, labels private-only as auditability warning, keeps legacy id durability caveat visible, focused dashboard test passed with 1 file / 13 tests, and Docker sync-build passed with 79 files / 551 tests. |
| 154 | Dashboard Timeline Read Model | T-0196 | Add a deterministic read model for selected-task timeline/history suitable for dashboard rendering. | Done: `hadara.dashboard.timeline.v1` service/schema and `/api/timeline` route added, Workstream consumes timeline events, no SSE/polling/persistence/mutation added, focused dashboard/timeline tests passed with 2 files / 14 tests, and Docker sync-build passed with 80 files / 552 tests. |
| 155 | Dashboard Bootstrap Read Model | T-0197 | Start Phase 5.5 by adding `hadara.dashboard.bootstrap.v1` and `/api/dashboard/bootstrap` as the first-paint aggregate read model for status, task summary, active-run/debt summaries, timeline overview, and optional compact selected-task summary. | Done: bootstrap service/schema/route added with disabled cache metadata, compact selected-task proof, invalid selected-task degradation, no deep evidence payload, focused Docker tests passed with 3 files / 17 tests, and Docker sync-build passed with 81 files / 555 tests. |
| 156 | Dashboard Progressive Bootstrap Frontend | T-0198 | Change the dashboard frontend to bootstrap-first progressive rendering with immediate shell paint, in-memory previous successful bootstrap reuse, source/cache badges, and stale Workstream copy cleanup. | Done: dashboard first reads `/api/dashboard/bootstrap`, falls back to `/api/status` then fixture/inline, keeps previous successful in-memory view on refresh failure, shows cache status, replaces stale Workstream copy, forbids browser project-state storage, focused Docker tests passed with 2 files / 16 tests, and Docker sync-build passed with 81 files / 555 tests. |
| 157 | Dashboard Task Detail Aggregate Endpoint | T-0199 | Add `hadara.dashboard.task_detail.v1` and `/api/dashboard/task-detail?taskId=` so selected-task detail uses one backend aggregate instead of frontend fan-out across workbench/evidence/timeline APIs. | Done: task-detail service/schema/route added over workbench, evidence lint, sanitized evidence list, and timeline; backend proof derivation separates private-only auditability warning from blockers; frontend selected-task reads the aggregate route only; focused Docker tests passed with 3 files / 16 tests; Docker sync-build passed with 82 files / 557 tests. |
| 158 | Dashboard Timeline Identity Hardening | T-0200 | Upgrade dashboard timeline evidence events to prefer semantic evidence id, fingerprint, source line, and id stability metadata when available. | Done: timeline evidence events now use normalized evidence ids, expose fingerprint/sourceLine/idSource/idStability metadata, keep `artifact-N` fallback-only, preserve private path suppression, focused Docker tests passed with 3 files / 16 tests, and Docker sync-build passed with 82 files / 557 tests. |
| 159 | Dashboard Serve TTL Cache | T-0201 | Add a process-memory TTL cache for dashboard bootstrap, timeline, and task-detail reads, with response cache metadata and `?cache=bypass` support. | Done: route-level process-memory TTL cache added for bootstrap, task-detail, and timeline reads; cache metadata reports hit/miss/stale/bypass/disabled, bypass recomputes without overwriting cache, `/api/dashboard/cache/status` exposes metadata only, focused host test was unavailable due missing `vitest`, and Docker sync-build passed with 83 files / 560 tests. |
| 160 | Dashboard Degraded UX and Performance Budget | T-0202 | Make dashboard refresh failures and responsiveness observable through loading phases, previous-view retention, source/cache/load status display, performance-budget documentation, and debug-surface boundary tests. | Done: dashboard shows load phase chips, refresh failure keeps previous in-memory view metadata degraded when available, `debugSnapshot()` is read-only metadata only, `docs/DASHBOARD_PERFORMANCE_BUDGET.md` records advisory targets/evidence guidance, no browser project-state persistence was added, and Docker sync-build passed with 83 files / 561 tests. |
| 161 | Optional Dashboard Polling Refresh | T-0203 | Add optional memory-only polling after bootstrap/detail/cache behavior is stable. | Done: polling is off by default, operator-toggleable, uses memory-only `setTimeout` scheduling through the existing read-only refresh path, backs off on degraded reads, pauses while the document is hidden, exposes debug metadata, keeps browser project-state persistence and SSE/WebSocket absent, and Docker sync-build passed with 83 files / 561 tests. |
| 162 | Dashboard Production Readiness Review | T-0204 | Run a final production-readiness pass over dashboard read models, UX, cache, schemas, route inventory, read-only boundaries, private path exposure, browser storage, responsive layout, and performance evidence. | Done: `docs/DASHBOARD_PRODUCTION_READINESS_REVIEW.md` documents route/schema/boundary inventory, residual risks, and readiness conclusion; `docs/SCHEMAS.md` includes dashboard timeline schema status; readiness docs test added; Docker sync-build passed with 84 files / 562 tests. |
| 163 | Dashboard Playwright Performance Measurement | T-0205 | Measure dashboard shell/bootstrap/task-detail/timeline route timings with Playwright Docker after production-readiness work. | Done: `docs/DASHBOARD_PERFORMANCE_MEASUREMENT.md` records shell HTML fetch 4.4 ms, bootstrap bypass avg 174.7 ms, task-detail bypass avg 243.3 ms, timeline bypass avg 150.4 ms, cache hits near 1-2 ms, and Docker sync-build passed with 84 files / 562 tests. |
| 164 | Dashboard Production Hardening Follow-up | T-0206 | Harden post-review dashboard production concerns around project-scoped cache keys, redacted browser-facing project metadata, sidebar navigation, and screenshot polish. | Done: cache keys use `dashboard:sha256:<12hex>:...`, aggregate reports include redacted project references, sidebar tabs switch in-page views, long chips are constrained, and Docker sync-build passed with 84 files / 563 tests. |
| 165 | Dashboard Design Language and Tokens | T-0207 | Establish the Phase 5.6 dashboard design language, tokens, component roles, and accessibility contrast targets before the UI reset. | Done: `docs/DASHBOARD_DESIGN_LANGUAGE.md` authored; contrast target adjusted; visual/a11y gate passed. |
| 166 | Dashboard Frontend Tech Decision Spike | T-0208 | Decide the Phase 5.6 frontend rebuild approach and prove it serves under the existing CSP without external resources. | Done: Preact single-asset bundle decision recorded; build/serve path validated; visual/a11y gate passed. |
| 167 | Dashboard Operator Console Shell Rebuild | T-0209 | Rebuild the dashboard shell into a product-quality operator console with per-view content, honest degraded states, and mobile navigation fixes. | Done: Phase 5.6 shell/views/degraded/loading/mobile fixes landed; full Docker suite passed with 84 files / 562 tests; visual/a11y gate passed. |
| 168 | Dashboard Active Next and Command Affordance | T-0210 | Make active/next work and copy-only command guidance the primary dashboard focal block. | Done: active/next panel and copy-only affordances implemented; host serve smoke and visual/a11y gate passed. |
| 169 | Dashboard Activity Feed Timeline Redesign | T-0211 | Render timeline events as a scannable narrative activity feed with offline-aware empty states. | Done: activity feed redesign and offline labels implemented; visual/a11y gate passed. |
| 170 | Dashboard Proof Verdict and Evidence Lens Redesign | T-0212 | Replace parser-like proof rows with verdict-first evidence presentation and explicit evidence drill affordance. | Done: proof verdict/evidence lens redesign and drill link implemented; visual/a11y gate passed. |
| 171 | Dashboard Metrics With Meaning and Developer JSON | T-0213 | Reframe counts as contextual metrics and move raw JSON behind a read-only disclosure. | Done: contextual metric cards and Developer JSON disclosure implemented; full Docker suite passed with 84 files / 562 tests. |
| 172 | Dashboard Visual Regression and A11y Gate | T-0214 | Lock Phase 5.6 with Playwright screenshot fixtures, axe-core accessibility checks, and new-design static invariants. | Done: visual/a11y gate passed for home/detail/empty/degraded; full Docker suite passed with 84 files / 562 tests. |
| 173 | Phase 5.6 Close / Handoff Sync | T-0215 | Close Phase 5.6 and stage the Phase 5.7 projection-performance track. | Done: T-0207 through T-0214 finished/closed/audited; T-0216 through T-0223 capsules created; project state/handoff/slices updated. |
| 174 | Dashboard Projection Contract | T-0216 | Define `hadara.dashboard.core.v1`, projection metadata, freshness/completeness enums, and bootstrap compatibility before storage or route work. | Done: `hadara.dashboard.core.v1` schema fixture registered; dashboard read-model/schema docs updated; focused schema tests passed; Docker sync-build passed with 85 files / 564 tests. |
| 175 | Dashboard Local Projection Store | T-0217 | Add a local `.hadara/local/cache/dashboard` projection store with atomic replace, redaction, and context-export exclusion. | Done: local projection store service added with redacted wrapper records, file-safe section/key paths, temp-file/rename replacement, raw project path rejection, focused tests, and Docker validation gap recorded due approval usage limit. |
| 176 | Dashboard Core Route from Projection | T-0218 | Serve `/api/dashboard/core` from cheap sources and projection summaries without broad task-capsule scans on the request path. | Done: `/api/dashboard/core` serves `hadara.dashboard.core.v1`, builds cheap live core from bounded docs, writes/reads `core/index.json`, supports bypass recompute, and focused tests assert no task-capsule scan; Docker validation gap recorded due approval usage limit. |
| 177 | Background Refresh and Serve Warmup | T-0219 | Move slow scans into explicit background refresh while keeping serve-start warmup lightweight. | Done: serve-start warmup, `/api/dashboard/refresh`, `/api/dashboard/projection/status`, refresh coalescing, and metadata-only status were added; post-T-0223 follow-up changed serve-start warmup to delayed core-only refresh and changed manual refresh to yielded task/heavy/core stages so first requests are not queued behind immediate full projection scans; focused tests cover warmup/coalescing/body exclusion/stage yielding; Docker validation gap recorded due approval usage limit. |
| 178 | Incremental Task Projection | T-0220 | Track source signals and refresh changed task summaries without reparsing every capsule. | Done: task projection index records `TASK.md`/`evidence.jsonl` signals, changed/reused ids, and redacted summaries; refresh builds it before core; core prefers task projection; focused tests cover unchanged reuse and redacted storage; Docker validation gap recorded due approval usage limit. |
| 179 | Timeline / Debt Projection | T-0221 | Maintain timeline and debt summaries as background projections instead of request-time full reads. | Done: background refresh writes timeline overview and debt summary projections; `/api/dashboard/timeline` and `/api/dashboard/debt` read projections or missing warnings without foreground heavy scans; focused tests cover redaction/no-scan behavior; Docker validation gap recorded due approval usage limit. |
| 180 | Frontend Core + Heavy Merge | T-0222 | Render dashboard core first and merge heavy sections as task-detail/timeline/debt projections arrive. | Done: authored frontend source loads `/api/dashboard/core` before bootstrap/status fallback and backfills debt/timeline projection routes; static source test expectation updated; static bundle rebuild/Docker validation remain blocked by missing deps/approval usage limit and carried to T-0223. |
| 181 | Projection Validation and Visual/A11y States | T-0223 | Lock no-broad-scan behavior, projection redaction, stale/refreshing/degraded states, and visual/a11y coverage. | Done: projection visual/a11y gate now stubs core/timeline/debt/projection-status routes with redacted fixtures and captures projection-ready/detail/stale/refreshing/missing/offline/degraded states; static tests pin fixture schema/redaction/route coverage; `git diff --check`, visual-check syntax, and fixture parse/redaction checks passed; host/Docker validation gaps recorded. |
| 182 | Dashboard Refresh Refactor and Validation Read Model | T-0224 | Fix stale validation fallback and remove avoidable broad synchronous scans from explicit dashboard refresh. | Done: strict refresh refactor spec added; shared table-first handoff validation parser added; manual refresh uses async task projection, core-fed timeline, aggregate-only debt, and core-final stages; done-level harness blocks `TASK.md` metadata placeholders; Docker sync-build passed 90 files / 589 tests; built route smoke confirmed refresh completion and `latestContainsT0096:false`. |
| 183 | Dashboard Cooperative Refresh Progress | T-0225 | Make dashboard projection refresh cooperative and observable while core reads remain non-blocking. | Done: refresh status exposes current stage, processed/total, and last yield metadata; task projection async rebuild reports batch progress; core route returns stale/pending projection metadata during refresh; UI Refresh triggers `/api/dashboard/refresh` without waiting for completion; focused Docker tests passed 3 files / 22 tests; Docker sync-build passed 90 files / 591 tests; built route smoke observed refresh progress and non-blocking stale core. |
| 184 | Dashboard Refresh Responsiveness Measurement | T-0226 | Add refresh responsiveness measurement and per-stage duration metadata before further optimization. | Done: projection status exposes current/completed stage timing and slow-stage warnings; `scripts/dashboard-refresh-responsiveness.mjs` records core p50/p95 during refresh, task-signals progress, stage durations, and `/workspace` vs `/tmp` comparison; measurement docs added; focused Docker tests passed 4 files / 23 tests; Docker sync-build passed 91 files / 592 tests; built measurement smoke reported workspace task-signals 3780 ms versus tmp-ext4 147 ms. |
| 185 | Task Status History Done Gate | T-0227 | Ensure task finish and done-level validation keep `TASK.md` Status History consistent with Done state, and consolidate Markdown section reading on heading-line matching. | Done: `task finish --execute` appends/repairs final Done Status History rows; done-level harness validation rejects non-Done Status History endings with `TASK_STATUS_HISTORY_NOT_DONE`; task/protocol/read-model section extraction uses the shared heading-line Markdown section reader; T-0226 and T-0196 fixture history were aligned; focused Docker tests passed 7 files / 74 tests; Docker sync-build passed 91 files / 595 tests. |
| 186 | TUI Projection-First Operator Read Model | T-0228 | Pause web Dashboard work after Phase 5.7 hardening and align TUI with shared dashboard/operator read models so terminal state shows projection source, refresh progress, pending, and stale sections without HTTP calls or project-document writes. | Done: `docs/specs/tui/HADARA_TUI_Shared_Operator_Read_Model_Spec.md` added; TUI read model now exposes shared dashboard core/projection status; snapshots show source/refresh/pending state; focused Docker TUI tests passed 4 files / 46 tests; Docker sync-build passed 91 files / 595 tests; built snapshot smoke displayed `source projection refresh idle pending timeline,debt`. |
| 187 | TUI Selected Task Detail Shared Read Model | T-0229 | Move selected task proof/evidence semantics to `createDashboardTaskDetailReport()` while keeping existing document viewer files compatible before the task index/cache performance slice. | Done: selected task aggregate now carries dashboard task-detail/proof data; snapshot proof display prefers shared proof; focused TUI tests passed 4 files / 46 tests; Docker sync-build passed 91 files / 595 tests; built snapshot smoke passed but remained slow at 42.56s on `/mnt/f`. |
| 188 | TUI Projection-First Task Index Cache Replacement | T-0230 | Replace TUI broad task index/cache scans with dashboard task projection plus Task Board source signals, path-based selected docs, and fast snapshot selected-proof defer. | Done: TUI task list/cache now avoid broad `tasks/` scans on normal HADARA workspaces; selected docs read directly from summary capsule paths; `hadara tui --snapshot` uses fast profile; focused TUI/CLI tests passed 6 files / 60 tests; Docker sync-build passed 91 files / 595 tests; built `/mnt/f` snapshot improved from 42.56s to 4.05s, with internal read-model/render around 160 ms and CLI startup imports deferred to T-0231. |
| 189 | TUI CLI Lazy Startup for Snapshot Smoke | T-0231 | Remove eager CLI handler import cost from `hadara tui --snapshot` so built `/mnt/f` snapshot smoke can meet the 2s target. | Done: `src/cli/main.ts` lazy-loads command handlers after dispatch; focused CLI/TUI tests passed 8 files / 53 tests; Docker sync-build passed 91 files / 595 tests; built `/mnt/f` snapshot smoke improved from 4.05s to 1.37s. |
| 190 | TUI Overview Markdown Table Preview Cleanup | T-0232 | Remove raw Markdown table header/delimiter noise from compact TUI Overview cards over table-first Task Capsule and handoff docs. | Done: Markdown preview extraction skips table headers and summarizes data rows; fast TUI handoff status uses shared table-aware parser; focused TUI/status tests passed 4 files / 34 tests; Docker sync-build passed 91 files / 597 tests; built snapshot grep found no reported table preview header strings. |
