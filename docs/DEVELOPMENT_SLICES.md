# DEVELOPMENT_SLICES

HADARA development should proceed in small, evidence-backed slices. Each slice must live in a Task Capsule, run meaningful validation, update `EVIDENCE.md` and `evidence.jsonl`, and refresh `docs/AGENT_HANDOFF.md` before stopping.

| Order | Slice | Capsule | Purpose | Done Evidence |
|---|---|---|---|---|
| 1 | Harness validate JSON | T-0009 | Stabilize `hadara harness validate --task <id> --json` for external agents and CI. | Build/tests pass, CLI JSON sample recorded. |
| 2 | Harness replay skeleton | T-0010 | Add deterministic replay schema and ScriptedProvider-backed no-op scenario checks. | Done: 7 test files passed, 34 tests passed, replay JSON smoke recorded. |
| 3 | CLI JSON normalization | T-0011, T-0012, T-0013, T-0014, T-0015 | Apply common JSON envelope and exit codes to core CLI commands. | Done: doctor, task list/show, policy, Hermes, and evidence collect JSON. |
| 4 | Evidence artifact handling | T-0016 | Copy public artifacts into managed task/session evidence storage without leaking private paths. | Done: public artifact copy tests and private path suppression checks. |
| 5 | Policy execution preflight | T-0017 | Connect policy evaluator to ShellTool design before shell execution exists. | Done: allowed, approval-required, and denied preflight tests/smokes. |
| 6 | Provider fallback executor | T-0018 | Add fallback orchestration on top of the existing ProviderClient contract. | Done: contract tests with MockProvider/ScriptedProvider. |
| 7 | Shell preflight harness | T-0019 | Add deterministic fake shell observations gated by policy preflight before real shell execution. | Done: Docker check and fake shell policy gate tests. |
| 8 | Task Capsule format validation | T-0020 | Enforce Markdown format markers so handoff/context compression cannot silently drift capsule structure. | Done: Docker check and format drift regression tests. |
| 9 | Agent loop minimal harness | T-0021 | Implement a bounded deterministic loop using ScriptedProvider responses and fake shell observations first. | Done: Docker check, CLI run smoke, evidence index validation, and T-0021 harness validation. |
| 10 | Workspace file boundary | T-0023 | Ensure CLI file inputs are confined to project-root realpaths before broader tool/file surfaces grow. | Done: Docker check passed, 18 test files passed, 74 tests passed, T-0023 harness validation passed. |
| 11 | Evidence artifact redaction | T-0024 | Prevent public committed artifacts from carrying secrets or binary private data. | Done: Docker check passed, 18 test files passed, 78 tests passed, T-0024 harness validation passed. |
| 12 | CLI args parser | T-0025 | Split growing option parsing into strict reusable helpers. | Done: Docker check passed, 19 test files passed, 84 tests passed, T-0025 harness validation passed. |
| 13 | Agent loop evidence attachment | T-0026 | Attach deterministic fake-shell loop outputs as evidence without real shell execution. | Done: Docker check passed, T-0026 harness validation passed, run JSON smoke recorded evidence metadata. |
| 14 | Deterministic ScriptedProvider and Capsule Evidence Index | T-0027 | Ensure deterministic scripts are consumed in order and new capsules include required evidence indexes. | Done: Docker check passed, T-0027 harness validation passed, task-create smoke confirmed empty `evidence.jsonl`. |
| 15 | Init profiles protocol docs | T-0028 | Improve first-use HADARA initialization with protocol docs and profile selection. | Done: Docker check passed, T-0028 harness validation passed, full-profile Hermes export smoke passed. |
| 16 | Done-level harness validation | T-0029 | Add completion gates beyond structural Task Capsule validation. | Done: Docker check passed, done-level harness validation passed. |
| 17 | Run scenario scaffold | T-0030 | Generate deterministic run script and fake-shell fixture files for demo/task workflows. | Done: Docker check passed, built CLI scaffold/run smoke returned `ok: true`. |
| 18 | CLI handler extraction | T-0031 | Reduce `src/cli/main.ts` density by extracting cohesive command helpers. | Done: Docker check passed, init/run scaffold smoke passed, `main.ts` reduced from 667 to 459 LOC. |
| 19 | CLI harness handler extraction | T-0032 | Continue CLI dispatcher reduction by extracting harness validate/replay handling. | Done: Docker check passed, harness validate/replay smokes passed, `main.ts` reduced from 459 to 419 LOC. |
| 20 | CLI evidence handler extraction | T-0033 | Continue CLI dispatcher reduction by extracting evidence collect handling. | Done: Docker check passed, evidence collect JSON/text smokes passed, `main.ts` reduced from 419 to 387 LOC. |
| 21 | CLI policy handler extraction | T-0034 | Continue CLI dispatcher reduction by extracting policy check/preflight handling. | Done: Docker check passed, policy check/preflight JSON/text smokes passed, `main.ts` reduced from 387 to 358 LOC. |
| 22 | CLI Hermes and handoff handler extraction | T-0035 | Continue CLI dispatcher reduction by extracting Hermes and handoff handling. | Done: Docker check passed, Hermes JSON/text and handoff smokes passed, `main.ts` reduced from 358 to 332 LOC. |
| 23 | CLI remaining handler extraction | T-0036 | Finish current CLI dispatcher extraction pass by moving init, doctor, task, mcp, and run handling. | Done: Docker check passed, remaining command smokes passed, `main.ts` reduced from 332 to 115 LOC. |
| 24 | Runtime validation and harness semantics | T-0037 | Harden reviewed runtime validation gaps before expanding external bridge surfaces. | Done: Docker check passed, hardening smokes passed, 105 tests passed. |
| 25 | CLI JSON error envelope | T-0038 | Return stable JSON envelopes for early CLI parse and validation failures. | Done: Docker check passed, JSON error smokes passed, 108 tests passed. |
| 26 | Policy safe command exactness | T-0039 | Prevent safe command prefix matches from being classified as safe. | Done: Docker check passed, exactness smoke passed, 109 tests passed. |
| 27 | Handoff compaction policy | T-0040 | Keep project handoff current by moving old history into dedicated history docs. | Done: Docker check passed, compact handoff and history docs added. |
| 28 | Old Draft task reclassification | T-0041 | Mark overtaken early Draft tasks so external agents follow the current roadmap instead. | Done: T-0003 Superseded, T-0006 Partial, validation passed. |
| 29 | Hermes/MCP read-only contract | T-0042 | Define CLI JSON and read-only MCP bridge contracts before server implementation. | Done: contract docs added, Hermes guidance updated, validation passed. |
| 30 | MCP JSON-RPC server skeleton | T-0043 | Add stdio MCP server skeleton with no write tools, shell execution, provider calls, or file mutation. | Done: Docker check passed, 23 test files passed, 115 tests passed, built CLI initialize smoke returned read-only metadata. |
| 31 | MCP read tools implementation | T-0044 | Implement read-only tools from `docs/MCP_BRIDGE_CONTRACT.md`. | Done: Docker check passed, 24 test files passed, 122 tests passed, built CLI tools/call smoke returned MCP JSON text payload. |
| 32 | MCP bridge harness tests | T-0045 | Validate MCP tool outputs against existing CLI JSON contracts. | Done: Docker check passed, 25 test files passed, 128 tests passed, MCP bridge contract tests added. |
| 33 | Evidence attach tool contract | T-0046 | Define a future write-capable evidence attach contract, still without shell execution. | Done: contract doc added, policy/evidence safety gates documented, Docker check passed. |
| 34 | Evidence attach guard tests | T-0047 | Ensure future evidence attach contract does not accidentally advertise write-capable MCP behavior before implementation. | Done: Docker check passed, 26 test files passed, 131 tests passed, evidence attach remains unadvertised and unimplemented. |
| 35 | Gated MCP evidence attach implementation | T-0048 | Implement `hadara.evidence.attach` behind explicit operator opt-in while preserving default read-only MCP behavior. | Done: Docker check passed, 26 test files passed, 132 tests passed, opt-in tools/list smoke advertised evidence attach. |
| 36 | MCP evidence attach safety tests | T-0049 | Validate workspace boundary, public artifact redaction, and evidence JSON parity for opt-in evidence attach. | Done: Docker check passed, 27 test files passed, 137 tests passed, safety contract tests added. |
| 37 | MCP write audit log | T-0050 | Add private portable audit records for opt-in MCP evidence attach write attempts. | Done: Docker check passed, 27 test files passed, 142 tests passed, done-level harness validation passed. |
| 38 | MCP phase/mode config | T-0051 | Keep MCP initialize metadata aligned with default read-only and evidence attach-enabled modes. | Done: Docker check passed, 27 test files passed, 142 tests passed, done-level harness validation passed. |
| 39 | MCP evidence attach approval record | T-0052 | Require per-call approval actor and reason for opt-in MCP evidence attach writes. | Done: Docker check passed, 27 test files passed, 142 tests passed, done-level harness validation passed. |
| 40 | Operations Status JSON | T-0053 | Provide a single dashboard/external-agent JSON snapshot before implementing UI. | Done: Docker check passed, status JSON tests and CLI smokes passed, done-level harness validation passed. |
| 41 | Operations Status JSON cleanup | T-0054 | Harden status JSON degradation signals and stable dashboard-facing task count fields. | Done: Docker check passed, 28 test files passed, 147 tests passed, done-level harness validation passed. |
| 42 | Dashboard read model contract | T-0055 | Define dashboard consumption contract after Operations Status JSON exists. | Done: Docker check passed, 28 test files passed, 148 tests passed, sample fixture and field mapping added. |
| 43 | Minimal static dashboard | T-0056 | Build a minimal static dashboard consuming sample status JSON. | Done: Docker check passed, 29 test files passed, 150 tests passed, static fixture/dashboard smoke added. |
| 44 | Dashboard mockup adoption | T-0057 | Promote the comfort dark mockup to the dashboard visual baseline while preserving `hadara.ops.status.v1` data binding. | Done: Docker check passed, 29 test files passed, 152 tests passed, static mockup shell tests added. |
| 45 | Dashboard fixture binding smoke | T-0058 | Verify the adopted mockup shell renders fixture fields and fallback data without drift. | Done: Docker check passed, 29 test files passed, 155 tests passed, static data-field binding smoke added. |
| 46 | Dashboard served from HADARA CLI | T-0059 | Serve the static dashboard through a HADARA CLI entry point. | Done: Docker check passed, 29 test files passed, 155 tests passed, `hadara dashboard serve` static asset helper added. |
| 47 | Dashboard serve boundary hardening | T-0060 | Harden the static dashboard serving boundary before live dashboard integration. | Done: Docker check passed, 29 test files passed, 155 tests passed, method/header/traversal regressions added. |
| 48 | Evidence index schema hardening | T-0061 | Prevent manual evidence records from drifting away from `hadara.evidence.v1`. | Done: Docker check passed, 29 test files passed, 158 tests passed, required-field regressions added. |
| 49 | Dashboard server failure semantics | T-0062 | Make static dashboard server failures predictable before live dashboard work. | Done: Docker check passed, 29 test files passed, 158 tests passed, missing-root and internal-error regressions added. |
| 50 | Architecture/roadmap state reconciliation | T-0063 | Align high-level docs with current implemented, partial, and deferred capabilities. | Done: architecture and roadmap docs updated; build and done-level validation passed. |
| 51 | Roadmap v0.3 Operations Layer Freeze | T-0064 | Freeze v0.3 as a read-only operations layer before product-wide v1.0 expansion. | Done: ROADMAP and DEVELOPMENT_SLICES updated; exclusions explicit; validation passed. |
| 52 | Context Export MCP Instructions | T-0065 | Ensure exported context instructs external agents to use HADARA CLI/MCP surfaces first. | Done: context export source docs and instruction regression tests pass. |
| 53 | Compatibility Fixture | T-0066 | Prove Hermes-like external agent read-only workflow against HADARA MCP/CLI JSON. | Done: fixture replay contract test passes and write/execution-like MCP tools remain unavailable. |
| 54 | CLI/MCP Service Parity Refactor | T-0067 | Move duplicated CLI/MCP read logic into shared services/read models. | Done: project/handoff read services are shared and CLI/MCP parity tests pass. |
| 55 | Single Active Run State | T-0068 | Track active task/run without queue or multi-agent assumptions. | Done: active run manifest, stale handoff detection, status projection, and resume projection tests pass. |
| 56 | Operational Debt Track | T-0069 | Convert known_issue.log into structured product/development debt track. | Done: debt schema, capsule-size indicators, and premature acceptance checks pass. |
| 57 | Operations State Robustness Fix | T-0070 | Harden local operations state and debt checks before provider preparation. | Done: active-run corruption, missing task, premature acceptance, and section extraction regressions pass. |
| 58 | Core v1.0 Technical Plan Refresh | T-0072 | Align v1.0 planning docs with T-0066 through T-0070 implementation/design mismatch notes. | Done: technical plan, schema notes, backlog, board, state, and handoff updated; validation passed. |
| 59 | Redaction Hardening | T-0074 | Promote public evidence redaction from simple regex checks to a registry/report model. | Done: redaction registry/report tests cover AWS, GitHub, JWT, private key, npm, capture-group preservation, and no-capture replacement cases. |
| 60 | Redaction Policy Follow-up | T-0075 | Separate redaction scan/report output from public evidence policy blocking and settle near-term MCP planning names. | Done: threshold helper, public artifact policy update, planning docs, and Docker checks passed. |
| 61 | Evidence List Read Model | T-0076 | Add stable CLI/MCP/dashboard evidence list read models before timeline/dashboard integration. | Done: shared `hadara.evidence.list.v1` report builder, CLI JSON, read-only MCP tool, and malformed JSONL degraded-read tests pass. |
| 62 | Context Export MCP Read Tool | T-0077 | Add read-only MCP context export as memory payload without writing generated context files. | Done: `hadara.context.export` MCP tests pass, memory report returns `contextPath: null`, and file mutation is absent. |
| 63 | Tools List Read Model | T-0078 | Expose current CLI/MCP capabilities and disabled surfaces to external agents. | Done: neutral capability registry, shared `hadara.tools.list.v1`, fuller CLI surface discovery, read-only MCP `hadara.tools.list`, availability/risk metadata, opt-in evidence attach status, and disabled shell/provider/release/broad-write surfaces pass Docker validation. |
| 64 | Schema Layer Planning | T-0079 | Define the schema registry and first JSON schema fixtures before broad validation gates. | Done: `docs/SCHEMAS.md`, schema index fixture, and first JSON Schema fixtures for evidence list, context export, and tools list pass Docker validation. |
| 65 | Service Parity Expansion | T-0080, T-0081, T-0084, T-0085 | Expand shared services/read models beyond project/handoff into task, evidence, policy, harness, context, and status. | Done: task, policy, harness validate, context export, evidence/tools read models, and Operations Status JSON now live behind shared service/read-model boundaries. |
| 65a | Read-model Cleanup Follow-up Notes | T-0082 | Capture cleanup gaps before release gates: redaction policy observability, schema strictness levels, task.read evidence normalization, and PolicyService authorization boundaries. | Done: cleanup notes captured in docs and policy check mode input matches policy evaluate string/default behavior. |
| 65b | Task Read Evidence Normalization | T-0083 | Reuse evidence-list normalization for task.read embedded evidence data. | Done: task.read evidenceIndex and files["evidence.jsonl"] use normalized evidence records; focused task/evidence/MCP tests pass. |
| 65c | Harness Validate Service Parity | T-0084 | Route harness validate report generation through a shared service boundary and tighten task.read private evidence defaults. | Done: CLI/MCP harness validate use `createHarnessValidateReport`; task.read private evidence is opt-in via `includePrivate`; focused tests pass. |
| 65d | Operations Status Service Parity | T-0085 | Route Operations Status JSON report generation through a shared service boundary. | Done: CLI status commands use `createOpsStatusReport` from `src/services/operations-status-service.ts`; focused status tests and full check pass. |
| 66 | Active Run Read Surfaces | T-0086 | Formalize read-only run-state CLI and MCP active-run/resume tools while deferring run-state writes. | Done: `run-state show/resume --json`, `hadara.active.run.read`, and `hadara.active.run.resume` tests pass; active-run writes remain deferred. |
| 66a | Active Run Resume Hardening | T-0088 | Harden read-only active-run resume guidance and schema fixtures after review. | Done: canonical Task Capsule path resolution, `ACTIVE_RUN_CAPSULE_MISMATCH` warnings, active-run schema fixtures, and read-only resume wording pass validation. |
| 66b | Active Run Runtime Schema Validation | T-0092, T-0094 | Add runtime schema validation for mutable active-run projection/resume read models before stricter release gates depend on them. | Done: active-run projection/resume reports pass runtime schema validation, malformed local-state degraded reports remain valid, report schema failures use a distinct warning code, and Docker checks pass. |
| 67 | Operational Debt Release Gates | T-0087 | Promote operational debt into read surfaces and release-gate modes. | Done: `hadara debt list/show --json`, read-only MCP `hadara.debt.list/show`, ops debt aggregates, advisory warning checks, and strict high-open debt blocking reports pass. |
| 67a | Redaction Policy Observability Tests | T-0089 | Cover safe redaction diagnostics for public evidence artifact policy before broader evidence/security inspection surfaces. | Done: safe artifact policy report helper covers pattern/severity/count/byte metadata, medium diagnostics do not block, and high/critical findings still block. |
| 68 | Policy Matrix Refactor | T-0090, T-0093 | Split tokenizer, command risk, permission matrix, presets, and preflight policy. | Done: focused policy tests and full Docker check pass with matrix regressions for read/test/build/write/network/destructive/release, release-risk blocking, network approval, strict release-gate exit codes, and T-0093 feedback verification. |
| 69 | Private Evidence Manifest | T-0091, T-0094 | Track private evidence metadata, hashes, retention, and context-export exclusion. | Done: private evidence manifests record hashes, retention, deferred encryption metadata, and audit events while committed files and context export exclude private content and paths; raw source copying is project-boundary-limited by default. |
| 70 | Logger and Audit Event Model | T-0095 | Add structured event schema and clarify stdout/stderr/audit/debug log boundaries. | Done: `hadara.event.v1` helpers and schema fixture exist, audit JSONL writes preserve compatibility fields while embedding structured events, and Docker checks pass. |
| 71 | Real Provider Adapter Preparation | T-0096 | Prepare provider schema and adapter contract only after the operations layer freeze and service/read-model parity work. | Done: provider config/call schema fixtures and helper APIs reject stored secrets, deny unknown config input fields, assert schema-valid outputs, omit prompt/response content, and pass Docker validation without real provider execution. |
| 72 | Dashboard Read Integration | T-0097 | Serve local read APIs for status/tasks/evidence/active-run/debt without shell or write behavior. | Done: dashboard route tests pass for `/api/status`, `/api/tasks`, `/api/evidence?taskId=<task-id>`, `/api/active-run`, and `/api/debt`; full Docker check passes. |
| 73 | CLI Write Boundary Preflight | T-0098 | Add expected-write preflight reports for CLI-owned write commands. | Done: `hadara.write.preflight.v1`, `hadara write preflight <command...> --json`, and focused tests list files before task/evidence/handoff/run-state/debt writes without executing target writes. |
| 73a | TUI Design Alignment | T-0099 | Incorporate `.mockup/tui` and `.mockup/tui-final` learnings into main docs before production TUI work. | Done: TUI design notes, architecture, roadmap, backlog/schema notes, and state docs consistently describe a read-only terminal work console without production implementation. |
| 73b | TUI Read-Model Aggregator | T-0100 | Implement the first integrated terminal UI service over shared read models without renderer, write, shell, provider, MCP, or release behavior. | Done: internal aggregate composes status/tasks/detail/evidence/active-run/debt/release/tools/write-preflight preview, focused tests pass, and no-write boundary is covered. |
| 73c | Task Board Append Done-Level Guard | T-0101 | Prevent completed capsules from leaving duplicate or stale `docs/TASK_BOARD.md` rows. | Done: done-level harness validation checks exactly one row, Done status, and capsule path for the validated task. |
| 73d | TUI Snapshot Renderer | T-0102 | Render Overview/Tasks/Detail/Help from the TUI aggregate model without interactive input or write behavior. | Done: deterministic no-color fixed-size snapshots pass for all panels, narrow clipping, and no-write behavior. |
| 73e | TUI Mockup Parity Module Port | T-0103 | Split `.mockup/tui/app.js` rendering concepts into TypeScript constants, layout, Markdown, and snapshot modules before interactive runtime work. | Done: mockup-style Work Console snapshots, document tabs, layout helpers, and Markdown renderer pass focused and full Docker validation. |
| 73f | TUI Snapshot Polish | T-0104 | Harden deterministic snapshot output, width policy, and wide-character Markdown rendering before interactive state work. | Done: generatedAt is hidden by default, compact mode is explicit, Korean snapshot/Markdown regressions pass, and full Docker validation passes. |
| 73g | TUI Interactive State | T-0105 | Add keyboard/search/refresh state transitions over the snapshot-ready renderer without writes or shell/provider/MCP behavior. | Done: pure state-transition tests cover panels, task selection/search, document tabs, scroll, refresh request/complete, quit, and no-write behavior; full Docker validation passes. |
| 73h | TUI Raw Terminal Shell | T-0106 | Add local terminal key decoding, redraw loop, clean shutdown, and optional mouse over the tested state/renderer without write behavior. | Done: internal injected-stream terminal shell covers key decoding, redraw, refresh/detail-refresh effects, raw-mode restoration, quit handling, and no-write boundary tests before any public CLI entry point is added. |
| 73i | TUI Public CLI Entry Point | T-0107 | Expose the read-only terminal work console through `hadara tui` while keeping smoke rendering available for non-interactive validation. | Done: `hadara tui` routes to the internal terminal shell for TTY sessions, `hadara tui --snapshot` renders a read-only frame, capability discovery marks it read-only, focused tests and full Docker validation pass. |
| 73j | TUI Native Runtime Docs Assimilation | T-0108 | Reflect the full TUI mockup parity/native runtime design into main v1.0 planning docs without omission. | Done: `docs/V1_0_IMPLEMENTATION_SCHEMAS.md` preserves the unabridged design body, and backlog/schema/test docs carry the follow-up capsule, schema posture, and validation requirements. |
| 73k | TUI Local Cache and Incremental Refresh | T-0109 | Add local TUI cache acceleration under `.hadara/local/tui/` with task index invalidation and opt-in CLI use. | Done: internal `hadara.tui.cache.v1` cache records, source-signal invalidation, full/fast/detail/none refresh modes, opt-in `hadara tui --cache`, private-evidence cache disable, context-export exclusion, focused tests, TypeScript build, and 1000-capsule benchmark pass. |
| 73l | TUI Visual Parity and Loading States | T-0110 | Apply mockup visual feel, themes, loading frames, and keyboard ergonomics to the read-only production TUI. | Done: HADARA/contrast themes, no-color default snapshots, status/log line, loading frames, card/task/detail polish, Tab/Shift-Tab/Home/End/search completion/Korean quit handling, focused tests, and full Docker validation pass. |
| 73m | TUI Frame Color and Viewer Parity Fix | T-0111 | Refine TUI visual parity after operator feedback on frame line color and legacy-feeling document viewer. | Done: theme output now uses mockup true-color RGB, badge/keycap foreground/background composition is fixed, detail viewer lines are colorized, keyboard document scrolling renders long docs, compact mockup tab labels are applied, cache source-signal fast validation reuses unchanged hashes after mtime/size checks, no-color output remains stable, focused tests and full Docker validation pass. |
| 73n | TUI Loading Animation Mouse and Resize Ergonomics | T-0112 | Make TUI open immediately with loading frames and add basic mouse/resize ergonomics without writes. | Done: startup/loading frames render before initial/full/detail loads, SGR mouse panel/task/doc-tab clicks work, resize redraws current frame, focused tests and full Docker validation pass. |
| 73o | TUI Async Loading and Read-Model Performance Refactor | T-0113 | Fix the remaining production TUI responsiveness gap versus the mockup by moving expensive aggregate reads off startup/detail paths. | Done: fast TUI read-model profile defers debt/release/tools/write-preview advisory reads, interactive startup/refresh/detail use the fast profile, missing `TASK.md` task cache entries degrade to zero-size index records, focused/full Docker checks pass, and measured `/workspace` terminal paths improved from 18-24s to about 2.8-3.1s. |
| 73p | TUI Mockup UX Parity Audit Fix | T-0114 | Close high-impact mockup UX mismatches before deeper async/worker loading work. | Done: fast-profile deferred advisory reads render as deferred, task-list windows follow interaction scroll/search state, active search copy shows a cursor, task-row mouse clicks open refreshed Detail, wide table clicks no longer hit left nav, and focused/full Docker checks pass. |
| 73q | TUI Renderer-Derived Mouse Hitboxes and Detail Tab Fix | T-0115 | Make mouse targeting, Task cursor windowing, and intermediate-width Overview card rendering follow the mockup-derived TUI frame behavior. | Done: snapshot renderer emits internal panel/task/document hitboxes, terminal SGR mouse handling consumes those hitboxes, SGR release payload digits are ignored instead of becoming panel keys, Task tab cursor/window movement follows the mockup visible-row offset policy, Overview work cards keep Previous Work borders and label colors at intermediate wide widths, focused TUI tests pass, full Docker check passes, and the read-only boundary is unchanged. |
| 73r | TUI Markdown Viewer and Live Overview Parity | T-0116 | Make the production Detail Markdown viewer, Overview current/previous work summaries, and loading pulse match the mockup while keeping read-model-first boundaries. | Done: Markdown headings/lists/tables match mockup semantics, heading-aware previews feed Overview summaries, latest two task rows drive Current/Previous Work, built interactive TUI uses async loading pulse with worker-backed reads when available, focused TUI tests pass, full Docker check passes, and the read-only boundary is unchanged. |
| 73s | TUI Arrow Input and Narrow Color Clipping Fix | T-0117 | Fix terminal-specific arrow-key decoding, hidden Detail viewer over-scroll, and narrow-window color clipping after T-0116 operator feedback. | Done: production decoder supports basic, application-cursor, and modifier arrow sequences; Detail document scroll clamps at the rendered bottom; ANSI-aware fitting preserves color while clipping narrow text; focused input/layout/state/terminal tests pass; full Docker check passes; read-only boundary is unchanged. |
| 73t | TUI Tasks Height and Overview Copy Parity | T-0118 | Align Tasks height and Overview copy/search behavior with the mockup after follow-up operator feedback. | Done: Tasks visible rows derive from Detail's available-height policy, terminal page movement uses the same row count, Resume Signals is concise, Work-card Next/Proof fallback order matches the mockup, numeric panel keys can be typed while Tasks search is active, focused TUI tests pass, full Docker check passes, and the read-only boundary is unchanged. |
| 74 | Release and Packaging Track | T-0119 | Define install, CI, clean-checkout, and release-gate behavior. | Done: release gate reports package/bin/script, Node/CI, clean-checkout smoke, generated artifact policy, and operational debt checks; focused/full Docker checks and built CLI advisory/strict smokes pass. |
| 75 | Dogfooding E2E Fixture | T-0120 | Replay a HADARA-on-HADARA workflow from context export to done-level validation. | Done: deterministic fixture replays in-memory context export, policy check, evidence attachment, handoff update, and done-level harness validation. |
| 76 | Done-level Capsule Scaffold Guard | T-0121 | Prevent completed Task Capsules from retaining initial scaffold/default Markdown content. | Done: done-level harness validation rejects scaffold/default leftovers, draft-level validation remains structural, focused harness/dogfooding tests pass, and full Docker check passes. |
| 77 | Remote CI/Release Observation | T-0122 | Confirm remote CI behavior after local Docker validation and before v1.0 release gate freeze. | Done: GitHub Actions CI run #109 on `main` succeeded for commit `8b4f33d1bf926d051cf63e13ca2de222bfc22d8c`; release-gate readiness checks documented remote CI observation without calling GitHub. |
| 78 | Operational Debt High Severity Mitigation | T-0123 | Reduce the remaining high operational debt gate before treating strict release readiness as green. | Done: OD-0003 and OD-0008 are mitigated by existing protocol/done-level safeguards, debt aggregate `highOpen` is 0, and strict release gate passes when all documented readiness checks pass. |
| 79 | Clean Checkout Package Smoke Planning | T-0124 | Make clean-checkout package smoke expectations explicit before executable package-smoke work. | Done: TEST_STRATEGY documents the smoke sequence and non-execution boundaries; the read-only strict release gate requires those markers before passing the clean-checkout smoke check. |
| 80 | Executable Package Smoke Artifact Boundary Design | T-0125 | Define executable package-smoke workspace, artifact, redaction/audit, and evidence boundaries before implementation. | Done: TEST_STRATEGY documents the boundary; the read-only strict release gate reports `PACKAGE_SMOKE_ARTIFACT_BOUNDARY` as the check and maps missing markers to `PACKAGE_SMOKE_ARTIFACT_BOUNDARY_UNCLEAR` issues while still performing no package-smoke execution. |
| 81 | Package Smoke Command Surface Design | T-0126 | Define `hadara package smoke` command naming, flags, approval, cleanup, timeout, failure, and evidence semantics before implementation. | Planned: use `docs/specs/HADARA_Release_Install_Package_Smoke_Capsule_Plan.md`; no package/release execution. |
| 82 | Package Metadata Release Readiness | T-0127 | Decide package name, version policy, `private` transition, `files` whitelist target, license path, and installed `hadara` verification. | Planned: record metadata decisions without publishing; defer `files` mutations until referenced installer/portable files exist. |
| 83 | Installer Script Surface and Schema | T-0128 | Define POSIX/Windows installer surfaces, portable launchers, default install locations, Node 22/WSL checks, and install report schemas. | Planned: installer/launcher contract only; release gate stays read-only. |
| 84 | Installer Dry-run Implementation | T-0129 | Implement dry-run install planning for installer scripts and portable launchers, splitting POSIX/Windows/portable subcapsules if needed. | Planned: `hadara.install.plan.v1` dry-run output with no install mutation. |
| 85 | Install Matrix Smoke Plan | T-0130 | Define Linux, Windows, WSL, npm package, and USB portable install matrix rows before execution. | Planned: distinguish Docker/Linux, real Windows, and USB/WSL validation expectations. |
| 86 | Major Feature Smoke Runner | T-0131 | Implement a bounded installed-CLI smoke runner with `core` and `release-readiness` profiles. | Planned: package smoke uses `core` to avoid release-gate evidence cycles. |
| 87 | Package Smoke Schema and Fixture | T-0132 | Register and fixture `hadara.packageSmoke.v1` before package-smoke execution. | Planned: prove reports omit raw package contents, raw npm logs, env secrets, private paths, and private store paths. |
| 88 | Package Smoke Dry-run Implementation | T-0133 | Implement `hadara package smoke --dry-run --json` without `npm pack`, install, artifact writes, or evidence attachment by default. | Planned: dry-run report previews workspace, steps, artifacts, and issues. |
| 89 | Local Package Smoke Execution | T-0134 | Implement explicit local package smoke with `npm pack`, isolated prefix install, `hadara doctor --json`, core feature smoke, cleanup, and reduced report. | Planned: no publish, GitHub release, Docker image build, global install, or public raw logs. |
| 90 | Clean Checkout Smoke Implementation | T-0135 | Implement clean-checkout source smoke in a disposable copy while keeping installed CLI smoke as a separate `hadara` command-form requirement. | Planned: source workspace remains unchanged; reduced/redacted report. |
| 91 | Smoke Evidence Integration | T-0136 | Attach reduced package/install smoke evidence safely while keeping raw logs private or temporary. | Planned: public evidence is redaction-checked under `tasks/<task-id>/artifacts/package-smoke/`. |
| 92 | Release Artifact Builder | T-0137 | Build tarball/checksum/manifest and verify package contents without publishing. | Planned: disposable or explicit output directory; reduced release artifact report. |
| 93 | Release Gate Evidence Freeze | T-0138 | Move release gate from plan markers toward evidence-backed readiness while keeping it read-only. | Planned: strict mode can require recent package/install/clean-checkout/artifact/remote-CI evidence. |
| 94 | CI/Release Workflow Target Decision | T-0139 | Decide primary and secondary release targets, recommended as npm package first and GitHub Release second, with Docker deferred. | Planned: document required token names without storing secrets; no publish/deploy. |
| 95 | Final Deployment Script Dry Run | T-0140 | Create dry-run release scripts that verify workspace, metadata, checks, smoke evidence, install matrix evidence, and artifact manifest. | Planned: reduced JSON output only; no publish, GitHub Release creation, or registry mutation. |
| 96 | Final Publish/Deploy Script | T-0141 | Implement explicit approval-gated publish/deploy script after dry-run and evidence gates are stable. | Planned: release mode, token presence checks without value logging, reduced evidence, private audit, and no default MCP callable surface. |
