{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "hadara.protocol.remediation.v1",
  "x-hadara-schema-id": "hadara.protocol.remediation.v1",
  "title": "HADARA Protocol Remediation Report",
  "type": "object",
  "additionalProperties": true,
  "required": ["schemaVersion", "command", "ok", "mode", "projectRoot", "fix", "summary", "actions", "issues"],
  "properties": {
    "schemaVersion": { "const": "hadara.protocol.remediation.v1" },
    "command": { "const": "protocol.remediate" },
    "ok": { "type": "boolean" },
    "mode": { "type": "string", "enum": ["dry-run", "execute"] },
    "projectRoot": { "type": "string", "minLength": 1 },
    "fix": {
      "type": "string",
      "enum": ["task-board-row", "decisions-table-frame", "project-state-profile", "evidence-jsonl"]
    },
    "summary": {
      "type": "object",
      "additionalProperties": true,
      "required": ["planned", "changed", "skipped"],
      "properties": {
        "planned": { "type": "integer" },
        "changed": { "type": "integer" },
        "skipped": { "type": "integer" }
      }
    },
    "actions": {
      "type": "array",
      "items": { "$ref": "#/$defs/action" }
    },
    "issues": {
      "type": "array",
      "items": { "$ref": "#/$defs/issue" }
    }
  },
  "$defs": {
    "action": {
      "type": "object",
      "additionalProperties": true,
      "required": ["id", "path", "status", "summary"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "path": { "type": "string", "minLength": 1 },
        "status": { "type": "string", "enum": ["planned", "created", "updated", "skipped"] },
        "summary": { "type": "string", "minLength": 1 },
        "before": { "type": "string" },
        "after": { "type": "string" },
        "expectedBeforeExists": { "type": "boolean" },
        "expectedBeforeHash": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
        "afterHash": { "type": "string", "pattern": "^[a-f0-9]{64}$" }
      }
    },
    "issue": {
      "type": "object",
      "additionalProperties": true,
      "required": ["severity", "code", "message"],
      "properties": {
        "severity": { "type": "string", "enum": ["error", "warning", "info"] },
        "code": { "type": "string", "minLength": 1, "pattern": "^[A-Z0-9_:-]+$" },
        "message": { "type": "string", "minLength": 1 },
        "path": { "type": "string", "minLength": 1 }
      }
    }
  }
}
