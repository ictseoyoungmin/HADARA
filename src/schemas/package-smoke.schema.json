{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "hadara.packageSmoke.v1",
  "x-hadara-schema-id": "hadara.packageSmoke.v1",
  "title": "HADARA Package Smoke Report",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "schemaVersion",
    "command",
    "ok",
    "mode",
    "readOnly",
    "execution",
    "workspace",
    "source",
    "steps",
    "artifacts",
    "privacy",
    "issues"
  ],
  "properties": {
    "schemaVersion": { "const": "hadara.packageSmoke.v1" },
    "command": { "const": "package.smoke" },
    "ok": { "type": "boolean" },
    "mode": { "type": "string", "enum": ["dry-run", "local"] },
    "readOnly": { "type": "boolean" },
    "provider": {
      "type": "object",
      "additionalProperties": true,
      "required": ["ecosystem", "smokeProfile", "command"],
      "properties": {
        "ecosystem": { "type": "string", "enum": ["npm", "python"] },
        "smokeProfile": { "type": "string", "enum": ["npm-package-smoke", "python-package-smoke"] },
        "command": { "const": "package.smoke" }
      }
    },
    "networkPolicy": {
      "type": "object",
      "additionalProperties": true,
      "required": ["mode", "enforced", "notes"],
      "properties": {
        "mode": { "type": "string", "enum": ["environment-inherited", "offline-requested", "offline-best-effort"] },
        "enforced": { "type": "boolean" },
        "notes": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        }
      }
    },
    "timeoutPolicy": {
      "type": "object",
      "additionalProperties": true,
      "required": ["scope", "defaultTimeoutSeconds", "effectiveTimeoutSeconds", "timeoutStepIds", "notes"],
      "properties": {
        "scope": { "const": "per-step" },
        "defaultTimeoutSeconds": { "type": "integer", "minimum": 1 },
        "effectiveTimeoutSeconds": { "type": "integer", "minimum": 1 },
        "timeoutStepIds": { "type": "array", "items": { "type": "string", "minLength": 1 } },
        "notes": { "type": "array", "items": { "type": "string", "minLength": 1 } }
      }
    },
    "execution": {
      "type": "object",
      "additionalProperties": true,
      "required": ["npmPackExecuted", "packageInstallExecuted", "featureSmokeExecuted", "releaseMutationExecuted", "publishExecuted"],
      "properties": {
        "npmPackExecuted": { "type": "boolean" },
        "pythonBuildExecuted": { "type": "boolean" },
        "twineCheckExecuted": { "type": "boolean" },
        "pipInstallExecuted": { "type": "boolean" },
        "packageInstallExecuted": { "type": "boolean" },
        "featureSmokeExecuted": { "type": "boolean" },
        "releaseMutationExecuted": { "const": false },
        "publishExecuted": { "const": false }
      }
    },
    "workspace": {
      "type": "object",
      "additionalProperties": true,
      "required": ["kind", "pathRedacted"],
      "properties": {
        "kind": { "type": "string", "enum": ["disposable", "project-source"] },
        "displayPath": { "type": "string", "minLength": 1 },
        "relativePath": { "type": "string", "minLength": 1 },
        "releaseInputHash": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
        "pathRedacted": { "const": true },
        "retention": { "type": "string", "enum": ["deleted", "kept-temporary", "private-local"] }
      }
    },
    "source": {
      "type": "object",
      "additionalProperties": true,
      "required": ["kind", "pathRedacted"],
      "properties": {
        "kind": { "type": "string", "enum": ["source-checkout", "tarball", "release-artifact"] },
        "displayPath": { "type": "string", "minLength": 1 },
        "relativePath": { "type": "string", "minLength": 1 },
        "pathRedacted": { "const": true }
      }
    },
    "rootRoles": {
      "type": "object",
      "additionalProperties": true,
      "required": ["sourceRoot", "evidenceRoot", "smokeProjectRoot"],
      "properties": {
        "sourceRoot": { "$ref": "#/$defs/rootRole" },
        "evidenceRoot": { "$ref": "#/$defs/rootRole" },
        "smokeProjectRoot": { "$ref": "#/$defs/rootRole" }
      }
    },
    "steps": {
      "type": "array",
      "items": { "$ref": "#/$defs/step" }
    },
    "artifacts": {
      "type": "array",
      "items": { "$ref": "#/$defs/artifact" }
    },
    "privacy": {
      "type": "object",
      "additionalProperties": true,
      "required": ["rawLogsIncluded", "rawPackageContentsIncluded", "privatePathsIncluded", "environmentSecretsIncluded", "privateStorePathsIncluded"],
      "properties": {
        "rawLogsIncluded": { "const": false },
        "rawPackageContentsIncluded": { "const": false },
        "privatePathsIncluded": { "const": false },
        "environmentSecretsIncluded": { "const": false },
        "privateStorePathsIncluded": { "const": false }
      }
    },
    "issues": {
      "type": "array",
      "items": { "$ref": "#/$defs/issue" }
    }
  },
  "$defs": {
    "rootRole": {
      "type": "object",
      "additionalProperties": true,
      "required": ["role", "displayPath", "pathRedacted", "fromOption"],
      "properties": {
        "role": { "type": "string", "enum": ["sourceRoot", "evidenceRoot", "smokeProjectRoot"] },
        "displayPath": { "type": "string", "minLength": 1 },
        "relativePath": { "type": "string", "minLength": 1 },
        "pathRedacted": { "const": true },
        "fromOption": { "type": "string", "enum": ["--source-root", "--evidence-root", "--smoke-project-root", "--project", "default-disposable"] }
      }
    },
    "step": {
      "type": "object",
      "additionalProperties": true,
      "required": ["id", "label", "status"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "label": { "type": "string", "minLength": 1 },
        "status": { "type": "string", "enum": ["planned", "passed", "failed", "skipped"] },
        "command": { "type": "string", "minLength": 1 },
        "exitCode": { "type": ["integer", "null"] },
        "elapsedMs": { "type": "integer" },
        "summary": { "type": "string", "minLength": 1 }
      }
    },
    "artifact": {
      "type": "object",
      "additionalProperties": true,
      "required": ["kind", "visibility"],
      "properties": {
        "kind": { "type": "string", "enum": ["summary", "command-log", "package-artifact", "install-tree"] },
        "visibility": { "type": "string", "enum": ["public", "private", "temporary"] },
        "evidencePath": { "type": "string", "pattern": "^tasks/T-[0-9]{4}-.+/artifacts/package-smoke/.+" },
        "relativePath": { "type": "string", "minLength": 1 },
        "pathRedacted": { "const": true },
        "rawContentIncluded": { "const": false },
        "byteLength": { "type": "integer" },
        "hash": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" }
      }
    },
    "issue": {
      "type": "object",
      "additionalProperties": true,
      "required": ["severity", "code", "message"],
      "properties": {
        "severity": { "type": "string", "enum": ["warning", "error"] },
        "code": { "type": "string", "minLength": 1 },
        "message": { "type": "string", "minLength": 1 },
        "stepId": { "type": "string", "minLength": 1 }
      }
    }
  }
}
