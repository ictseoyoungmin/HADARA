# T-0108 TUI Native Runtime Docs Assimilation

## Goal

Assimilate `docs/specs/HADARA_TUI_Mockup_Parity_HADARA_Native_Runtime_Design.md` into the main docs planning set with no summarized-away or omitted content, matching the way the core v1.0 technical plan was reflected into `docs/V1_0_IMPLEMENTATION_SCHEMAS.md`, `docs/V1_0_CAPSULE_BACKLOG.md`, and related docs.

## Scope

- Preserve the full TUI mockup parity / HADARA-native runtime design content in `docs/V1_0_IMPLEMENTATION_SCHEMAS.md` as an unabridged imported reference.
- Update `docs/V1_0_CAPSULE_BACKLOG.md` with concrete TUI follow-up capsule candidates from the spec.
- Update `docs/SCHEMAS.md` with the TUI cache/schema posture from the spec without promoting it to an enforced public schema.
- Update `docs/TEST_STRATEGY.md` with TUI-specific unit, performance, boundary, and cache validation requirements from the spec.
- Update task-local docs, evidence, board, state, and handoff.

## Out of Scope

- Code implementation of TUI cache, visual theme, mouse support, resize handling, compatibility CLI adapter, or performance benchmarks.
- Adding TUI cache schema fixtures or runtime validation.
- Changing TUI behavior, CLI behavior, or the current read-only command boundary.
- Removing or replacing the original spec file.

## Status

Done
