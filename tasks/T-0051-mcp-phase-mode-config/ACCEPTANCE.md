# Acceptance Criteria

- [x] Default initialize reports `hadara/readOnly: true` and `hadara/writes: false`.
- [x] Evidence attach-enabled initialize reports `hadara/readOnly: false`, `hadara/writes: true`, and `hadara/evidenceAttach: true`.
- [x] Both modes report `hadara/shellExecution: false` and `hadara/providerCalls: false`.
- [x] Instructions distinguish default read-only mode from evidence attach-enabled mode.
- [x] Required Docker validation passes.
- [x] Done-level capsule validation passes.
- [x] Evidence and handoff documents are updated.
