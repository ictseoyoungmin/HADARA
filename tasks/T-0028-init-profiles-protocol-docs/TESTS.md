# Tests

## Required

- Docker `npm ci && npm run check`
- Docker `node dist/cli/main.js harness validate --task T-0028 --json`

## Focused

- Init profile unit tests.

## Optional

- Built CLI smoke for `hadara init --profile full`.
