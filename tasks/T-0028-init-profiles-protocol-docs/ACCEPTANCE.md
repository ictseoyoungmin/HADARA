# Acceptance Criteria

- [x] `hadara init --profile minimal` is accepted.
- [x] `hadara init --profile full` is accepted.
- [x] `hadara init --profile hadara-protocol` is accepted as a protocol-heavy alias.
- [x] Unsupported init profiles are rejected.
- [x] Default/minimal init creates `docs/ARCHITECTURE.md`, `docs/IMPLEMENTATION_SOP.md`, and `docs/DEVELOPMENT_SLICES.md`.
- [x] Required Docker validation passes.
- [x] Evidence and handoff documents are updated.
