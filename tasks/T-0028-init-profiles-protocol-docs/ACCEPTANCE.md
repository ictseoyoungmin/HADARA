# Acceptance Criteria

- [x] `hadara init --profile basic` is accepted.
- [x] `hadara init --profile standard` is accepted.
- [x] `hadara init --profile governed` is accepted as the heavier governance profile.
- [x] Unsupported init profiles are rejected.
- [x] Default/standard init creates `docs/ARCHITECTURE.md`, `docs/IMPLEMENTATION_SOP.md`, and `docs/DEVELOPMENT_SLICES.md`.
- [x] Required Docker validation passes.
- [x] Evidence and handoff documents are updated.
