# Decisions

| Decision | Rationale |
|---|---|
| Default `hadara init` uses the standard profile. | Preserves current command shape while improving the baseline project contract. |
| `governed` is the heavier profile. | Gives users a scale-oriented profile name without adding project-specific behavior by default. |
