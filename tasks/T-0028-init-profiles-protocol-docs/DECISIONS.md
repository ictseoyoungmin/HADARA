# Decisions

| Decision | Rationale |
|---|---|
| Default `hadara init` uses the minimal profile. | Preserves current command shape while improving the baseline project contract. |
| `hadara-protocol` aliases the full profile. | Gives users a descriptive profile name without adding divergent behavior yet. |
