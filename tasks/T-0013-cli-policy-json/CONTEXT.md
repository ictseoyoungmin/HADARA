# Context

- The user asked to continue CLI JSON normalization for policy/hermes/evidence.
- `docs/AGENT_HANDOFF.md` recommends continuing CLI JSON normalization for policy/hermes/evidence commands.
- Policy is the safest next slice because the evaluator already exists and ShellTool execution is intentionally deferred.
- This slice leaves Hermes and Evidence JSON normalization for following capsules.

