# Tests

## Required

- Docker `npm ci`
- Docker `npm run check`
- Docker CLI smoke for allowed/ask policy JSON output.
- Docker CLI failure smoke for denied policy JSON output.

## Optional

- Human-readable/non-JSON policy output smoke.

