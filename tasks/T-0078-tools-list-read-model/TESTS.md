# Tests

## Required

- Docker `npm run check`
- Docker built CLI smoke: `node dist/cli/main.js tools list --json --project /workspace`
- Docker built CLI smoke: MCP `hadara.tools.list` call through `handleMcpJsonRpcMessage`

## Optional

- Done-level harness validation.
