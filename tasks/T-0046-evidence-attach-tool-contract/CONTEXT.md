# Context

T-0044 implemented read-only MCP tools. T-0045 added contract tests for MCP payload wrapping, selected CLI JSON parity, notification behavior, and dispatch issue mapping.

The next roadmap slice is T-0046: a contract-only future evidence attach tool. This task must not add write-capable runtime behavior.

Review feedback requested explicit future error taxonomy for write-capable MCP tools and clarification that `hadara.policy.evaluate` is not execution permission.
