# Tests

## Required

- Docker `npx vitest run tests/unit/redaction.test.ts`.
- Docker `npm run check`.
- Docker `node dist/cli/main.js harness validate --task T-0075 --level done --json --project /workspace`.

## Optional

- None.
