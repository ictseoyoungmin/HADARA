# Decisions

- Project and handoff read models now live under `src/services/` rather than inside MCP adapter code.
- MCP remains an adapter over shared/domain report builders.
- This slice does not add new external surface area.
