# Tests

## Required

- Docker `npm ci && npm run check`
- Docker `node dist/cli/main.js harness validate --task T-0032 --level done --json`

## Focused

- Harness validation tests.
- Built CLI harness validate/replay smokes.

## Optional

- LOC check for `src/cli/main.ts`.
