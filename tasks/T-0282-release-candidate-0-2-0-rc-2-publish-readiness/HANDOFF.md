# Handoff

## Current State

| Field | Value |
|---|---|
| Task | T-0282 |
| Status | Done |
| Last Updated | 2026-06-07 |

## Last Completed

| Item | Evidence |
|---|---|
| npm package metadata and docs now target `hadara@0.2.0-rc.2`. | `package.json`, `package-lock.json`, README, release notes, release readiness docs, and manual helper examples updated. |
| Docker build/check and workspace `dist` refresh passed. | `npm run dev:docker-sync-build` passed 100 files / 681 tests and built version smoke reported `0.2.0-rc.2`, `distLooksStale:false`. |
| rc2 package smoke, clean-checkout smoke, npm pack dry-run, and npm registry E404 check passed. | T-0282 evidence and reduced public artifacts under `artifacts/package-smoke/` and `artifacts/clean-checkout-smoke/`. |
| release dry-run has one expected pre-helper blocker. | Release-artifact evidence still points to T-0275 rc1 and must be regenerated by the clean-worktree manual publish helper before npm publish. |

## Next Recommended Step

| Step | Reason | Required Reading |
|---|---|---|
| Commit the T-0282 readiness state, then run `npm login --registry=https://registry.npmjs.org` and `scripts/release/manual-publish-rc.sh T-0282 --execute`. | The helper requires a clean git worktree, regenerates release artifact/package/clean evidence, reruns dry-run gates, prompts for `publish`, publishes to npm, and verifies `npm view`. | `docs/RELEASE_READINESS.md`, `scripts/release/manual-publish-rc.sh`, `tasks/T-0282-release-candidate-0-2-0-rc-2-publish-readiness/TESTS.md`. |
| Optionally add `--github-draft --github-release-note tasks/T-0282-release-candidate-0-2-0-rc-2-publish-readiness/GITHUB_RELEASE_NOTE.md`. | GitHub Release remains secondary and operator-confirmed. | `GITHUB_RELEASE_NOTE.md`. |

## Carry Forward Warnings

| Warning | Impact | Mitigation |
|---|---|---|
| `scripts/release/manual-publish-rc.sh` exits if the git worktree is dirty. | Running it before committing this readiness state will fail before npm publish. | Commit or otherwise clean the worktree first. |
| Release dry-run is blocked before the helper regenerates release-artifact evidence. | This is expected for rc2 because current release-artifact evidence is from T-0275 rc1. | Let the manual helper run `release artifact --execute --attach-evidence --task T-0282` from a clean committed worktree. |
| Earlier package/clean smokes failed under sandbox cache/DNS limits. | Failed evidence remains visible. | Later reruns passed with `/tmp` npm cache and network-capable execution; resolution evidence records the accepted residual risk and no publish mutation. |
