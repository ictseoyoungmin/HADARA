# Acceptance Criteria

- [x] `hadara doctor --json` returns a stable JSON envelope.
- [x] Human-readable `hadara doctor` still works.
- [x] Doctor report includes path and filesystem check statuses.
- [x] Tests or explicit constraints are recorded.
- [x] Evidence is attached to `EVIDENCE.md` and `evidence.jsonl`.
- [x] Handoff is updated.
