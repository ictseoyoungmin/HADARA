# Context

- `docs/AGENT_HANDOFF.md` recommends continuing CLI JSON normalization.
- `docs/DEVELOPMENT_SLICES.md` lists CLI JSON normalization as slice 3.
- `docs/specs/HADARA_실제_개발_사양서_v0.2.md` defines `--json` as a common CLI option and `doctor --json` as a core environment check surface.
- This slice establishes the pattern on `doctor` before broader CLI command normalization.

