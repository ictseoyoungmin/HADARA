# Plan

1. Move long completed-task history into `docs/HANDOFF_HISTORY.md`.
2. Move accumulated validation lines into `docs/VALIDATION_HISTORY.md`.
3. Replace `docs/AGENT_HANDOFF.md` with compact current sections.
4. Add compaction policy to `docs/IMPLEMENTATION_SOP.md`.
5. Run validation and update evidence.
