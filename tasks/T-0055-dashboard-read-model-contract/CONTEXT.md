# Context

T-0053 introduced Operations Status JSON. T-0054 hardened degraded-source warning behavior and stable dashboard-facing counts.

T-0055 defines how a future dashboard should consume that read model. It must not implement dashboard UI.

Design reference:

- `docs/design/mockups/HADARA_web_ui_v0.1_comfort_dark.html`

The mockup is visual direction only. The dashboard data source for this slice is `hadara.ops.status.v1`.
