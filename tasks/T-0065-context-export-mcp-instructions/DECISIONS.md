# Decisions

- Context export should prefer stable read models over raw capsule scraping.
- MCP unavailable is an expected case; exported context explicitly allows fallback to CLI JSON and repository documents.
- Export guidance remains read-only by default and does not promote evidence attach or any other write-capable MCP behavior.
