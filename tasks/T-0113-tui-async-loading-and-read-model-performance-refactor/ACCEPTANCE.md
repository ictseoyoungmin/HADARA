# Acceptance Criteria

- [x] TUI startup, refresh, and selected-detail terminal paths use a fast read-model profile rather than rebuilding the full advisory aggregate.
- [x] Fast TUI read model defers debt/release/tools/write-preview reads with an explicit warning issue.
- [x] TUI cache write/indexing tolerates a missing capsule `TASK.md` and can still produce a fast cache hit.
- [x] Before/after timing evidence is recorded for `/workspace`.
- [x] Focused tests and full Docker validation pass.
- [x] Evidence and handoff are updated.
