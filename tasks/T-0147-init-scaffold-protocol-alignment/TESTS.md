# Tests

## Required

- Docker focused `npx vitest run tests/unit/init.test.ts` passed with 1 file and 7 tests.
- Docker `npm run check` passed with 57 files and 409 tests.
- Built CLI `hadara init` smoke
- Built CLI done-level harness validation for T-0147

## Optional

- Inspect generated scaffold file list for Hermes absence and `.gitignore` presence
