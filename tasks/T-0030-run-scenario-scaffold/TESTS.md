# Tests

## Required

- Docker `npm ci && npm run check`
- Docker `node dist/cli/main.js harness validate --task T-0030 --level done --json`

## Focused

- Run scaffold unit tests.

## Optional

- Built CLI smoke that scaffolds a scenario and runs it with `hadara run`.
