# Decisions

- Treat exact versions in historical docs/evidence as intentional records.
- Treat exact current versions in source readiness checks as drift risk unless they represent a version policy pattern.
- Keep release gate checks read-only and wording-marker based for now, but avoid pinning the current RC number where package metadata can supply it.
