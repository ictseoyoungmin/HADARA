# Context

The roadmap calls for a single active agent/session model. Previous handoff behavior can overfit to the last completed capsule, so T-0068 introduces a small local manifest and read projection that tells agents what to resume and whether the handoff mentions that active task.
