# HADARA v0.5.0-rc.4

Pre-release candidate after the Init v1 scaffold/protocol reconciliation.

Highlights:

- Fresh minimal, standard, and governed Init v1 projects now pass protocol doctor without warnings.
- Init v1 uses `.hadara/context/READ_MAP.md` while legacy scaffold routing remains compatible.
- Release artifact, package smoke, clean-checkout smoke, and release dry-run evidence are regenerated
  from the RC4 source input.

This note is prepared for operator review. npm publication, GitHub Release mutation, and public
consumer recycle are not performed by the pre-operator readiness capsule.
