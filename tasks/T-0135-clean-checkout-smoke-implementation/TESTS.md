# Tests

## Required

- Docker focused clean-checkout smoke/schema/tools tests.
- Docker `npm run check`.
- Built CLI `hadara smoke clean-checkout --execute --json` smoke in a disposable copy.
- Built CLI strict release gate smoke.
- Done-level harness validation for T-0135.

## Optional

- Built CLI failure smoke using an invalid workspace or intentionally failing runner.
