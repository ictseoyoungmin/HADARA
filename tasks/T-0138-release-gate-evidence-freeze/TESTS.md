# Tests

## Required

- Focused Docker tests for release gate, schema runtime, and schema fixtures.
- Docker `npm run check`.
- Built CLI `release gate --mode strict --json` smoke.
- Done-level harness validation.

## Optional

- Built CLI missing-evidence fixture smoke if focused tests do not cover the behavior clearly.
