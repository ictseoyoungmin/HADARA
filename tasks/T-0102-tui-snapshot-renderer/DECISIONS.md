# Decisions

- Keep snapshot output no-color only in this slice.
- Return an internal snapshot envelope with `text` and fixed-width `lines`, but do not promote it to a public external schema.
- Render all four planned panels now, leaving interactive navigation and CLI entry point to later capsules.
