# Plan

1. Extract init profile logic from `src/cli/main.ts`.
2. Extract run scaffold logic from `src/cli/main.ts`.
3. Keep `main.ts` as the dispatcher for now.
4. Update focused tests.
5. Run Docker validation and done-level capsule validation.
6. Update evidence, project state, task board, slices, and handoff.
