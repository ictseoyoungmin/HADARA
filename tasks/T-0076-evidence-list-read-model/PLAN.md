# Plan

1. Read HADARA protocol docs, v1.0 backlog/schema notes, and existing evidence/MCP JSON surfaces.
2. Define the evidence list read model in a shared service.
3. Add CLI `hadara evidence list --json` and read-only MCP `hadara.evidence.list`.
4. Add focused unit and contract tests for report shape and degraded reads.
5. Run Docker validation, attach evidence, and update project handoff/state docs.
