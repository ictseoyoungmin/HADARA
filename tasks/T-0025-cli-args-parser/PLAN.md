# Plan

1. Add strict CLI args helpers and unit tests.
2. Replace `getOption()` usage in `src/cli/main.ts`.
3. Keep run max-step validation on the new integer helper.
4. Add regression coverage for missing values and flag-like values.
5. Run Docker check and T-0025 harness validation.
6. Update evidence, board, project state, slices, and handoff.
