# Acceptance Criteria

- [x] `hadara.event.v1` model helpers exist with stable normalization and JSONL-safe serialization.
- [x] Existing audit JSONL writes include a structured `event` payload while retaining current compatibility fields.
- [x] `hadara.event.v1` JSON Schema fixture is registered and covered by runtime schema tests.
- [x] Focused tests cover event redaction and audit compatibility.
- [x] Evidence is attached to `EVIDENCE.md` and `evidence.jsonl`.
- [x] Project board/state/slices and handoff docs are updated before stopping.
