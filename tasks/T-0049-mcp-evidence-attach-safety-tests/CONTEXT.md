# Context

T-0048 implemented `hadara.evidence.attach` behind explicit `--enable-evidence-attach` opt-in and reused the existing evidence collect report path.

T-0049 validates that the implementation preserves the contract safety gates around JSON payload shape, artifact boundary checks, redaction policy, and schema validation.
