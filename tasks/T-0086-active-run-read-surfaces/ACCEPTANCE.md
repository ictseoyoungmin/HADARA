# Acceptance Criteria

- [x] Scope is implemented.
- [x] `hadara run-state show --json` returns `hadara.active_run.projection.v1` without writing files.
- [x] MCP `hadara.active.run.read` returns the same active-run projection through a JSON text payload.
- [x] MCP `hadara.active.run.resume` returns read-only resume guidance from the active-run projection.
- [x] Tools discovery lists the new read-only CLI/MCP surfaces.
- [x] Tests or explicit constraints are recorded.
- [x] Evidence is attached.
- [x] Handoff is updated.
