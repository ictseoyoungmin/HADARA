# Plan

1. Read HADARA session docs, v1.0 planning references, and current policy implementation.
2. Define T-0090 scope and acceptance around a compatibility-preserving policy matrix refactor.
3. Split tokenizer, presets, command-risk, and permission-matrix logic out of `src/policy/policy.ts`.
4. Add focused unit coverage for command-risk categories and existing decisions.
5. Run focused policy tests, then broader Docker validation if feasible.
6. Attach evidence, validate the Task Capsule, and update board/state/handoff docs.
