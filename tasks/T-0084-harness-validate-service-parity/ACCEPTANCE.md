# Acceptance Criteria

- [x] Harness validate report creation is available from a shared service.
- [x] CLI `harness validate` routes through the shared service.
- [x] MCP `hadara.harness.validate` routes through the shared service.
- [x] MCP harness validate parity is covered against the shared service.
- [x] MCP `hadara.task.read` excludes private evidence metadata by default and supports `includePrivate`.
- [x] The sanitized `files["evidence.jsonl"]` behavior is documented.
- [x] Tests or explicit constraints are recorded.
- [x] Evidence is attached.
- [x] Handoff is updated.
