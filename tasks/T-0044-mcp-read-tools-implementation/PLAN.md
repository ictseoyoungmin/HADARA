# Plan

1. Read MCP and CLI JSON contracts.
2. Create a focused T-0044 Task Capsule.
3. Split MCP tool schemas, registry, and dispatch out of `server.ts`.
4. Implement read-only tool handlers and strict `tools/call` input validation.
5. Add focused unit tests for successful read tools and dispatch error mapping.
6. Run Docker validation and done-level capsule validation.
7. Update evidence, roadmap/state tracking, and handoff.
