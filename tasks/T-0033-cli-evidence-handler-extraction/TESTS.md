# Tests

## Required

- Docker `npm ci && npm run check`
- Docker `node dist/cli/main.js harness validate --task T-0033 --level done --json`

## Focused

- Evidence JSON unit tests.
- Built CLI evidence collect JSON/text smokes.

## Optional

- LOC check for `src/cli/main.ts`.
