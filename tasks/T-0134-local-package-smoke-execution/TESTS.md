# Tests

## Required

- Docker focused package-smoke/schema/tools tests.
- Docker `npm run check`.
- Built CLI `hadara package smoke --execute --json` smoke in a disposable copy.
- Built CLI strict release gate smoke.
- Done-level harness validation for T-0134.

## Optional

- Additional local CLI failure smoke for invalid source or intentionally failing installed command.
