# Decisions

- Manual `evidence.jsonl` records must use the same `hadara.evidence.v1` shape produced by `appendEvidence()`: `schemaVersion`, `time`, `taskId`, `kind`, `summary`, `result`, and `visibility`.
