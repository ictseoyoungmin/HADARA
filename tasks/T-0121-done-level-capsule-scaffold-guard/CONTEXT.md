# Context

- Required project protocol docs were read: `docs/PROJECT_STATE.md`, `docs/AGENT_HANDOFF.md`, `docs/TASK_BOARD.md`, `docs/IMPLEMENTATION_SOP.md`, and `docs/DEVELOPMENT_SLICES.md`.
- T-0120 is complete; this work is a new harness hardening slice.
- The existing done-level validator already checks status, acceptance, evidence records, handoff placeholders, and Task Board row consistency.
- The remaining gap is that several standard capsule Markdown files can remain exactly as generated by `hadara task create` while done-level validation still passes.
