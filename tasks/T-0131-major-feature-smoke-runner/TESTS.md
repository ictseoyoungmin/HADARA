# Tests

## Required

- Docker temp-copy `npm run check`.
- Built CLI `hadara smoke run --profile core --json`.
- Built CLI `hadara smoke run --profile release-readiness --json`.
- Done-level harness validation for T-0131.

## Optional

- Focused feature-smoke/schema/tools-list unit tests during implementation.
- `git diff --check`.
