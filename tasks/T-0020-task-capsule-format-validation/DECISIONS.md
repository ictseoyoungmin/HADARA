# Decisions

- Enforce capsule format through harness validation instead of relying only on agent memory.
- Use error severity for format drift because it can break session continuity.
- Keep the checks structural and minimal.
