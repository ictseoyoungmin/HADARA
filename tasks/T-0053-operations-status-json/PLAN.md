# Plan

1. Create the T-0053 Task Capsule.
2. Add design reference docs and keep the mockup as a reference artifact.
3. Define `hadara.ops.status.v1` in `docs/OPERATIONS_STATUS_CONTRACT.md`.
4. Implement `hadara status --json` and `hadara ops status --json`.
5. Add focused unit tests for the status JSON read model.
6. Run Docker validation and done-level capsule validation.
7. Update evidence, state tracking, and handoff.
