# Context

T-0044 implemented the read-only MCP tools and split schemas, registry, and dispatch.

T-0045 strengthens that bridge by testing MCP outputs against existing HADARA CLI JSON contracts where those builders exist. The task also locks in JSON-RPC notification behavior and the initial HADARA issue-code mapping for tool dispatch failures.
