# Acceptance Criteria

- [x] Docker-based dependency install succeeds.
- [x] `npm run check` succeeds in Docker.
- [x] `npm run dev -- doctor` succeeds with `HADARA_HOME` outside the repo.
- [x] `npm run dev -- init` succeeds with `HADARA_PROJECT_ROOT` set to the repo.
- [x] `task create`, `task list`, and `task show` succeed.
- [x] `evidence collect` succeeds.
- [x] `handoff update` succeeds.
- [x] Results are recorded in `EVIDENCE.md`.
