# Acceptance Criteria

- [x] `docs/SCHEMAS.md` defines schema registry boundaries, ids, versioning, and non-goals.
- [x] `src/schemas/schema-index.json` lists initial schema fixtures.
- [x] Initial JSON Schema fixtures exist for `hadara.evidence.list.v1`, `hadara.context.export.v1`, and `hadara.tools.list.v1`.
- [x] Tests verify schema files parse and the index points at existing aligned fixtures.
- [x] Evidence is attached.
- [x] Handoff is updated.
