# Context

- `docs/PROJECT_STATE.md` says HADARA is a bootstrap skeleton at the Phase 0 / Phase 1 boundary.
- `docs/AGENT_HANDOFF.md` recommends harness replay or CLI `--json` stabilization before ShellTool, provider fallback, dashboard, MCP body, or real adapters.
- `docs/specs/HADARA_실제_개발_사양서_v0.2.md` requires `hadara harness validate --task <id> --json` as an external agent/CI contract.
- Docker is the preferred validation path for Node/npm in this environment.

