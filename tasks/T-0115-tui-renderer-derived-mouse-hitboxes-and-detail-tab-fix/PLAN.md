# Plan

1. Read HADARA protocol docs, current TUI state, v1 planning notes, and mockup hitbox implementation.
2. Add renderer-derived hitbox metadata without changing the rendered frame.
3. Update terminal mouse handling to consume renderer hitboxes for panels, tasks, and Detail document tabs.
4. Add focused regressions for compact and wide hitbox behavior.
5. Run focused TUI checks, full check if feasible, attach evidence, and update project handoff/state docs.
