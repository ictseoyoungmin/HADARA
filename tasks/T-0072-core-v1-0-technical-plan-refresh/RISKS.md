# Risks

| Risk | Mitigation |
|---|---|
| Future implementers could treat future target schemas as already implemented contracts. | Label current implementation and future expansion separately in the planning docs. |
| Documentation-only work could drift from actual code paths. | Cross-check paths and schema names against `src/` and tests before completion. |
