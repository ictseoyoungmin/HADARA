# Tests

## Required

- Docker `npx vitest run tests/unit/dashboard-static.test.ts`
- Docker `npm run check`

## Optional

- Built CLI smoke for `hadara dashboard serve` only if route helper tests leave server wiring uncertain.
