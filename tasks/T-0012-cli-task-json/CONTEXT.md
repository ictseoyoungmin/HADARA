# Context

- `docs/AGENT_HANDOFF.md` recommends continuing CLI JSON normalization.
- `docs/DEVELOPMENT_SLICES.md` lists CLI JSON normalization as the current active development theme.
- T-0011 established the `doctor --json` pattern.
- This slice extends the pattern to read-only Task Capsule commands.

