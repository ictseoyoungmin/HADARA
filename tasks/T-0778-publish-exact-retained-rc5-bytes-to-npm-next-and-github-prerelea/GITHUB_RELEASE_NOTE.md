# HADARA 0.5.0-rc.5

HADARA 0.5.0-rc.5 is a release-candidate build of the HADARA CLI and project protocol.

## Install

```bash
npm install -g hadara@next
hadara version --json
```

## Validation

- The retained RC5 tarball, checksum, and manifest were verified before publication.
- Publication is verified with `npm view hadara@0.5.0-rc.5 version` and the `next` dist-tag.
- A disposable public consumer recycle verifies initialization, task lifecycle, evidence, close execution, idempotent retry, and fresh status.

## Notes

This note intentionally contains no operator credentials, private paths, or raw consumer logs.
