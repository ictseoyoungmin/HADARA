# Context

Feedback identified that `harness validate` is strong at structure checks but weak at completion checks. Draft validation should remain useful while work is ongoing. Done validation should reject capsules that still have unchecked acceptance criteria, no evidence records, placeholder handoff, or non-Done task status.

This task implements the smallest useful completion gate.
