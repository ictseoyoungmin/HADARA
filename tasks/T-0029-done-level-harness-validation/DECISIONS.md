# Decisions

| Decision | Rationale |
|---|---|
| Default validation level is `draft`. | Preserves compatibility with current Task Capsule validation use. |
| Done-level evidence requires at least one JSONL record. | `evidence.jsonl` is the machine-readable completion index. |
