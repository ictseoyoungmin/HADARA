# Decisions

- Treat this as a cleanup/clarification capsule, not an implementation capsule for redaction injection, schema validation, task.read evidence normalization, or policy matrix work.
- Preserve the T-0080 wording that Service Parity Expansion is in progress/first increment work, not complete.
- Use `fixture`, `contract`, and `releaseGate` as future schema strictness levels.
- Keep MCP `hadara.policy.evaluate` terminology as a tool name that returns `policy.preflight-shell`; it must not imply command execution.
