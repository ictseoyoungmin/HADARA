# Risks

| Risk | Impact | Likelihood | Mitigation | Status |
|---|---|---|---|---|
| Host dashboard bundle build cannot run without local dependencies. | Host-local build remains unavailable if `node_modules` is absent. | Medium | Docker dashboard build passed and rebuilt served HTML; use Docker workflow unless host dependencies are intentionally installed. | Mitigated |
| Full Docker validation and Playwright/axe visual gate were previously blocked. | Projection code/tests added since T-0217 needed a successful full reproducible validation pass. | Medium | Docker sync-build passed 90 files / 582 tests; Docker visual/a11y gate passed all projection-ready/detail/stale/refreshing/missing/offline/degraded states. | Closed |
| Projection status fixtures are currently gate inputs more than user-visible UI controls. | Future UI could stop surfacing stale/refreshing/missing metadata while the gate still stubs the route. | Medium | Static tests pin route stubbing and screenshots; future frontend status rendering should add explicit locator assertions once the UI consumes `/api/dashboard/projection/status`. | Tracked |
| Manual projection refresh still has synchronous scan work inside yielded stages. | An explicit `/api/dashboard/refresh` no longer runs the full task/heavy/core chain in one event-loop turn, but individual stages can still be costly on slow mounts. | Medium | Serve-start warmup is delayed core-only and manual refresh yields between stages; future core refactor can chunk task discovery or offload heavy projection rebuilds if `/mnt/f` remains visibly slow. | Tracked |
| Selected capsule detail previously reused global workbench/timeline work. | Detail view could spend many seconds loading one capsule and then show unavailable if the aggregate timed out or degraded. | Medium | Task-detail aggregation now builds selected-task fast workbench data and task-scoped timeline events; built `dist` smoke returned T-0223 detail in 1852 ms with `ok:true`. | Mitigated |
| Markdown table headers can leak into dashboard summary cards. | Home/timeline summary text can show `| Area | State | Notes |` instead of meaningful handoff rows. | Medium | Handoff section parsing is table-aware in dashboard core and operations status services; Docker tests and built smoke cover data-row output. | Mitigated |
