# Context

`--enable-evidence-attach` is server-level operator opt-in. T-0052 adds a lightweight per-call approval record so Hermes or other MCP callers must include an explicit approval actor and reason for every write attempt.
