# Plan

1. Read required project docs plus `.mockup/tui` and `.mockup/tui-final` reference code.
2. Add theme primitives and ANSI-width-safe layout helpers.
3. Apply mockup-style visual polish, status/log line, and loading frames to the snapshot renderer and terminal shell.
4. Extend production key handling and ensure selection/search state drives rendered rows.
5. Add focused regression tests for snapshot themes/loading, state keys, terminal decoding/loading, and CLI theme behavior.
6. Run focused TUI tests and full Docker validation.
7. Record evidence and update handoff/state docs.
