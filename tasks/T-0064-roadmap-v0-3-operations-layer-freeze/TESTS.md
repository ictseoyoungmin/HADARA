# Tests

## Required

- Text scan for v0.3 freeze, deferred exclusions, and operational debt sections.
- Docker `npm run build`.
- Docker `node dist/cli/main.js harness validate --task T-0064 --level done --json`.

## Focused

- ROADMAP section review for v0.3-v1.0 candidate scopes.

## Optional

- Full Docker `npm ci && npm run check` when paired with adjacent code-bearing context export work.
