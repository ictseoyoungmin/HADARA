# Context

`docs/specs/HADARA_실제_개발_사양서_v0.3.md` defines v0.3 as a single-active-agent operations layer with read-only MCP/CLI surfaces, context export, compatibility fixture work, and static dashboard support.

The previous roadmap still used broad Phase 0-3 language. It needed a sharper freeze boundary so near-term work does not drift into real provider execution, broad MCP writes, live dashboard streaming, or multi-agent concurrency too early.
