# T-0064 Roadmap v0.3 Operations Layer Freeze

## Goal

Freeze v0.3 as a read-only operations layer before broader product and provider expansion.

## Scope

- Update `docs/ROADMAP.md` so v0.3 Operations Layer is the current milestone.
- Separate v0.3, v0.4, v0.5, v0.6, v0.7, v0.8, and v1.0 candidate scopes.
- Explicitly record v0.3 in-scope and out-of-scope boundaries.
- Move real provider adapter preparation behind the v0.3 follow-up slices in `docs/DEVELOPMENT_SLICES.md`.
- Add an Operational Debt Track derived from `known_issue.log`.

## Out of Scope

- Code behavior changes.
- MCP write tools.
- MCP shell execution.
- MCP release/package execution.
- Real provider execution as the default path.
- Live dashboard streaming.
- Multi-agent concurrent execution.

## Status

Done
