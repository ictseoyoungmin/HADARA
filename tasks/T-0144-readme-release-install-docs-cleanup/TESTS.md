# Tests

## Required

- Docker `npm run check`
- Built CLI done-level harness validation for T-0144

## Optional

- Built CLI README-relevant smoke checks such as `hadara doctor --json` or `hadara install plan --platform linux --json`
