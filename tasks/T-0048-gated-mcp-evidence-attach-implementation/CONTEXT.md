# Context

T-0046 documented the future evidence attach contract. T-0047 added guard tests proving evidence attach was not advertised or callable by default.

T-0048 may implement evidence attach only as a narrow, explicitly enabled MCP write tool. The default MCP runtime must remain read-only.
