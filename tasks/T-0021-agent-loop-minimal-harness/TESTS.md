# Tests

## Required

- Docker `npm ci`
- Docker `npm run check`
- Docker `hadara run ... --json` fake shell smoke.
- Docker `hadara harness validate --task T-0021 --json`

## Optional

- Targeted agent loop unit test.
